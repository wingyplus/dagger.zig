// Code generated by dagger-zig-codegen. DO NOT EDIT.
const std = @import("std");

pub const QueryBuilder = struct {
    pub fn selection(self: *QueryBuilder, name: []const u8, args: anytype) !*QueryBuilder {
        _ = self;
        _ = name;
        _ = args;
        return error.Unimplemented;
    }
};

/// The `AddressID` scalar type represents an identifier for an object of type Address.
pub const AddressID = struct { value: []const u8 };

/// The `BindingID` scalar type represents an identifier for an object of type Binding.
pub const BindingID = struct { value: []const u8 };

/// The `CacheVolumeID` scalar type represents an identifier for an object of type CacheVolume.
pub const CacheVolumeID = struct { value: []const u8 };

/// The `ChangesetID` scalar type represents an identifier for an object of type Changeset.
pub const ChangesetID = struct { value: []const u8 };

/// The `CheckGroupID` scalar type represents an identifier for an object of type CheckGroup.
pub const CheckGroupID = struct { value: []const u8 };

/// The `CheckID` scalar type represents an identifier for an object of type Check.
pub const CheckID = struct { value: []const u8 };

/// The `CloudID` scalar type represents an identifier for an object of type Cloud.
pub const CloudID = struct { value: []const u8 };

/// The `ContainerID` scalar type represents an identifier for an object of type Container.
pub const ContainerID = struct { value: []const u8 };

/// The `CurrentModuleID` scalar type represents an identifier for an object of type CurrentModule.
pub const CurrentModuleID = struct { value: []const u8 };

/// The `DiffStatID` scalar type represents an identifier for an object of type DiffStat.
pub const DiffStatID = struct { value: []const u8 };

/// The `DirectoryID` scalar type represents an identifier for an object of type Directory.
pub const DirectoryID = struct { value: []const u8 };

/// The `EngineCacheEntryID` scalar type represents an identifier for an object of type EngineCacheEntry.
pub const EngineCacheEntryID = struct { value: []const u8 };

/// The `EngineCacheEntrySetID` scalar type represents an identifier for an object of type EngineCacheEntrySet.
pub const EngineCacheEntrySetID = struct { value: []const u8 };

/// The `EngineCacheID` scalar type represents an identifier for an object of type EngineCache.
pub const EngineCacheID = struct { value: []const u8 };

/// The `EngineID` scalar type represents an identifier for an object of type Engine.
pub const EngineID = struct { value: []const u8 };

/// The `EnumTypeDefID` scalar type represents an identifier for an object of type EnumTypeDef.
pub const EnumTypeDefID = struct { value: []const u8 };

/// The `EnumValueTypeDefID` scalar type represents an identifier for an object of type EnumValueTypeDef.
pub const EnumValueTypeDefID = struct { value: []const u8 };

/// The `EnvFileID` scalar type represents an identifier for an object of type EnvFile.
pub const EnvFileID = struct { value: []const u8 };

/// The `EnvID` scalar type represents an identifier for an object of type Env.
pub const EnvID = struct { value: []const u8 };

/// The `EnvVariableID` scalar type represents an identifier for an object of type EnvVariable.
pub const EnvVariableID = struct { value: []const u8 };

/// The `ErrorID` scalar type represents an identifier for an object of type Error.
pub const ErrorID = struct { value: []const u8 };

/// The `ErrorValueID` scalar type represents an identifier for an object of type ErrorValue.
pub const ErrorValueID = struct { value: []const u8 };

/// The `FieldTypeDefID` scalar type represents an identifier for an object of type FieldTypeDef.
pub const FieldTypeDefID = struct { value: []const u8 };

/// The `FileID` scalar type represents an identifier for an object of type File.
pub const FileID = struct { value: []const u8 };

/// The `FunctionArgID` scalar type represents an identifier for an object of type FunctionArg.
pub const FunctionArgID = struct { value: []const u8 };

/// The `FunctionCallArgValueID` scalar type represents an identifier for an object of type FunctionCallArgValue.
pub const FunctionCallArgValueID = struct { value: []const u8 };

/// The `FunctionCallID` scalar type represents an identifier for an object of type FunctionCall.
pub const FunctionCallID = struct { value: []const u8 };

/// The `FunctionID` scalar type represents an identifier for an object of type Function.
pub const FunctionID = struct { value: []const u8 };

/// The `GeneratedCodeID` scalar type represents an identifier for an object of type GeneratedCode.
pub const GeneratedCodeID = struct { value: []const u8 };

/// The `GeneratorGroupID` scalar type represents an identifier for an object of type GeneratorGroup.
pub const GeneratorGroupID = struct { value: []const u8 };

/// The `GeneratorID` scalar type represents an identifier for an object of type Generator.
pub const GeneratorID = struct { value: []const u8 };

/// The `GitRefID` scalar type represents an identifier for an object of type GitRef.
pub const GitRefID = struct { value: []const u8 };

/// The `GitRepositoryID` scalar type represents an identifier for an object of type GitRepository.
pub const GitRepositoryID = struct { value: []const u8 };

/// The `HealthcheckConfigID` scalar type represents an identifier for an object of type HealthcheckConfig.
pub const HealthcheckConfigID = struct { value: []const u8 };

/// The `HostID` scalar type represents an identifier for an object of type Host.
pub const HostID = struct { value: []const u8 };

/// The `InputTypeDefID` scalar type represents an identifier for an object of type InputTypeDef.
pub const InputTypeDefID = struct { value: []const u8 };

/// The `InterfaceTypeDefID` scalar type represents an identifier for an object of type InterfaceTypeDef.
pub const InterfaceTypeDefID = struct { value: []const u8 };

/// An arbitrary JSON-encoded value.
pub const JSON = struct { value: []const u8 };

/// The `JSONValueID` scalar type represents an identifier for an object of type JSONValue.
pub const JSONValueID = struct { value: []const u8 };

/// The `LLMID` scalar type represents an identifier for an object of type LLM.
pub const LLMID = struct { value: []const u8 };

/// The `LLMTokenUsageID` scalar type represents an identifier for an object of type LLMTokenUsage.
pub const LLMTokenUsageID = struct { value: []const u8 };

/// The `LabelID` scalar type represents an identifier for an object of type Label.
pub const LabelID = struct { value: []const u8 };

/// The `ListTypeDefID` scalar type represents an identifier for an object of type ListTypeDef.
pub const ListTypeDefID = struct { value: []const u8 };

/// The `ModuleConfigClientID` scalar type represents an identifier for an object of type ModuleConfigClient.
pub const ModuleConfigClientID = struct { value: []const u8 };

/// The `ModuleID` scalar type represents an identifier for an object of type Module.
pub const ModuleID = struct { value: []const u8 };

/// The `ModuleSourceID` scalar type represents an identifier for an object of type ModuleSource.
pub const ModuleSourceID = struct { value: []const u8 };

/// The `ObjectTypeDefID` scalar type represents an identifier for an object of type ObjectTypeDef.
pub const ObjectTypeDefID = struct { value: []const u8 };

/// The platform config OS and architecture in a Container.
///
/// The format is [os]/[platform]/[version] (e.g., "darwin/arm64/v7", "windows/amd64", "linux/arm64").
pub const Platform = struct { value: []const u8 };

/// The `PortID` scalar type represents an identifier for an object of type Port.
pub const PortID = struct { value: []const u8 };

/// The `QueryID` scalar type represents an identifier for an object of type Query.
pub const QueryID = struct { value: []const u8 };

/// The `SDKConfigID` scalar type represents an identifier for an object of type SDKConfig.
pub const SDKConfigID = struct { value: []const u8 };

/// The `ScalarTypeDefID` scalar type represents an identifier for an object of type ScalarTypeDef.
pub const ScalarTypeDefID = struct { value: []const u8 };

/// The `SearchResultID` scalar type represents an identifier for an object of type SearchResult.
pub const SearchResultID = struct { value: []const u8 };

/// The `SearchSubmatchID` scalar type represents an identifier for an object of type SearchSubmatch.
pub const SearchSubmatchID = struct { value: []const u8 };

/// The `SecretID` scalar type represents an identifier for an object of type Secret.
pub const SecretID = struct { value: []const u8 };

/// The `ServiceID` scalar type represents an identifier for an object of type Service.
pub const ServiceID = struct { value: []const u8 };

/// The `SocketID` scalar type represents an identifier for an object of type Socket.
pub const SocketID = struct { value: []const u8 };

/// The `SourceMapID` scalar type represents an identifier for an object of type SourceMap.
pub const SourceMapID = struct { value: []const u8 };

/// The `StatID` scalar type represents an identifier for an object of type Stat.
pub const StatID = struct { value: []const u8 };

/// The `TerminalID` scalar type represents an identifier for an object of type Terminal.
pub const TerminalID = struct { value: []const u8 };

/// The `TypeDefID` scalar type represents an identifier for an object of type TypeDef.
pub const TypeDefID = struct { value: []const u8 };

/// The `UpGroupID` scalar type represents an identifier for an object of type UpGroup.
pub const UpGroupID = struct { value: []const u8 };

/// The `UpID` scalar type represents an identifier for an object of type Up.
pub const UpID = struct { value: []const u8 };

/// The absence of a value.
///
/// A Null Void is used as a placeholder for resolvers that do not return anything.
pub const Void = struct { value: []const u8 };

/// The `WorkspaceID` scalar type represents an identifier for an object of type Workspace.
pub const WorkspaceID = struct { value: []const u8 };

/// Key value object that represents a build argument.
pub const BuildArg = struct {
    /// The build argument name.
    name: []const u8,
    /// The build argument value.
    value: []const u8,
};

/// Key value object that represents a pipeline label.
pub const PipelineLabel = struct {
    /// Label name.
    name: []const u8,
    /// Label value.
    value: []const u8,
};

/// Port forwarding rules for tunneling network traffic.
pub const PortForward = struct {
    /// Destination port for traffic.
    backend: i32,
    /// Port to expose to clients. If unspecified, a default will be chosen.
    frontend: ?i32,
    /// Transport layer protocol to use for traffic.
    protocol: ?NetworkProtocol,
};

/// Sharing mode of the cache volume.
pub const CacheSharingMode = enum {
    /// Shares the cache volume amongst many build pipelines
    shared,
    /// Keeps a cache volume for a single build pipeline
    private,
    /// Shares the cache volume amongst many build pipelines, but will serialize the writes
    locked,

    pub fn fromString(s: []const u8) !@This() {
        if (std.mem.eql(u8, s, "SHARED")) return .shared;
        if (std.mem.eql(u8, s, "PRIVATE")) return .private;
        if (std.mem.eql(u8, s, "LOCKED")) return .locked;
        return error.UnknownEnumValue;
    }
};

/// Strategy to use when merging changesets with conflicting changes.
pub const ChangesetMergeConflict = enum {
    /// Fail before attempting merge if file-level conflicts are detected
    failEarly,
    /// Attempt the merge and fail if git merge fails due to conflicts
    fail,
    /// Let git create conflict markers in files. For modify/delete conflicts, keeps the modified version. Fails on binary conflicts.
    leaveConflictMarkers,
    /// The conflict is resolved by applying the version of the calling changeset
    preferOurs,
    /// The conflict is resolved by applying the version of the other changeset
    preferTheirs,

    pub fn fromString(s: []const u8) !@This() {
        if (std.mem.eql(u8, s, "FAIL_EARLY")) return .failEarly;
        if (std.mem.eql(u8, s, "FAIL")) return .fail;
        if (std.mem.eql(u8, s, "LEAVE_CONFLICT_MARKERS")) return .leaveConflictMarkers;
        if (std.mem.eql(u8, s, "PREFER_OURS")) return .preferOurs;
        if (std.mem.eql(u8, s, "PREFER_THEIRS")) return .preferTheirs;
        return error.UnknownEnumValue;
    }
};

/// Strategy to use when merging multiple changesets with git octopus merge.
pub const ChangesetsMergeConflict = enum {
    /// Fail before attempting merge if file-level conflicts are detected between any changesets
    failEarly,
    /// Attempt the octopus merge and fail if git merge fails due to conflicts
    fail,

    pub fn fromString(s: []const u8) !@This() {
        if (std.mem.eql(u8, s, "FAIL_EARLY")) return .failEarly;
        if (std.mem.eql(u8, s, "FAIL")) return .fail;
        return error.UnknownEnumValue;
    }
};

/// The type of change for a diff stat entry.
pub const DiffStatKind = enum {
    /// A file or directory was added.
    added,
    /// A file was modified.
    modified,
    /// A file or directory was removed.
    removed,
    /// A file was renamed.
    renamed,

    pub fn fromString(s: []const u8) !@This() {
        if (std.mem.eql(u8, s, "ADDED")) return .added;
        if (std.mem.eql(u8, s, "MODIFIED")) return .modified;
        if (std.mem.eql(u8, s, "REMOVED")) return .removed;
        if (std.mem.eql(u8, s, "RENAMED")) return .renamed;
        return error.UnknownEnumValue;
    }
};

/// File type.
pub const ExistsType = enum {
    /// Tests path is a regular file
    regularType,
    /// Tests path is a directory
    directoryType,
    /// Tests path is a symlink
    symlinkType,

    pub fn fromString(s: []const u8) !@This() {
        if (std.mem.eql(u8, s, "REGULAR_TYPE")) return .regularType;
        if (std.mem.eql(u8, s, "DIRECTORY_TYPE")) return .directoryType;
        if (std.mem.eql(u8, s, "SYMLINK_TYPE")) return .symlinkType;
        return error.UnknownEnumValue;
    }
};

/// File type.
pub const FileType = enum {
    /// unknown file type
    unknown,
    /// regular file type
    regular,
    /// directory file type
    directory,
    /// symlink file type
    symlink,
    /// regular file type
    regularType,
    /// directory file type
    directoryType,
    /// symlink file type
    symlinkType,

    pub fn fromString(s: []const u8) !@This() {
        if (std.mem.eql(u8, s, "UNKNOWN")) return .unknown;
        if (std.mem.eql(u8, s, "REGULAR")) return .regular;
        if (std.mem.eql(u8, s, "DIRECTORY")) return .directory;
        if (std.mem.eql(u8, s, "SYMLINK")) return .symlink;
        if (std.mem.eql(u8, s, "REGULAR_TYPE")) return .regularType;
        if (std.mem.eql(u8, s, "DIRECTORY_TYPE")) return .directoryType;
        if (std.mem.eql(u8, s, "SYMLINK_TYPE")) return .symlinkType;
        return error.UnknownEnumValue;
    }
};

/// The behavior configured for function result caching.
pub const FunctionCachePolicy = enum {
    ///
    default,
    ///
    persession,
    ///
    never,

    pub fn fromString(s: []const u8) !@This() {
        if (std.mem.eql(u8, s, "Default")) return .default;
        if (std.mem.eql(u8, s, "PerSession")) return .persession;
        if (std.mem.eql(u8, s, "Never")) return .never;
        return error.UnknownEnumValue;
    }
};

/// Compression algorithm to use for image layers.
pub const ImageLayerCompression = enum {
    ///
    gzip,
    ///
    zstd,
    ///
    estargz,
    ///
    uncompressed,
    ///
    gzip,
    ///
    zstd,
    ///
    estargz,
    ///
    uncompressed,

    pub fn fromString(s: []const u8) !@This() {
        if (std.mem.eql(u8, s, "Gzip")) return .gzip;
        if (std.mem.eql(u8, s, "Zstd")) return .zstd;
        if (std.mem.eql(u8, s, "EStarGZ")) return .estargz;
        if (std.mem.eql(u8, s, "Uncompressed")) return .uncompressed;
        if (std.mem.eql(u8, s, "GZIP")) return .gzip;
        if (std.mem.eql(u8, s, "ZSTD")) return .zstd;
        if (std.mem.eql(u8, s, "ESTARGZ")) return .estargz;
        if (std.mem.eql(u8, s, "UNCOMPRESSED")) return .uncompressed;
        return error.UnknownEnumValue;
    }
};

/// Mediatypes to use in published or exported image metadata.
pub const ImageMediaTypes = enum {
    ///
    ocimediatypes,
    ///
    dockermediatypes,
    ///
    oci,
    ///
    docker,

    pub fn fromString(s: []const u8) !@This() {
        if (std.mem.eql(u8, s, "OCIMediaTypes")) return .ocimediatypes;
        if (std.mem.eql(u8, s, "DockerMediaTypes")) return .dockermediatypes;
        if (std.mem.eql(u8, s, "OCI")) return .oci;
        if (std.mem.eql(u8, s, "DOCKER")) return .docker;
        return error.UnknownEnumValue;
    }
};

/// Experimental features of a module
pub const ModuleSourceExperimentalFeature = enum {
    /// Self calls
    selfCalls,

    pub fn fromString(s: []const u8) !@This() {
        if (std.mem.eql(u8, s, "SELF_CALLS")) return .selfCalls;
        return error.UnknownEnumValue;
    }
};

/// The kind of module source.
pub const ModuleSourceKind = enum {
    ///
    localSource,
    ///
    gitSource,
    ///
    dirSource,
    ///
    local,
    ///
    git,
    ///
    dir,

    pub fn fromString(s: []const u8) !@This() {
        if (std.mem.eql(u8, s, "LOCAL_SOURCE")) return .localSource;
        if (std.mem.eql(u8, s, "GIT_SOURCE")) return .gitSource;
        if (std.mem.eql(u8, s, "DIR_SOURCE")) return .dirSource;
        if (std.mem.eql(u8, s, "LOCAL")) return .local;
        if (std.mem.eql(u8, s, "GIT")) return .git;
        if (std.mem.eql(u8, s, "DIR")) return .dir;
        return error.UnknownEnumValue;
    }
};

/// Transport layer network protocol associated to a port.
pub const NetworkProtocol = enum {
    ///
    tcp,
    ///
    udp,

    pub fn fromString(s: []const u8) !@This() {
        if (std.mem.eql(u8, s, "TCP")) return .tcp;
        if (std.mem.eql(u8, s, "UDP")) return .udp;
        return error.UnknownEnumValue;
    }
};

/// Expected return type of an execution
pub const ReturnType = enum {
    /// A successful execution (exit code 0)
    success,
    /// A failed execution (exit codes 1-127 and 192-255)
    failure,
    /// Any execution (exit codes 0-127 and 192-255)
    any,

    pub fn fromString(s: []const u8) !@This() {
        if (std.mem.eql(u8, s, "SUCCESS")) return .success;
        if (std.mem.eql(u8, s, "FAILURE")) return .failure;
        if (std.mem.eql(u8, s, "ANY")) return .any;
        return error.UnknownEnumValue;
    }
};

/// Distinguishes the different kinds of TypeDefs.
pub const TypeDefKind = enum {
    /// A string value.
    stringKind,
    /// An integer value.
    integerKind,
    /// A float value.
    floatKind,
    /// A boolean value.
    booleanKind,
    /// A scalar value of any basic kind.
    scalarKind,
    /// Always paired with a ListTypeDef.
    ///
    /// A list of values all having the same type.
    listKind,
    /// Always paired with an ObjectTypeDef.
    ///
    /// A named type defined in the GraphQL schema, with fields and functions.
    objectKind,
    /// Always paired with an InterfaceTypeDef.
    ///
    /// A named type of functions that can be matched+implemented by other objects+interfaces.
    interfaceKind,
    /// A graphql input type, used only when representing the core API via TypeDefs.
    inputKind,
    /// A special kind used to signify that no value is returned.
    ///
    /// This is used for functions that have no return value. The outer TypeDef specifying this Kind is always Optional, as the Void is never actually represented.
    voidKind,
    /// A GraphQL enum type and its values
    ///
    /// Always paired with an EnumTypeDef.
    enumKind,
    /// A string value.
    string,
    /// An integer value.
    integer,
    /// A float value.
    float,
    /// A boolean value.
    boolean,
    /// A scalar value of any basic kind.
    scalar,
    /// Always paired with a ListTypeDef.
    ///
    /// A list of values all having the same type.
    list,
    /// Always paired with an ObjectTypeDef.
    ///
    /// A named type defined in the GraphQL schema, with fields and functions.
    object,
    /// Always paired with an InterfaceTypeDef.
    ///
    /// A named type of functions that can be matched+implemented by other objects+interfaces.
    interface,
    /// A graphql input type, used only when representing the core API via TypeDefs.
    input,
    /// A special kind used to signify that no value is returned.
    ///
    /// This is used for functions that have no return value. The outer TypeDef specifying this Kind is always Optional, as the Void is never actually represented.
    void,
    /// A GraphQL enum type and its values
    ///
    /// Always paired with an EnumTypeDef.
    @"enum",

    pub fn fromString(s: []const u8) !@This() {
        if (std.mem.eql(u8, s, "STRING_KIND")) return .stringKind;
        if (std.mem.eql(u8, s, "INTEGER_KIND")) return .integerKind;
        if (std.mem.eql(u8, s, "FLOAT_KIND")) return .floatKind;
        if (std.mem.eql(u8, s, "BOOLEAN_KIND")) return .booleanKind;
        if (std.mem.eql(u8, s, "SCALAR_KIND")) return .scalarKind;
        if (std.mem.eql(u8, s, "LIST_KIND")) return .listKind;
        if (std.mem.eql(u8, s, "OBJECT_KIND")) return .objectKind;
        if (std.mem.eql(u8, s, "INTERFACE_KIND")) return .interfaceKind;
        if (std.mem.eql(u8, s, "INPUT_KIND")) return .inputKind;
        if (std.mem.eql(u8, s, "VOID_KIND")) return .voidKind;
        if (std.mem.eql(u8, s, "ENUM_KIND")) return .enumKind;
        if (std.mem.eql(u8, s, "STRING")) return .string;
        if (std.mem.eql(u8, s, "INTEGER")) return .integer;
        if (std.mem.eql(u8, s, "FLOAT")) return .float;
        if (std.mem.eql(u8, s, "BOOLEAN")) return .boolean;
        if (std.mem.eql(u8, s, "SCALAR")) return .scalar;
        if (std.mem.eql(u8, s, "LIST")) return .list;
        if (std.mem.eql(u8, s, "OBJECT")) return .object;
        if (std.mem.eql(u8, s, "INTERFACE")) return .interface;
        if (std.mem.eql(u8, s, "INPUT")) return .input;
        if (std.mem.eql(u8, s, "VOID")) return .void;
        if (std.mem.eql(u8, s, "ENUM")) return .@"enum";
        return error.UnknownEnumValue;
    }
};

pub const Client = struct {
    query_builder: *QueryBuilder,
    allocator: std.mem.Allocator,

    pub fn loadAddressFromID(self: *Client, id: AddressID) !Address {
        const qb = try self.query_builder.selection("loadAddressFromID", .{ .id = id });
        return Address{ .query_builder = qb, .client = self };
    }

    pub fn loadBindingFromID(self: *Client, id: BindingID) !Binding {
        const qb = try self.query_builder.selection("loadBindingFromID", .{ .id = id });
        return Binding{ .query_builder = qb, .client = self };
    }

    pub fn loadCacheVolumeFromID(self: *Client, id: CacheVolumeID) !CacheVolume {
        const qb = try self.query_builder.selection("loadCacheVolumeFromID", .{ .id = id });
        return CacheVolume{ .query_builder = qb, .client = self };
    }

    pub fn loadChangesetFromID(self: *Client, id: ChangesetID) !Changeset {
        const qb = try self.query_builder.selection("loadChangesetFromID", .{ .id = id });
        return Changeset{ .query_builder = qb, .client = self };
    }

    pub fn loadCheckFromID(self: *Client, id: CheckID) !Check {
        const qb = try self.query_builder.selection("loadCheckFromID", .{ .id = id });
        return Check{ .query_builder = qb, .client = self };
    }

    pub fn loadCheckGroupFromID(self: *Client, id: CheckGroupID) !CheckGroup {
        const qb = try self.query_builder.selection("loadCheckGroupFromID", .{ .id = id });
        return CheckGroup{ .query_builder = qb, .client = self };
    }

    pub fn loadCloudFromID(self: *Client, id: CloudID) !Cloud {
        const qb = try self.query_builder.selection("loadCloudFromID", .{ .id = id });
        return Cloud{ .query_builder = qb, .client = self };
    }

    pub fn loadContainerFromID(self: *Client, id: ContainerID) !Container {
        const qb = try self.query_builder.selection("loadContainerFromID", .{ .id = id });
        return Container{ .query_builder = qb, .client = self };
    }

    pub fn loadCurrentModuleFromID(self: *Client, id: CurrentModuleID) !CurrentModule {
        const qb = try self.query_builder.selection("loadCurrentModuleFromID", .{ .id = id });
        return CurrentModule{ .query_builder = qb, .client = self };
    }

    pub fn loadDiffStatFromID(self: *Client, id: DiffStatID) !DiffStat {
        const qb = try self.query_builder.selection("loadDiffStatFromID", .{ .id = id });
        return DiffStat{ .query_builder = qb, .client = self };
    }

    pub fn loadDirectoryFromID(self: *Client, id: DirectoryID) !Directory {
        const qb = try self.query_builder.selection("loadDirectoryFromID", .{ .id = id });
        return Directory{ .query_builder = qb, .client = self };
    }

    pub fn loadEngineFromID(self: *Client, id: EngineID) !Engine {
        const qb = try self.query_builder.selection("loadEngineFromID", .{ .id = id });
        return Engine{ .query_builder = qb, .client = self };
    }

    pub fn loadEngineCacheFromID(self: *Client, id: EngineCacheID) !EngineCache {
        const qb = try self.query_builder.selection("loadEngineCacheFromID", .{ .id = id });
        return EngineCache{ .query_builder = qb, .client = self };
    }

    pub fn loadEngineCacheEntryFromID(self: *Client, id: EngineCacheEntryID) !EngineCacheEntry {
        const qb = try self.query_builder.selection("loadEngineCacheEntryFromID", .{ .id = id });
        return EngineCacheEntry{ .query_builder = qb, .client = self };
    }

    pub fn loadEngineCacheEntrySetFromID(self: *Client, id: EngineCacheEntrySetID) !EngineCacheEntrySet {
        const qb = try self.query_builder.selection("loadEngineCacheEntrySetFromID", .{ .id = id });
        return EngineCacheEntrySet{ .query_builder = qb, .client = self };
    }

    pub fn loadEnumTypeDefFromID(self: *Client, id: EnumTypeDefID) !EnumTypeDef {
        const qb = try self.query_builder.selection("loadEnumTypeDefFromID", .{ .id = id });
        return EnumTypeDef{ .query_builder = qb, .client = self };
    }

    pub fn loadEnumValueTypeDefFromID(self: *Client, id: EnumValueTypeDefID) !EnumValueTypeDef {
        const qb = try self.query_builder.selection("loadEnumValueTypeDefFromID", .{ .id = id });
        return EnumValueTypeDef{ .query_builder = qb, .client = self };
    }

    pub fn loadEnvFromID(self: *Client, id: EnvID) !Env {
        const qb = try self.query_builder.selection("loadEnvFromID", .{ .id = id });
        return Env{ .query_builder = qb, .client = self };
    }

    pub fn loadEnvFileFromID(self: *Client, id: EnvFileID) !EnvFile {
        const qb = try self.query_builder.selection("loadEnvFileFromID", .{ .id = id });
        return EnvFile{ .query_builder = qb, .client = self };
    }

    pub fn loadEnvVariableFromID(self: *Client, id: EnvVariableID) !EnvVariable {
        const qb = try self.query_builder.selection("loadEnvVariableFromID", .{ .id = id });
        return EnvVariable{ .query_builder = qb, .client = self };
    }

    pub fn loadErrorFromID(self: *Client, id: ErrorID) !Error {
        const qb = try self.query_builder.selection("loadErrorFromID", .{ .id = id });
        return Error{ .query_builder = qb, .client = self };
    }

    pub fn loadErrorValueFromID(self: *Client, id: ErrorValueID) !ErrorValue {
        const qb = try self.query_builder.selection("loadErrorValueFromID", .{ .id = id });
        return ErrorValue{ .query_builder = qb, .client = self };
    }

    pub fn loadFieldTypeDefFromID(self: *Client, id: FieldTypeDefID) !FieldTypeDef {
        const qb = try self.query_builder.selection("loadFieldTypeDefFromID", .{ .id = id });
        return FieldTypeDef{ .query_builder = qb, .client = self };
    }

    pub fn loadFileFromID(self: *Client, id: FileID) !File {
        const qb = try self.query_builder.selection("loadFileFromID", .{ .id = id });
        return File{ .query_builder = qb, .client = self };
    }

    pub fn loadFunctionFromID(self: *Client, id: FunctionID) !Function {
        const qb = try self.query_builder.selection("loadFunctionFromID", .{ .id = id });
        return Function{ .query_builder = qb, .client = self };
    }

    pub fn loadFunctionArgFromID(self: *Client, id: FunctionArgID) !FunctionArg {
        const qb = try self.query_builder.selection("loadFunctionArgFromID", .{ .id = id });
        return FunctionArg{ .query_builder = qb, .client = self };
    }

    pub fn loadFunctionCallFromID(self: *Client, id: FunctionCallID) !FunctionCall {
        const qb = try self.query_builder.selection("loadFunctionCallFromID", .{ .id = id });
        return FunctionCall{ .query_builder = qb, .client = self };
    }

    pub fn loadFunctionCallArgValueFromID(self: *Client, id: FunctionCallArgValueID) !FunctionCallArgValue {
        const qb = try self.query_builder.selection("loadFunctionCallArgValueFromID", .{ .id = id });
        return FunctionCallArgValue{ .query_builder = qb, .client = self };
    }

    pub fn loadGeneratedCodeFromID(self: *Client, id: GeneratedCodeID) !GeneratedCode {
        const qb = try self.query_builder.selection("loadGeneratedCodeFromID", .{ .id = id });
        return GeneratedCode{ .query_builder = qb, .client = self };
    }

    pub fn loadGeneratorFromID(self: *Client, id: GeneratorID) !Generator {
        const qb = try self.query_builder.selection("loadGeneratorFromID", .{ .id = id });
        return Generator{ .query_builder = qb, .client = self };
    }

    pub fn loadGeneratorGroupFromID(self: *Client, id: GeneratorGroupID) !GeneratorGroup {
        const qb = try self.query_builder.selection("loadGeneratorGroupFromID", .{ .id = id });
        return GeneratorGroup{ .query_builder = qb, .client = self };
    }

    pub fn loadGitRefFromID(self: *Client, id: GitRefID) !GitRef {
        const qb = try self.query_builder.selection("loadGitRefFromID", .{ .id = id });
        return GitRef{ .query_builder = qb, .client = self };
    }

    pub fn loadGitRepositoryFromID(self: *Client, id: GitRepositoryID) !GitRepository {
        const qb = try self.query_builder.selection("loadGitRepositoryFromID", .{ .id = id });
        return GitRepository{ .query_builder = qb, .client = self };
    }

    pub fn loadHealthcheckConfigFromID(self: *Client, id: HealthcheckConfigID) !HealthcheckConfig {
        const qb = try self.query_builder.selection("loadHealthcheckConfigFromID", .{ .id = id });
        return HealthcheckConfig{ .query_builder = qb, .client = self };
    }

    pub fn loadHostFromID(self: *Client, id: HostID) !Host {
        const qb = try self.query_builder.selection("loadHostFromID", .{ .id = id });
        return Host{ .query_builder = qb, .client = self };
    }

    pub fn loadInputTypeDefFromID(self: *Client, id: InputTypeDefID) !InputTypeDef {
        const qb = try self.query_builder.selection("loadInputTypeDefFromID", .{ .id = id });
        return InputTypeDef{ .query_builder = qb, .client = self };
    }

    pub fn loadInterfaceTypeDefFromID(self: *Client, id: InterfaceTypeDefID) !InterfaceTypeDef {
        const qb = try self.query_builder.selection("loadInterfaceTypeDefFromID", .{ .id = id });
        return InterfaceTypeDef{ .query_builder = qb, .client = self };
    }

    pub fn loadJSONValueFromID(self: *Client, id: JSONValueID) !JSONValue {
        const qb = try self.query_builder.selection("loadJSONValueFromID", .{ .id = id });
        return JSONValue{ .query_builder = qb, .client = self };
    }

    pub fn loadLLMFromID(self: *Client, id: LLMID) !LLM {
        const qb = try self.query_builder.selection("loadLLMFromID", .{ .id = id });
        return LLM{ .query_builder = qb, .client = self };
    }

    pub fn loadLLMTokenUsageFromID(self: *Client, id: LLMTokenUsageID) !LLMTokenUsage {
        const qb = try self.query_builder.selection("loadLLMTokenUsageFromID", .{ .id = id });
        return LLMTokenUsage{ .query_builder = qb, .client = self };
    }

    pub fn loadLabelFromID(self: *Client, id: LabelID) !Label {
        const qb = try self.query_builder.selection("loadLabelFromID", .{ .id = id });
        return Label{ .query_builder = qb, .client = self };
    }

    pub fn loadListTypeDefFromID(self: *Client, id: ListTypeDefID) !ListTypeDef {
        const qb = try self.query_builder.selection("loadListTypeDefFromID", .{ .id = id });
        return ListTypeDef{ .query_builder = qb, .client = self };
    }

    pub fn loadModuleFromID(self: *Client, id: ModuleID) !Module {
        const qb = try self.query_builder.selection("loadModuleFromID", .{ .id = id });
        return Module{ .query_builder = qb, .client = self };
    }

    pub fn loadModuleConfigClientFromID(self: *Client, id: ModuleConfigClientID) !ModuleConfigClient {
        const qb = try self.query_builder.selection("loadModuleConfigClientFromID", .{ .id = id });
        return ModuleConfigClient{ .query_builder = qb, .client = self };
    }

    pub fn loadModuleSourceFromID(self: *Client, id: ModuleSourceID) !ModuleSource {
        const qb = try self.query_builder.selection("loadModuleSourceFromID", .{ .id = id });
        return ModuleSource{ .query_builder = qb, .client = self };
    }

    pub fn loadObjectTypeDefFromID(self: *Client, id: ObjectTypeDefID) !ObjectTypeDef {
        const qb = try self.query_builder.selection("loadObjectTypeDefFromID", .{ .id = id });
        return ObjectTypeDef{ .query_builder = qb, .client = self };
    }

    pub fn loadPortFromID(self: *Client, id: PortID) !Port {
        const qb = try self.query_builder.selection("loadPortFromID", .{ .id = id });
        return Port{ .query_builder = qb, .client = self };
    }

    pub fn loadSDKConfigFromID(self: *Client, id: SDKConfigID) !SDKConfig {
        const qb = try self.query_builder.selection("loadSDKConfigFromID", .{ .id = id });
        return SDKConfig{ .query_builder = qb, .client = self };
    }

    pub fn loadScalarTypeDefFromID(self: *Client, id: ScalarTypeDefID) !ScalarTypeDef {
        const qb = try self.query_builder.selection("loadScalarTypeDefFromID", .{ .id = id });
        return ScalarTypeDef{ .query_builder = qb, .client = self };
    }

    pub fn loadSearchResultFromID(self: *Client, id: SearchResultID) !SearchResult {
        const qb = try self.query_builder.selection("loadSearchResultFromID", .{ .id = id });
        return SearchResult{ .query_builder = qb, .client = self };
    }

    pub fn loadSearchSubmatchFromID(self: *Client, id: SearchSubmatchID) !SearchSubmatch {
        const qb = try self.query_builder.selection("loadSearchSubmatchFromID", .{ .id = id });
        return SearchSubmatch{ .query_builder = qb, .client = self };
    }

    pub fn loadSecretFromID(self: *Client, id: SecretID) !Secret {
        const qb = try self.query_builder.selection("loadSecretFromID", .{ .id = id });
        return Secret{ .query_builder = qb, .client = self };
    }

    pub fn loadServiceFromID(self: *Client, id: ServiceID) !Service {
        const qb = try self.query_builder.selection("loadServiceFromID", .{ .id = id });
        return Service{ .query_builder = qb, .client = self };
    }

    pub fn loadSocketFromID(self: *Client, id: SocketID) !Socket {
        const qb = try self.query_builder.selection("loadSocketFromID", .{ .id = id });
        return Socket{ .query_builder = qb, .client = self };
    }

    pub fn loadSourceMapFromID(self: *Client, id: SourceMapID) !SourceMap {
        const qb = try self.query_builder.selection("loadSourceMapFromID", .{ .id = id });
        return SourceMap{ .query_builder = qb, .client = self };
    }

    pub fn loadStatFromID(self: *Client, id: StatID) !Stat {
        const qb = try self.query_builder.selection("loadStatFromID", .{ .id = id });
        return Stat{ .query_builder = qb, .client = self };
    }

    pub fn loadTerminalFromID(self: *Client, id: TerminalID) !Terminal {
        const qb = try self.query_builder.selection("loadTerminalFromID", .{ .id = id });
        return Terminal{ .query_builder = qb, .client = self };
    }

    pub fn loadTypeDefFromID(self: *Client, id: TypeDefID) !TypeDef {
        const qb = try self.query_builder.selection("loadTypeDefFromID", .{ .id = id });
        return TypeDef{ .query_builder = qb, .client = self };
    }

    pub fn loadUpFromID(self: *Client, id: UpID) !Up {
        const qb = try self.query_builder.selection("loadUpFromID", .{ .id = id });
        return Up{ .query_builder = qb, .client = self };
    }

    pub fn loadUpGroupFromID(self: *Client, id: UpGroupID) !UpGroup {
        const qb = try self.query_builder.selection("loadUpGroupFromID", .{ .id = id });
        return UpGroup{ .query_builder = qb, .client = self };
    }

    pub fn loadWorkspaceFromID(self: *Client, id: WorkspaceID) !Workspace {
        const qb = try self.query_builder.selection("loadWorkspaceFromID", .{ .id = id });
        return Workspace{ .query_builder = qb, .client = self };
    }

    /// initialize an address to load directories, containers, secrets or other object types.
    pub fn address(self: *@This(), value: []const u8) !Address {
        const qb = try self.query_builder.selection("address", .{
            .value = value,
        });
        return Address{ .query_builder = qb, .client = self.client };
    }

    /// Constructs a cache volume for a given cache key.
    pub fn cachevolume(self: *@This(), key: []const u8) !CacheVolume {
        const qb = try self.query_builder.selection("cacheVolume", .{
            .key = key,
        });
        return CacheVolume{ .query_builder = qb, .client = self.client };
    }

    /// Creates an empty changeset
    pub fn changeset(self: *@This()) !Changeset {
        const qb = try self.query_builder.selection("changeset", .{});
        return Changeset{ .query_builder = qb, .client = self.client };
    }

    /// Dagger Cloud configuration and state
    pub fn cloud(self: *@This()) !Cloud {
        const qb = try self.query_builder.selection("cloud", .{});
        return Cloud{ .query_builder = qb, .client = self.client };
    }

    /// Creates a scratch container, with no image or metadata.
    ///
    /// To pull an image, follow up with the "from" function.
    pub fn container(self: *@This(), options: struct {
        platform: ?Platform = null,
    }) !Container {
        const qb = try self.query_builder.selection("container", .{
            .platform = options.platform,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Returns the current environment
    ///
    /// When called from a function invoked via an LLM tool call, this will be the LLM's current environment, including any modifications made through calling tools. Env values returned by functions become the new environment for subsequent calls, and Changeset values returned by functions are applied to the environment's workspace.
    ///
    /// When called from a module function outside of an LLM, this returns an Env with the current module installed, and with the current module's source directory as its workspace.
    pub fn currentenv(self: *@This()) !Env {
        const qb = try self.query_builder.selection("currentEnv", .{});
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// The FunctionCall context that the SDK caller is currently executing in.
    ///
    /// If the caller is not currently executing in a function, this will return an error.
    pub fn currentfunctioncall(self: *@This()) !FunctionCall {
        const qb = try self.query_builder.selection("currentFunctionCall", .{});
        return FunctionCall{ .query_builder = qb, .client = self.client };
    }

    /// The module currently being served in the session, if any.
    pub fn currentmodule(self: *@This()) !CurrentModule {
        const qb = try self.query_builder.selection("currentModule", .{});
        return CurrentModule{ .query_builder = qb, .client = self.client };
    }

    /// The TypeDef representations of the objects currently being served in the session.
    pub fn currenttypedefs(self: *@This(), options: struct {
        hidecore: ?bool = null,
    }) ![]const TypeDef {
        const qb = try self.query_builder.selection("currentTypeDefs", .{
            .hidecore = options.hidecore,
        });
        const ids = try self.client.execute(qb, []const TypeDefID);
        var results = try self.client.allocator.alloc(TypeDef, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadTypeDefFromID(id);
        }
        return results;
    }

    /// Detect and return the current workspace.
    pub fn currentworkspace(self: *@This()) !Workspace {
        const qb = try self.query_builder.selection("currentWorkspace", .{});
        return Workspace{ .query_builder = qb, .client = self.client };
    }

    /// The default platform of the engine.
    pub fn defaultplatform(self: *@This()) !Platform {
        const qb = try self.query_builder.selection("defaultPlatform", .{});
        return try self.client.execute(qb, Platform);
    }

    /// Creates an empty directory.
    pub fn directory(self: *@This()) !Directory {
        const qb = try self.query_builder.selection("directory", .{});
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// The Dagger engine container configuration and state
    pub fn engine(self: *@This()) !Engine {
        const qb = try self.query_builder.selection("engine", .{});
        return Engine{ .query_builder = qb, .client = self.client };
    }

    /// Initializes a new environment
    pub fn env(self: *@This(), options: struct {
        privileged: ?bool = null,
        writable: ?bool = null,
    }) !Env {
        const qb = try self.query_builder.selection("env", .{
            .privileged = options.privileged,
            .writable = options.writable,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Initialize an environment file
    pub fn envfile(self: *@This(), options: struct {
        expand: ?bool = null,
    }) !EnvFile {
        const qb = try self.query_builder.selection("envFile", .{
            .expand = options.expand,
        });
        return EnvFile{ .query_builder = qb, .client = self.client };
    }

    /// Create a new error.
    pub fn @"error"(self: *@This(), message: []const u8) !Error {
        const qb = try self.query_builder.selection("error", .{
            .message = message,
        });
        return Error{ .query_builder = qb, .client = self.client };
    }

    /// Creates a file with the specified contents.
    pub fn file(self: *@This(), name: []const u8, contents: []const u8, options: struct {
        permissions: ?i32 = null,
    }) !File {
        const qb = try self.query_builder.selection("file", .{
            .name = name,
            .contents = contents,
            .permissions = options.permissions,
        });
        return File{ .query_builder = qb, .client = self.client };
    }

    /// Creates a function.
    pub fn function(self: *@This(), name: []const u8, returntype: TypeDefID) !Function {
        const qb = try self.query_builder.selection("function", .{
            .name = name,
            .returntype = returntype,
        });
        return Function{ .query_builder = qb, .client = self.client };
    }

    /// Create a code generation result, given a directory containing the generated code.
    pub fn generatedcode(self: *@This(), code: DirectoryID) !GeneratedCode {
        const qb = try self.query_builder.selection("generatedCode", .{
            .code = code,
        });
        return GeneratedCode{ .query_builder = qb, .client = self.client };
    }

    /// Queries a Git repository.
    pub fn git(self: *@This(), url: []const u8, options: struct {
        keepgitdir: ?bool = null,
        sshknownhosts: ?[]const u8 = null,
        sshauthsocket: ?SocketID = null,
        httpauthusername: ?[]const u8 = null,
        httpauthtoken: ?SecretID = null,
        httpauthheader: ?SecretID = null,
        experimentalservicehost: ?ServiceID = null,
    }) !GitRepository {
        const qb = try self.query_builder.selection("git", .{
            .url = url,
            .keepgitdir = options.keepgitdir,
            .sshknownhosts = options.sshknownhosts,
            .sshauthsocket = options.sshauthsocket,
            .httpauthusername = options.httpauthusername,
            .httpauthtoken = options.httpauthtoken,
            .httpauthheader = options.httpauthheader,
            .experimentalservicehost = options.experimentalservicehost,
        });
        return GitRepository{ .query_builder = qb, .client = self.client };
    }

    /// Queries the host environment.
    pub fn host(self: *@This()) !Host {
        const qb = try self.query_builder.selection("host", .{});
        return Host{ .query_builder = qb, .client = self.client };
    }

    /// Returns a file containing an http remote url content.
    pub fn http(self: *@This(), url: []const u8, options: struct {
        name: ?[]const u8 = null,
        permissions: ?i32 = null,
        authheader: ?SecretID = null,
        experimentalservicehost: ?ServiceID = null,
    }) !File {
        const qb = try self.query_builder.selection("http", .{
            .url = url,
            .name = options.name,
            .permissions = options.permissions,
            .authheader = options.authheader,
            .experimentalservicehost = options.experimentalservicehost,
        });
        return File{ .query_builder = qb, .client = self.client };
    }

    /// A unique identifier for this Query.
    pub fn id(self: *@This()) !QueryID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, QueryID);
    }

    /// Initialize a JSON value
    pub fn json(self: *@This()) !JSONValue {
        const qb = try self.query_builder.selection("json", .{});
        return JSONValue{ .query_builder = qb, .client = self.client };
    }

    /// Initialize a Large Language Model (LLM)
    pub fn llm(self: *@This(), options: struct {
        model: ?[]const u8 = null,
        maxapicalls: ?i32 = null,
    }) !LLM {
        const qb = try self.query_builder.selection("llm", .{
            .model = options.model,
            .maxapicalls = options.maxapicalls,
        });
        return LLM{ .query_builder = qb, .client = self.client };
    }

    /// Load a Address from its ID.
    pub fn loadaddressfromid(self: *@This(), id: AddressID) !Address {
        const qb = try self.query_builder.selection("loadAddressFromID", .{
            .id = id,
        });
        return Address{ .query_builder = qb, .client = self.client };
    }

    /// Load a Binding from its ID.
    pub fn loadbindingfromid(self: *@This(), id: BindingID) !Binding {
        const qb = try self.query_builder.selection("loadBindingFromID", .{
            .id = id,
        });
        return Binding{ .query_builder = qb, .client = self.client };
    }

    /// Load a CacheVolume from its ID.
    pub fn loadcachevolumefromid(self: *@This(), id: CacheVolumeID) !CacheVolume {
        const qb = try self.query_builder.selection("loadCacheVolumeFromID", .{
            .id = id,
        });
        return CacheVolume{ .query_builder = qb, .client = self.client };
    }

    /// Load a Changeset from its ID.
    pub fn loadchangesetfromid(self: *@This(), id: ChangesetID) !Changeset {
        const qb = try self.query_builder.selection("loadChangesetFromID", .{
            .id = id,
        });
        return Changeset{ .query_builder = qb, .client = self.client };
    }

    /// Load a Check from its ID.
    pub fn loadcheckfromid(self: *@This(), id: CheckID) !Check {
        const qb = try self.query_builder.selection("loadCheckFromID", .{
            .id = id,
        });
        return Check{ .query_builder = qb, .client = self.client };
    }

    /// Load a CheckGroup from its ID.
    pub fn loadcheckgroupfromid(self: *@This(), id: CheckGroupID) !CheckGroup {
        const qb = try self.query_builder.selection("loadCheckGroupFromID", .{
            .id = id,
        });
        return CheckGroup{ .query_builder = qb, .client = self.client };
    }

    /// Load a Cloud from its ID.
    pub fn loadcloudfromid(self: *@This(), id: CloudID) !Cloud {
        const qb = try self.query_builder.selection("loadCloudFromID", .{
            .id = id,
        });
        return Cloud{ .query_builder = qb, .client = self.client };
    }

    /// Load a Container from its ID.
    pub fn loadcontainerfromid(self: *@This(), id: ContainerID) !Container {
        const qb = try self.query_builder.selection("loadContainerFromID", .{
            .id = id,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Load a CurrentModule from its ID.
    pub fn loadcurrentmodulefromid(self: *@This(), id: CurrentModuleID) !CurrentModule {
        const qb = try self.query_builder.selection("loadCurrentModuleFromID", .{
            .id = id,
        });
        return CurrentModule{ .query_builder = qb, .client = self.client };
    }

    /// Load a DiffStat from its ID.
    pub fn loaddiffstatfromid(self: *@This(), id: DiffStatID) !DiffStat {
        const qb = try self.query_builder.selection("loadDiffStatFromID", .{
            .id = id,
        });
        return DiffStat{ .query_builder = qb, .client = self.client };
    }

    /// Load a Directory from its ID.
    pub fn loaddirectoryfromid(self: *@This(), id: DirectoryID) !Directory {
        const qb = try self.query_builder.selection("loadDirectoryFromID", .{
            .id = id,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Load a EngineCacheEntry from its ID.
    pub fn loadenginecacheentryfromid(self: *@This(), id: EngineCacheEntryID) !EngineCacheEntry {
        const qb = try self.query_builder.selection("loadEngineCacheEntryFromID", .{
            .id = id,
        });
        return EngineCacheEntry{ .query_builder = qb, .client = self.client };
    }

    /// Load a EngineCacheEntrySet from its ID.
    pub fn loadenginecacheentrysetfromid(self: *@This(), id: EngineCacheEntrySetID) !EngineCacheEntrySet {
        const qb = try self.query_builder.selection("loadEngineCacheEntrySetFromID", .{
            .id = id,
        });
        return EngineCacheEntrySet{ .query_builder = qb, .client = self.client };
    }

    /// Load a EngineCache from its ID.
    pub fn loadenginecachefromid(self: *@This(), id: EngineCacheID) !EngineCache {
        const qb = try self.query_builder.selection("loadEngineCacheFromID", .{
            .id = id,
        });
        return EngineCache{ .query_builder = qb, .client = self.client };
    }

    /// Load a Engine from its ID.
    pub fn loadenginefromid(self: *@This(), id: EngineID) !Engine {
        const qb = try self.query_builder.selection("loadEngineFromID", .{
            .id = id,
        });
        return Engine{ .query_builder = qb, .client = self.client };
    }

    /// Load a EnumTypeDef from its ID.
    pub fn loadenumtypedeffromid(self: *@This(), id: EnumTypeDefID) !EnumTypeDef {
        const qb = try self.query_builder.selection("loadEnumTypeDefFromID", .{
            .id = id,
        });
        return EnumTypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Load a EnumValueTypeDef from its ID.
    pub fn loadenumvaluetypedeffromid(self: *@This(), id: EnumValueTypeDefID) !EnumValueTypeDef {
        const qb = try self.query_builder.selection("loadEnumValueTypeDefFromID", .{
            .id = id,
        });
        return EnumValueTypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Load a EnvFile from its ID.
    pub fn loadenvfilefromid(self: *@This(), id: EnvFileID) !EnvFile {
        const qb = try self.query_builder.selection("loadEnvFileFromID", .{
            .id = id,
        });
        return EnvFile{ .query_builder = qb, .client = self.client };
    }

    /// Load a Env from its ID.
    pub fn loadenvfromid(self: *@This(), id: EnvID) !Env {
        const qb = try self.query_builder.selection("loadEnvFromID", .{
            .id = id,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Load a EnvVariable from its ID.
    pub fn loadenvvariablefromid(self: *@This(), id: EnvVariableID) !EnvVariable {
        const qb = try self.query_builder.selection("loadEnvVariableFromID", .{
            .id = id,
        });
        return EnvVariable{ .query_builder = qb, .client = self.client };
    }

    /// Load a Error from its ID.
    pub fn loaderrorfromid(self: *@This(), id: ErrorID) !Error {
        const qb = try self.query_builder.selection("loadErrorFromID", .{
            .id = id,
        });
        return Error{ .query_builder = qb, .client = self.client };
    }

    /// Load a ErrorValue from its ID.
    pub fn loaderrorvaluefromid(self: *@This(), id: ErrorValueID) !ErrorValue {
        const qb = try self.query_builder.selection("loadErrorValueFromID", .{
            .id = id,
        });
        return ErrorValue{ .query_builder = qb, .client = self.client };
    }

    /// Load a FieldTypeDef from its ID.
    pub fn loadfieldtypedeffromid(self: *@This(), id: FieldTypeDefID) !FieldTypeDef {
        const qb = try self.query_builder.selection("loadFieldTypeDefFromID", .{
            .id = id,
        });
        return FieldTypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Load a File from its ID.
    pub fn loadfilefromid(self: *@This(), id: FileID) !File {
        const qb = try self.query_builder.selection("loadFileFromID", .{
            .id = id,
        });
        return File{ .query_builder = qb, .client = self.client };
    }

    /// Load a FunctionArg from its ID.
    pub fn loadfunctionargfromid(self: *@This(), id: FunctionArgID) !FunctionArg {
        const qb = try self.query_builder.selection("loadFunctionArgFromID", .{
            .id = id,
        });
        return FunctionArg{ .query_builder = qb, .client = self.client };
    }

    /// Load a FunctionCallArgValue from its ID.
    pub fn loadfunctioncallargvaluefromid(self: *@This(), id: FunctionCallArgValueID) !FunctionCallArgValue {
        const qb = try self.query_builder.selection("loadFunctionCallArgValueFromID", .{
            .id = id,
        });
        return FunctionCallArgValue{ .query_builder = qb, .client = self.client };
    }

    /// Load a FunctionCall from its ID.
    pub fn loadfunctioncallfromid(self: *@This(), id: FunctionCallID) !FunctionCall {
        const qb = try self.query_builder.selection("loadFunctionCallFromID", .{
            .id = id,
        });
        return FunctionCall{ .query_builder = qb, .client = self.client };
    }

    /// Load a Function from its ID.
    pub fn loadfunctionfromid(self: *@This(), id: FunctionID) !Function {
        const qb = try self.query_builder.selection("loadFunctionFromID", .{
            .id = id,
        });
        return Function{ .query_builder = qb, .client = self.client };
    }

    /// Load a GeneratedCode from its ID.
    pub fn loadgeneratedcodefromid(self: *@This(), id: GeneratedCodeID) !GeneratedCode {
        const qb = try self.query_builder.selection("loadGeneratedCodeFromID", .{
            .id = id,
        });
        return GeneratedCode{ .query_builder = qb, .client = self.client };
    }

    /// Load a Generator from its ID.
    pub fn loadgeneratorfromid(self: *@This(), id: GeneratorID) !Generator {
        const qb = try self.query_builder.selection("loadGeneratorFromID", .{
            .id = id,
        });
        return Generator{ .query_builder = qb, .client = self.client };
    }

    /// Load a GeneratorGroup from its ID.
    pub fn loadgeneratorgroupfromid(self: *@This(), id: GeneratorGroupID) !GeneratorGroup {
        const qb = try self.query_builder.selection("loadGeneratorGroupFromID", .{
            .id = id,
        });
        return GeneratorGroup{ .query_builder = qb, .client = self.client };
    }

    /// Load a GitRef from its ID.
    pub fn loadgitreffromid(self: *@This(), id: GitRefID) !GitRef {
        const qb = try self.query_builder.selection("loadGitRefFromID", .{
            .id = id,
        });
        return GitRef{ .query_builder = qb, .client = self.client };
    }

    /// Load a GitRepository from its ID.
    pub fn loadgitrepositoryfromid(self: *@This(), id: GitRepositoryID) !GitRepository {
        const qb = try self.query_builder.selection("loadGitRepositoryFromID", .{
            .id = id,
        });
        return GitRepository{ .query_builder = qb, .client = self.client };
    }

    /// Load a HealthcheckConfig from its ID.
    pub fn loadhealthcheckconfigfromid(self: *@This(), id: HealthcheckConfigID) !HealthcheckConfig {
        const qb = try self.query_builder.selection("loadHealthcheckConfigFromID", .{
            .id = id,
        });
        return HealthcheckConfig{ .query_builder = qb, .client = self.client };
    }

    /// Load a Host from its ID.
    pub fn loadhostfromid(self: *@This(), id: HostID) !Host {
        const qb = try self.query_builder.selection("loadHostFromID", .{
            .id = id,
        });
        return Host{ .query_builder = qb, .client = self.client };
    }

    /// Load a InputTypeDef from its ID.
    pub fn loadinputtypedeffromid(self: *@This(), id: InputTypeDefID) !InputTypeDef {
        const qb = try self.query_builder.selection("loadInputTypeDefFromID", .{
            .id = id,
        });
        return InputTypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Load a InterfaceTypeDef from its ID.
    pub fn loadinterfacetypedeffromid(self: *@This(), id: InterfaceTypeDefID) !InterfaceTypeDef {
        const qb = try self.query_builder.selection("loadInterfaceTypeDefFromID", .{
            .id = id,
        });
        return InterfaceTypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Load a JSONValue from its ID.
    pub fn loadjsonvaluefromid(self: *@This(), id: JSONValueID) !JSONValue {
        const qb = try self.query_builder.selection("loadJSONValueFromID", .{
            .id = id,
        });
        return JSONValue{ .query_builder = qb, .client = self.client };
    }

    /// Load a LLM from its ID.
    pub fn loadllmfromid(self: *@This(), id: LLMID) !LLM {
        const qb = try self.query_builder.selection("loadLLMFromID", .{
            .id = id,
        });
        return LLM{ .query_builder = qb, .client = self.client };
    }

    /// Load a LLMTokenUsage from its ID.
    pub fn loadllmtokenusagefromid(self: *@This(), id: LLMTokenUsageID) !LLMTokenUsage {
        const qb = try self.query_builder.selection("loadLLMTokenUsageFromID", .{
            .id = id,
        });
        return LLMTokenUsage{ .query_builder = qb, .client = self.client };
    }

    /// Load a Label from its ID.
    pub fn loadlabelfromid(self: *@This(), id: LabelID) !Label {
        const qb = try self.query_builder.selection("loadLabelFromID", .{
            .id = id,
        });
        return Label{ .query_builder = qb, .client = self.client };
    }

    /// Load a ListTypeDef from its ID.
    pub fn loadlisttypedeffromid(self: *@This(), id: ListTypeDefID) !ListTypeDef {
        const qb = try self.query_builder.selection("loadListTypeDefFromID", .{
            .id = id,
        });
        return ListTypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Load a ModuleConfigClient from its ID.
    pub fn loadmoduleconfigclientfromid(self: *@This(), id: ModuleConfigClientID) !ModuleConfigClient {
        const qb = try self.query_builder.selection("loadModuleConfigClientFromID", .{
            .id = id,
        });
        return ModuleConfigClient{ .query_builder = qb, .client = self.client };
    }

    /// Load a Module from its ID.
    pub fn loadmodulefromid(self: *@This(), id: ModuleID) !Module {
        const qb = try self.query_builder.selection("loadModuleFromID", .{
            .id = id,
        });
        return Module{ .query_builder = qb, .client = self.client };
    }

    /// Load a ModuleSource from its ID.
    pub fn loadmodulesourcefromid(self: *@This(), id: ModuleSourceID) !ModuleSource {
        const qb = try self.query_builder.selection("loadModuleSourceFromID", .{
            .id = id,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Load a ObjectTypeDef from its ID.
    pub fn loadobjecttypedeffromid(self: *@This(), id: ObjectTypeDefID) !ObjectTypeDef {
        const qb = try self.query_builder.selection("loadObjectTypeDefFromID", .{
            .id = id,
        });
        return ObjectTypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Load a Port from its ID.
    pub fn loadportfromid(self: *@This(), id: PortID) !Port {
        const qb = try self.query_builder.selection("loadPortFromID", .{
            .id = id,
        });
        return Port{ .query_builder = qb, .client = self.client };
    }

    /// Load a Query from its ID.
    pub fn loadqueryfromid(self: *@This(), id: QueryID) !Query {
        const qb = try self.query_builder.selection("loadQueryFromID", .{
            .id = id,
        });
        return Query{ .query_builder = qb, .client = self.client };
    }

    /// Load a SDKConfig from its ID.
    pub fn loadsdkconfigfromid(self: *@This(), id: SDKConfigID) !?SDKConfig {
        const qb = try self.query_builder.selection("loadSDKConfigFromID", .{
            .id = id,
        });
        return SDKConfig{ .query_builder = qb, .client = self.client };
    }

    /// Load a ScalarTypeDef from its ID.
    pub fn loadscalartypedeffromid(self: *@This(), id: ScalarTypeDefID) !ScalarTypeDef {
        const qb = try self.query_builder.selection("loadScalarTypeDefFromID", .{
            .id = id,
        });
        return ScalarTypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Load a SearchResult from its ID.
    pub fn loadsearchresultfromid(self: *@This(), id: SearchResultID) !SearchResult {
        const qb = try self.query_builder.selection("loadSearchResultFromID", .{
            .id = id,
        });
        return SearchResult{ .query_builder = qb, .client = self.client };
    }

    /// Load a SearchSubmatch from its ID.
    pub fn loadsearchsubmatchfromid(self: *@This(), id: SearchSubmatchID) !SearchSubmatch {
        const qb = try self.query_builder.selection("loadSearchSubmatchFromID", .{
            .id = id,
        });
        return SearchSubmatch{ .query_builder = qb, .client = self.client };
    }

    /// Load a Secret from its ID.
    pub fn loadsecretfromid(self: *@This(), id: SecretID) !Secret {
        const qb = try self.query_builder.selection("loadSecretFromID", .{
            .id = id,
        });
        return Secret{ .query_builder = qb, .client = self.client };
    }

    /// Load a Service from its ID.
    pub fn loadservicefromid(self: *@This(), id: ServiceID) !Service {
        const qb = try self.query_builder.selection("loadServiceFromID", .{
            .id = id,
        });
        return Service{ .query_builder = qb, .client = self.client };
    }

    /// Load a Socket from its ID.
    pub fn loadsocketfromid(self: *@This(), id: SocketID) !Socket {
        const qb = try self.query_builder.selection("loadSocketFromID", .{
            .id = id,
        });
        return Socket{ .query_builder = qb, .client = self.client };
    }

    /// Load a SourceMap from its ID.
    pub fn loadsourcemapfromid(self: *@This(), id: SourceMapID) !SourceMap {
        const qb = try self.query_builder.selection("loadSourceMapFromID", .{
            .id = id,
        });
        return SourceMap{ .query_builder = qb, .client = self.client };
    }

    /// Load a Stat from its ID.
    pub fn loadstatfromid(self: *@This(), id: StatID) !?Stat {
        const qb = try self.query_builder.selection("loadStatFromID", .{
            .id = id,
        });
        return Stat{ .query_builder = qb, .client = self.client };
    }

    /// Load a Terminal from its ID.
    pub fn loadterminalfromid(self: *@This(), id: TerminalID) !Terminal {
        const qb = try self.query_builder.selection("loadTerminalFromID", .{
            .id = id,
        });
        return Terminal{ .query_builder = qb, .client = self.client };
    }

    /// Load a TypeDef from its ID.
    pub fn loadtypedeffromid(self: *@This(), id: TypeDefID) !TypeDef {
        const qb = try self.query_builder.selection("loadTypeDefFromID", .{
            .id = id,
        });
        return TypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Load a Up from its ID.
    pub fn loadupfromid(self: *@This(), id: UpID) !Up {
        const qb = try self.query_builder.selection("loadUpFromID", .{
            .id = id,
        });
        return Up{ .query_builder = qb, .client = self.client };
    }

    /// Load a UpGroup from its ID.
    pub fn loadupgroupfromid(self: *@This(), id: UpGroupID) !UpGroup {
        const qb = try self.query_builder.selection("loadUpGroupFromID", .{
            .id = id,
        });
        return UpGroup{ .query_builder = qb, .client = self.client };
    }

    /// Load a Workspace from its ID.
    pub fn loadworkspacefromid(self: *@This(), id: WorkspaceID) !Workspace {
        const qb = try self.query_builder.selection("loadWorkspaceFromID", .{
            .id = id,
        });
        return Workspace{ .query_builder = qb, .client = self.client };
    }

    /// Create a new module.
    pub fn module(self: *@This()) !Module {
        const qb = try self.query_builder.selection("module", .{});
        return Module{ .query_builder = qb, .client = self.client };
    }

    /// Create a new module source instance from a source ref string
    pub fn modulesource(self: *@This(), refstring: []const u8, options: struct {
        refpin: ?[]const u8 = null,
        disablefindup: ?bool = null,
        allownotexists: ?bool = null,
        requirekind: ?ModuleSourceKind = null,
    }) !ModuleSource {
        const qb = try self.query_builder.selection("moduleSource", .{
            .refstring = refstring,
            .refpin = options.refpin,
            .disablefindup = options.disablefindup,
            .allownotexists = options.allownotexists,
            .requirekind = options.requirekind,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Creates a new secret.
    pub fn secret(self: *@This(), uri: []const u8, options: struct {
        cachekey: ?[]const u8 = null,
    }) !Secret {
        const qb = try self.query_builder.selection("secret", .{
            .uri = uri,
            .cachekey = options.cachekey,
        });
        return Secret{ .query_builder = qb, .client = self.client };
    }

    /// Sets a secret given a user defined name to its plaintext and returns the secret.
    ///
    /// The plaintext value is limited to a size of 128000 bytes.
    pub fn setsecret(self: *@This(), name: []const u8, plaintext: []const u8) !Secret {
        const qb = try self.query_builder.selection("setSecret", .{
            .name = name,
            .plaintext = plaintext,
        });
        return Secret{ .query_builder = qb, .client = self.client };
    }

    /// Creates source map metadata.
    pub fn sourcemap(self: *@This(), filename: []const u8, line: i32, column: i32) !SourceMap {
        const qb = try self.query_builder.selection("sourceMap", .{
            .filename = filename,
            .line = line,
            .column = column,
        });
        return SourceMap{ .query_builder = qb, .client = self.client };
    }

    /// Create a new TypeDef.
    pub fn typedef(self: *@This()) !TypeDef {
        const qb = try self.query_builder.selection("typeDef", .{});
        return TypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Get the current Dagger Engine version.
    pub fn version(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("version", .{});
        return try self.client.execute(qb, []const u8);
    }

    pub fn execute(self: *Client, query_builder: *QueryBuilder, response_type: anytype) !@TypeOf(response_type) {
        _ = self;
        _ = query_builder;
        _ = response_type;
        return error.Unimplemented;
    }
};

/// A standardized address to load containers, directories, secrets, and other object types. Address format depends on the type, and is validated at type selection.
pub const Address = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Load a container from the address.
    pub fn container(self: *@This()) !Container {
        const qb = try self.query_builder.selection("container", .{});
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Load a directory from the address.
    pub fn directory(self: *@This(), options: struct {
        exclude: ?[]const []const u8 = null,
        include: ?[]const []const u8 = null,
        gitignore: ?bool = null,
        nocache: ?bool = null,
    }) !Directory {
        const qb = try self.query_builder.selection("directory", .{
            .exclude = options.exclude,
            .include = options.include,
            .gitignore = options.gitignore,
            .nocache = options.nocache,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Load a file from the address.
    pub fn file(self: *@This(), options: struct {
        exclude: ?[]const []const u8 = null,
        include: ?[]const []const u8 = null,
        gitignore: ?bool = null,
        nocache: ?bool = null,
    }) !File {
        const qb = try self.query_builder.selection("file", .{
            .exclude = options.exclude,
            .include = options.include,
            .gitignore = options.gitignore,
            .nocache = options.nocache,
        });
        return File{ .query_builder = qb, .client = self.client };
    }

    /// Load a git ref (branch, tag or commit) from the address.
    pub fn gitref(self: *@This()) !GitRef {
        const qb = try self.query_builder.selection("gitRef", .{});
        return GitRef{ .query_builder = qb, .client = self.client };
    }

    /// Load a git repository from the address.
    pub fn gitrepository(self: *@This()) !GitRepository {
        const qb = try self.query_builder.selection("gitRepository", .{});
        return GitRepository{ .query_builder = qb, .client = self.client };
    }

    /// A unique identifier for this Address.
    pub fn id(self: *@This()) !AddressID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, AddressID);
    }

    /// Load a secret from the address.
    pub fn secret(self: *@This()) !Secret {
        const qb = try self.query_builder.selection("secret", .{});
        return Secret{ .query_builder = qb, .client = self.client };
    }

    /// Load a service from the address.
    pub fn service(self: *@This()) !Service {
        const qb = try self.query_builder.selection("service", .{});
        return Service{ .query_builder = qb, .client = self.client };
    }

    /// Load a local socket from the address.
    pub fn socket(self: *@This()) !Socket {
        const qb = try self.query_builder.selection("socket", .{});
        return Socket{ .query_builder = qb, .client = self.client };
    }

    /// The address value
    pub fn value(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("value", .{});
        return try self.client.execute(qb, []const u8);
    }
};

///
pub const Binding = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Retrieve the binding value, as type Address
    pub fn asaddress(self: *@This()) !Address {
        const qb = try self.query_builder.selection("asAddress", .{});
        return Address{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type CacheVolume
    pub fn ascachevolume(self: *@This()) !CacheVolume {
        const qb = try self.query_builder.selection("asCacheVolume", .{});
        return CacheVolume{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type Changeset
    pub fn aschangeset(self: *@This()) !Changeset {
        const qb = try self.query_builder.selection("asChangeset", .{});
        return Changeset{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type Check
    pub fn ascheck(self: *@This()) !Check {
        const qb = try self.query_builder.selection("asCheck", .{});
        return Check{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type CheckGroup
    pub fn ascheckgroup(self: *@This()) !CheckGroup {
        const qb = try self.query_builder.selection("asCheckGroup", .{});
        return CheckGroup{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type Cloud
    pub fn ascloud(self: *@This()) !Cloud {
        const qb = try self.query_builder.selection("asCloud", .{});
        return Cloud{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type Container
    pub fn ascontainer(self: *@This()) !Container {
        const qb = try self.query_builder.selection("asContainer", .{});
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type DiffStat
    pub fn asdiffstat(self: *@This()) !DiffStat {
        const qb = try self.query_builder.selection("asDiffStat", .{});
        return DiffStat{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type Directory
    pub fn asdirectory(self: *@This()) !Directory {
        const qb = try self.query_builder.selection("asDirectory", .{});
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type Env
    pub fn asenv(self: *@This()) !Env {
        const qb = try self.query_builder.selection("asEnv", .{});
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type EnvFile
    pub fn asenvfile(self: *@This()) !EnvFile {
        const qb = try self.query_builder.selection("asEnvFile", .{});
        return EnvFile{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type File
    pub fn asfile(self: *@This()) !File {
        const qb = try self.query_builder.selection("asFile", .{});
        return File{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type Generator
    pub fn asgenerator(self: *@This()) !Generator {
        const qb = try self.query_builder.selection("asGenerator", .{});
        return Generator{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type GeneratorGroup
    pub fn asgeneratorgroup(self: *@This()) !GeneratorGroup {
        const qb = try self.query_builder.selection("asGeneratorGroup", .{});
        return GeneratorGroup{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type GitRef
    pub fn asgitref(self: *@This()) !GitRef {
        const qb = try self.query_builder.selection("asGitRef", .{});
        return GitRef{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type GitRepository
    pub fn asgitrepository(self: *@This()) !GitRepository {
        const qb = try self.query_builder.selection("asGitRepository", .{});
        return GitRepository{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type JSONValue
    pub fn asjsonvalue(self: *@This()) !JSONValue {
        const qb = try self.query_builder.selection("asJSONValue", .{});
        return JSONValue{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type Module
    pub fn asmodule(self: *@This()) !Module {
        const qb = try self.query_builder.selection("asModule", .{});
        return Module{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type ModuleConfigClient
    pub fn asmoduleconfigclient(self: *@This()) !ModuleConfigClient {
        const qb = try self.query_builder.selection("asModuleConfigClient", .{});
        return ModuleConfigClient{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type ModuleSource
    pub fn asmodulesource(self: *@This()) !ModuleSource {
        const qb = try self.query_builder.selection("asModuleSource", .{});
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type SearchResult
    pub fn assearchresult(self: *@This()) !SearchResult {
        const qb = try self.query_builder.selection("asSearchResult", .{});
        return SearchResult{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type SearchSubmatch
    pub fn assearchsubmatch(self: *@This()) !SearchSubmatch {
        const qb = try self.query_builder.selection("asSearchSubmatch", .{});
        return SearchSubmatch{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type Secret
    pub fn assecret(self: *@This()) !Secret {
        const qb = try self.query_builder.selection("asSecret", .{});
        return Secret{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type Service
    pub fn asservice(self: *@This()) !Service {
        const qb = try self.query_builder.selection("asService", .{});
        return Service{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type Socket
    pub fn assocket(self: *@This()) !Socket {
        const qb = try self.query_builder.selection("asSocket", .{});
        return Socket{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type Stat
    pub fn asstat(self: *@This()) !?Stat {
        const qb = try self.query_builder.selection("asStat", .{});
        return Stat{ .query_builder = qb, .client = self.client };
    }

    /// Returns the binding's string value
    pub fn asstring(self: *@This()) !?[]const u8 {
        const qb = try self.query_builder.selection("asString", .{});
        return try self.client.execute(qb, ?[]const u8);
    }

    /// Retrieve the binding value, as type Up
    pub fn asup(self: *@This()) !Up {
        const qb = try self.query_builder.selection("asUp", .{});
        return Up{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type UpGroup
    pub fn asupgroup(self: *@This()) !UpGroup {
        const qb = try self.query_builder.selection("asUpGroup", .{});
        return UpGroup{ .query_builder = qb, .client = self.client };
    }

    /// Retrieve the binding value, as type Workspace
    pub fn asworkspace(self: *@This()) !Workspace {
        const qb = try self.query_builder.selection("asWorkspace", .{});
        return Workspace{ .query_builder = qb, .client = self.client };
    }

    /// Returns the digest of the binding value
    pub fn digest(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("digest", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this Binding.
    pub fn id(self: *@This()) !BindingID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, BindingID);
    }

    /// Returns true if the binding is null
    pub fn isnull(self: *@This()) !bool {
        const qb = try self.query_builder.selection("isNull", .{});
        return try self.client.execute(qb, bool);
    }

    /// Returns the binding name
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Returns the binding type
    pub fn typename(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("typeName", .{});
        return try self.client.execute(qb, []const u8);
    }
};

/// A directory whose contents persist across runs.
pub const CacheVolume = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// A unique identifier for this CacheVolume.
    pub fn id(self: *@This()) !CacheVolumeID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, CacheVolumeID);
    }
};

/// A comparison between two directories representing changes that can be applied.
pub const Changeset = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Files and directories that were added in the newer directory.
    pub fn addedpaths(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("addedPaths", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// The newer/upper snapshot.
    pub fn after(self: *@This()) !Directory {
        const qb = try self.query_builder.selection("after", .{});
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Return a Git-compatible patch of the changes
    pub fn aspatch(self: *@This()) !File {
        const qb = try self.query_builder.selection("asPatch", .{});
        return File{ .query_builder = qb, .client = self.client };
    }

    /// The older/lower snapshot to compare against.
    pub fn before(self: *@This()) !Directory {
        const qb = try self.query_builder.selection("before", .{});
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Structured per-path diff statistics (kind and line counts) for this changeset.
    pub fn diffstats(self: *@This()) ![]const DiffStat {
        const qb = try self.query_builder.selection("diffStats", .{});
        const ids = try self.client.execute(qb, []const DiffStatID);
        var results = try self.client.allocator.alloc(DiffStat, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadDiffStatFromID(id);
        }
        return results;
    }

    /// Applies the diff represented by this changeset to a path on the host.
    pub fn @"export"(self: *@This(), path: []const u8) ![]const u8 {
        const qb = try self.query_builder.selection("export", .{
            .path = path,
        });
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this Changeset.
    pub fn id(self: *@This()) !ChangesetID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, ChangesetID);
    }

    /// Returns true if the changeset is empty (i.e. there are no changes).
    pub fn isempty(self: *@This()) !bool {
        const qb = try self.query_builder.selection("isEmpty", .{});
        return try self.client.execute(qb, bool);
    }

    /// Return a snapshot containing only the created and modified files
    pub fn layer(self: *@This()) !Directory {
        const qb = try self.query_builder.selection("layer", .{});
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Files and directories that existed before and were updated in the newer directory.
    pub fn modifiedpaths(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("modifiedPaths", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// Files and directories that were removed. Directories are indicated by a trailing slash, and their child paths are not included.
    pub fn removedpaths(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("removedPaths", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// Force evaluation in the engine.
    pub fn sync(self: *@This()) !ChangesetID {
        const qb = try self.query_builder.selection("sync", .{});
        const id = try self.client.execute(qb, ChangesetID);
        return try self.client.loadChangesetFromID(id);
    }

    /// Add changes to an existing changeset
    ///
    /// By default the operation will fail in case of conflicts, for instance a file modified in both changesets. The behavior can be adjusted using onConflict argument
    pub fn withchangeset(self: *@This(), changes: ChangesetID, options: struct {
        onconflict: ?ChangesetMergeConflict = null,
    }) !Changeset {
        const qb = try self.query_builder.selection("withChangeset", .{
            .changes = changes,
            .onconflict = options.onconflict,
        });
        return Changeset{ .query_builder = qb, .client = self.client };
    }

    /// Add changes from multiple changesets using git octopus merge strategy
    ///
    /// This is more efficient than chaining multiple withChangeset calls when merging many changesets.
    ///
    /// Only FAIL and FAIL_EARLY conflict strategies are supported (octopus merge cannot use -X ours/theirs).
    pub fn withchangesets(self: *@This(), changes: []const ChangesetID, options: struct {
        onconflict: ?ChangesetsMergeConflict = null,
    }) !Changeset {
        const qb = try self.query_builder.selection("withChangesets", .{
            .changes = changes,
            .onconflict = options.onconflict,
        });
        return Changeset{ .query_builder = qb, .client = self.client };
    }
};

///
pub const Check = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Whether the check completed
    pub fn completed(self: *@This()) !bool {
        const qb = try self.query_builder.selection("completed", .{});
        return try self.client.execute(qb, bool);
    }

    /// The description of the check
    pub fn description(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("description", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// If the check failed, this is the error
    pub fn @"error"(self: *@This()) !?Error {
        const qb = try self.query_builder.selection("error", .{});
        return Error{ .query_builder = qb, .client = self.client };
    }

    /// A unique identifier for this Check.
    pub fn id(self: *@This()) !CheckID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, CheckID);
    }

    /// Return the fully qualified name of the check
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The original module in which the check has been defined
    pub fn originalmodule(self: *@This()) !Module {
        const qb = try self.query_builder.selection("originalModule", .{});
        return Module{ .query_builder = qb, .client = self.client };
    }

    /// Whether the check passed
    pub fn passed(self: *@This()) !bool {
        const qb = try self.query_builder.selection("passed", .{});
        return try self.client.execute(qb, bool);
    }

    /// The path of the check within its module
    pub fn path(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("path", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// An emoji representing the result of the check
    pub fn resultemoji(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("resultEmoji", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Execute the check
    pub fn run(self: *@This()) !Check {
        const qb = try self.query_builder.selection("run", .{});
        return Check{ .query_builder = qb, .client = self.client };
    }
};

///
pub const CheckGroup = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// A unique identifier for this CheckGroup.
    pub fn id(self: *@This()) !CheckGroupID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, CheckGroupID);
    }

    /// Return a list of individual checks and their details
    pub fn list(self: *@This()) ![]const Check {
        const qb = try self.query_builder.selection("list", .{});
        const ids = try self.client.execute(qb, []const CheckID);
        var results = try self.client.allocator.alloc(Check, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadCheckFromID(id);
        }
        return results;
    }

    /// Generate a markdown report
    pub fn report(self: *@This()) !File {
        const qb = try self.query_builder.selection("report", .{});
        return File{ .query_builder = qb, .client = self.client };
    }

    /// Execute all selected checks
    pub fn run(self: *@This(), options: struct {
        failfast: ?bool = null,
    }) !CheckGroup {
        const qb = try self.query_builder.selection("run", .{
            .failfast = options.failfast,
        });
        return CheckGroup{ .query_builder = qb, .client = self.client };
    }
};

/// Dagger Cloud configuration and state
pub const Cloud = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// A unique identifier for this Cloud.
    pub fn id(self: *@This()) !CloudID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, CloudID);
    }

    /// The trace URL for the current session
    pub fn traceurl(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("traceURL", .{});
        return try self.client.execute(qb, []const u8);
    }
};

/// An OCI-compatible container, also known as a Docker container.
pub const Container = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Turn the container into a Service.
    ///
    /// Be sure to set any exposed ports before this conversion.
    pub fn asservice(self: *@This(), options: struct {
        args: ?[]const []const u8 = null,
        useentrypoint: ?bool = null,
        experimentalprivilegednesting: ?bool = null,
        insecurerootcapabilities: ?bool = null,
        expand: ?bool = null,
        noinit: ?bool = null,
    }) !Service {
        const qb = try self.query_builder.selection("asService", .{
            .args = options.args,
            .useentrypoint = options.useentrypoint,
            .experimentalprivilegednesting = options.experimentalprivilegednesting,
            .insecurerootcapabilities = options.insecurerootcapabilities,
            .expand = options.expand,
            .noinit = options.noinit,
        });
        return Service{ .query_builder = qb, .client = self.client };
    }

    /// Package the container state as an OCI image, and return it as a tar archive
    pub fn astarball(self: *@This(), options: struct {
        platformvariants: ?[]const ContainerID = null,
        forcedcompression: ?ImageLayerCompression = null,
        mediatypes: ?ImageMediaTypes = null,
    }) !File {
        const qb = try self.query_builder.selection("asTarball", .{
            .platformvariants = options.platformvariants,
            .forcedcompression = options.forcedcompression,
            .mediatypes = options.mediatypes,
        });
        return File{ .query_builder = qb, .client = self.client };
    }

    /// The combined buffered standard output and standard error stream of the last executed command
    ///
    /// Returns an error if no command was executed
    pub fn combinedoutput(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("combinedOutput", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Return the container's default arguments.
    pub fn defaultargs(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("defaultArgs", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// Retrieve a directory from the container's root filesystem
    ///
    /// Mounts are included.
    pub fn directory(self: *@This(), path: []const u8, options: struct {
        expand: ?bool = null,
    }) !Directory {
        const qb = try self.query_builder.selection("directory", .{
            .path = path,
            .expand = options.expand,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container's configured docker healthcheck.
    pub fn dockerhealthcheck(self: *@This()) !?HealthcheckConfig {
        const qb = try self.query_builder.selection("dockerHealthcheck", .{});
        return HealthcheckConfig{ .query_builder = qb, .client = self.client };
    }

    /// Return the container's OCI entrypoint.
    pub fn entrypoint(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("entrypoint", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// Retrieves the value of the specified environment variable.
    pub fn envvariable(self: *@This(), name: []const u8) !?[]const u8 {
        const qb = try self.query_builder.selection("envVariable", .{
            .name = name,
        });
        return try self.client.execute(qb, ?[]const u8);
    }

    /// Retrieves the list of environment variables passed to commands.
    pub fn envvariables(self: *@This()) ![]const EnvVariable {
        const qb = try self.query_builder.selection("envVariables", .{});
        const ids = try self.client.execute(qb, []const EnvVariableID);
        var results = try self.client.allocator.alloc(EnvVariable, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadEnvVariableFromID(id);
        }
        return results;
    }

    /// check if a file or directory exists
    pub fn exists(self: *@This(), path: []const u8, options: struct {
        expectedtype: ?ExistsType = null,
        donotfollowsymlinks: ?bool = null,
    }) !bool {
        const qb = try self.query_builder.selection("exists", .{
            .path = path,
            .expectedtype = options.expectedtype,
            .donotfollowsymlinks = options.donotfollowsymlinks,
        });
        return try self.client.execute(qb, bool);
    }

    /// The exit code of the last executed command
    ///
    /// Returns an error if no command was executed
    pub fn exitcode(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("exitCode", .{});
        return try self.client.execute(qb, i32);
    }

    /// EXPERIMENTAL API! Subject to change/removal at any time.
    ///
    /// Configures all available GPUs on the host to be accessible to this container.
    ///
    /// This currently works for Nvidia devices only.
    pub fn experimentalwithallgpus(self: *@This()) !Container {
        const qb = try self.query_builder.selection("experimentalWithAllGPUs", .{});
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// EXPERIMENTAL API! Subject to change/removal at any time.
    ///
    /// Configures the provided list of devices to be accessible to this container.
    ///
    /// This currently works for Nvidia devices only.
    pub fn experimentalwithgpu(self: *@This(), devices: []const []const u8) !Container {
        const qb = try self.query_builder.selection("experimentalWithGPU", .{
            .devices = devices,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Writes the container as an OCI tarball to the destination file path on the host.
    ///
    /// It can also export platform variants.
    pub fn @"export"(self: *@This(), path: []const u8, options: struct {
        platformvariants: ?[]const ContainerID = null,
        forcedcompression: ?ImageLayerCompression = null,
        mediatypes: ?ImageMediaTypes = null,
        expand: ?bool = null,
    }) ![]const u8 {
        const qb = try self.query_builder.selection("export", .{
            .path = path,
            .platformvariants = options.platformvariants,
            .forcedcompression = options.forcedcompression,
            .mediatypes = options.mediatypes,
            .expand = options.expand,
        });
        return try self.client.execute(qb, []const u8);
    }

    /// Exports the container as an image to the host's container image store.
    pub fn exportimage(self: *@This(), name: []const u8, options: struct {
        platformvariants: ?[]const ContainerID = null,
        forcedcompression: ?ImageLayerCompression = null,
        mediatypes: ?ImageMediaTypes = null,
    }) !void {
        const qb = try self.query_builder.selection("exportImage", .{
            .name = name,
            .platformvariants = options.platformvariants,
            .forcedcompression = options.forcedcompression,
            .mediatypes = options.mediatypes,
        });
        _ = try self.client.execute(qb, void);
    }

    /// Retrieves the list of exposed ports.
    ///
    /// This includes ports already exposed by the image, even if not explicitly added with dagger.
    pub fn exposedports(self: *@This()) ![]const Port {
        const qb = try self.query_builder.selection("exposedPorts", .{});
        const ids = try self.client.execute(qb, []const PortID);
        var results = try self.client.allocator.alloc(Port, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadPortFromID(id);
        }
        return results;
    }

    /// Retrieves a file at the given path.
    ///
    /// Mounts are included.
    pub fn file(self: *@This(), path: []const u8, options: struct {
        expand: ?bool = null,
    }) !File {
        const qb = try self.query_builder.selection("file", .{
            .path = path,
            .expand = options.expand,
        });
        return File{ .query_builder = qb, .client = self.client };
    }

    /// Download a container image, and apply it to the container state. All previous state will be lost.
    pub fn from(self: *@This(), address: []const u8) !Container {
        const qb = try self.query_builder.selection("from", .{
            .address = address,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// A unique identifier for this Container.
    pub fn id(self: *@This()) !ContainerID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, ContainerID);
    }

    /// The unique image reference which can only be retrieved immediately after the 'Container.From' call.
    pub fn imageref(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("imageRef", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Reads the container from an OCI tarball.
    pub fn import(self: *@This(), source: FileID, options: struct {
        tag: ?[]const u8 = null,
    }) !Container {
        const qb = try self.query_builder.selection("import", .{
            .source = source,
            .tag = options.tag,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves the value of the specified label.
    pub fn label(self: *@This(), name: []const u8) !?[]const u8 {
        const qb = try self.query_builder.selection("label", .{
            .name = name,
        });
        return try self.client.execute(qb, ?[]const u8);
    }

    /// Retrieves the list of labels passed to container.
    pub fn labels(self: *@This()) ![]const Label {
        const qb = try self.query_builder.selection("labels", .{});
        const ids = try self.client.execute(qb, []const LabelID);
        var results = try self.client.allocator.alloc(Label, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadLabelFromID(id);
        }
        return results;
    }

    /// Retrieves the list of paths where a directory is mounted.
    pub fn mounts(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("mounts", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// The platform this container executes and publishes as.
    pub fn platform(self: *@This()) !Platform {
        const qb = try self.query_builder.selection("platform", .{});
        return try self.client.execute(qb, Platform);
    }

    /// Package the container state as an OCI image, and publish it to a registry
    ///
    /// Returns the fully qualified address of the published image, with digest
    pub fn publish(self: *@This(), address: []const u8, options: struct {
        platformvariants: ?[]const ContainerID = null,
        forcedcompression: ?ImageLayerCompression = null,
        mediatypes: ?ImageMediaTypes = null,
    }) ![]const u8 {
        const qb = try self.query_builder.selection("publish", .{
            .address = address,
            .platformvariants = options.platformvariants,
            .forcedcompression = options.forcedcompression,
            .mediatypes = options.mediatypes,
        });
        return try self.client.execute(qb, []const u8);
    }

    /// Return a snapshot of the container's root filesystem. The snapshot can be modified then written back using withRootfs. Use that method for filesystem modifications.
    pub fn rootfs(self: *@This()) !Directory {
        const qb = try self.query_builder.selection("rootfs", .{});
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Return file status
    pub fn stat(self: *@This(), path: []const u8, options: struct {
        donotfollowsymlinks: ?bool = null,
    }) !?Stat {
        const qb = try self.query_builder.selection("stat", .{
            .path = path,
            .donotfollowsymlinks = options.donotfollowsymlinks,
        });
        return Stat{ .query_builder = qb, .client = self.client };
    }

    /// The buffered standard error stream of the last executed command
    ///
    /// Returns an error if no command was executed
    pub fn stderr(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("stderr", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The buffered standard output stream of the last executed command
    ///
    /// Returns an error if no command was executed
    pub fn stdout(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("stdout", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Forces evaluation of the pipeline in the engine.
    ///
    /// It doesn't run the default command if no exec has been set.
    pub fn sync(self: *@This()) !ContainerID {
        const qb = try self.query_builder.selection("sync", .{});
        const id = try self.client.execute(qb, ContainerID);
        return try self.client.loadContainerFromID(id);
    }

    /// Opens an interactive terminal for this container using its configured default terminal command if not overridden by args (or sh as a fallback default).
    pub fn terminal(self: *@This(), options: struct {
        cmd: ?[]const []const u8 = null,
        experimentalprivilegednesting: ?bool = null,
        insecurerootcapabilities: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("terminal", .{
            .cmd = options.cmd,
            .experimentalprivilegednesting = options.experimentalprivilegednesting,
            .insecurerootcapabilities = options.insecurerootcapabilities,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Starts a Service and creates a tunnel that forwards traffic from the caller's network to that service.
    ///
    /// Be sure to set any exposed ports before calling this api.
    pub fn up(self: *@This(), options: struct {
        random: ?bool = null,
        ports: ?[]const PortForward = null,
        args: ?[]const []const u8 = null,
        useentrypoint: ?bool = null,
        experimentalprivilegednesting: ?bool = null,
        insecurerootcapabilities: ?bool = null,
        expand: ?bool = null,
        noinit: ?bool = null,
    }) !?void {
        const qb = try self.query_builder.selection("up", .{
            .random = options.random,
            .ports = options.ports,
            .args = options.args,
            .useentrypoint = options.useentrypoint,
            .experimentalprivilegednesting = options.experimentalprivilegednesting,
            .insecurerootcapabilities = options.insecurerootcapabilities,
            .expand = options.expand,
            .noinit = options.noinit,
        });
        _ = try self.client.execute(qb, void);
    }

    /// Retrieves the user to be set for all commands.
    pub fn user(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("user", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Retrieves this container plus the given OCI annotation.
    pub fn withannotation(self: *@This(), name: []const u8, value: []const u8) !Container {
        const qb = try self.query_builder.selection("withAnnotation", .{
            .name = name,
            .value = value,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Configures default arguments for future commands. Like CMD in Dockerfile.
    pub fn withdefaultargs(self: *@This(), args: []const []const u8) !Container {
        const qb = try self.query_builder.selection("withDefaultArgs", .{
            .args = args,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Set the default command to invoke for the container's terminal API.
    pub fn withdefaultterminalcmd(self: *@This(), args: []const []const u8, options: struct {
        experimentalprivilegednesting: ?bool = null,
        insecurerootcapabilities: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withDefaultTerminalCmd", .{
            .args = args,
            .experimentalprivilegednesting = options.experimentalprivilegednesting,
            .insecurerootcapabilities = options.insecurerootcapabilities,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Return a new container snapshot, with a directory added to its filesystem
    pub fn withdirectory(self: *@This(), path: []const u8, source: DirectoryID, options: struct {
        exclude: ?[]const []const u8 = null,
        include: ?[]const []const u8 = null,
        gitignore: ?bool = null,
        owner: ?[]const u8 = null,
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withDirectory", .{
            .path = path,
            .source = source,
            .exclude = options.exclude,
            .include = options.include,
            .gitignore = options.gitignore,
            .owner = options.owner,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container with the specificed docker healtcheck command set.
    pub fn withdockerhealthcheck(self: *@This(), args: []const []const u8, options: struct {
        shell: ?bool = null,
        interval: ?[]const u8 = null,
        timeout: ?[]const u8 = null,
        startperiod: ?[]const u8 = null,
        startinterval: ?[]const u8 = null,
        retries: ?i32 = null,
    }) !Container {
        const qb = try self.query_builder.selection("withDockerHealthcheck", .{
            .args = args,
            .shell = options.shell,
            .interval = options.interval,
            .timeout = options.timeout,
            .startperiod = options.startperiod,
            .startinterval = options.startinterval,
            .retries = options.retries,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Set an OCI-style entrypoint. It will be included in the container's OCI configuration. Note, withExec ignores the entrypoint by default.
    pub fn withentrypoint(self: *@This(), args: []const []const u8, options: struct {
        keepdefaultargs: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withEntrypoint", .{
            .args = args,
            .keepdefaultargs = options.keepdefaultargs,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Export environment variables from an env-file to the container.
    pub fn withenvfilevariables(self: *@This(), source: EnvFileID) !Container {
        const qb = try self.query_builder.selection("withEnvFileVariables", .{
            .source = source,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Set a new environment variable in the container.
    pub fn withenvvariable(self: *@This(), name: []const u8, value: []const u8, options: struct {
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withEnvVariable", .{
            .name = name,
            .value = value,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Raise an error.
    pub fn witherror(self: *@This(), err: []const u8) !Container {
        const qb = try self.query_builder.selection("withError", .{
            .err = err,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Execute a command in the container, and return a new snapshot of the container state after execution.
    pub fn withexec(self: *@This(), args: []const []const u8, options: struct {
        useentrypoint: ?bool = null,
        stdin: ?[]const u8 = null,
        redirectstdin: ?[]const u8 = null,
        redirectstdout: ?[]const u8 = null,
        redirectstderr: ?[]const u8 = null,
        expect: ?ReturnType = null,
        experimentalprivilegednesting: ?bool = null,
        insecurerootcapabilities: ?bool = null,
        expand: ?bool = null,
        noinit: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withExec", .{
            .args = args,
            .useentrypoint = options.useentrypoint,
            .stdin = options.stdin,
            .redirectstdin = options.redirectstdin,
            .redirectstdout = options.redirectstdout,
            .redirectstderr = options.redirectstderr,
            .expect = options.expect,
            .experimentalprivilegednesting = options.experimentalprivilegednesting,
            .insecurerootcapabilities = options.insecurerootcapabilities,
            .expand = options.expand,
            .noinit = options.noinit,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Expose a network port. Like EXPOSE in Dockerfile (but with healthcheck support)
    ///
    /// Exposed ports serve two purposes:
    ///
    /// - For health checks and introspection, when running services
    ///
    /// - For setting the EXPOSE OCI field when publishing the container
    pub fn withexposedport(self: *@This(), port: i32, options: struct {
        protocol: ?NetworkProtocol = null,
        description: ?[]const u8 = null,
        experimentalskiphealthcheck: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withExposedPort", .{
            .port = port,
            .protocol = options.protocol,
            .description = options.description,
            .experimentalskiphealthcheck = options.experimentalskiphealthcheck,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Return a container snapshot with a file added
    pub fn withfile(self: *@This(), path: []const u8, source: FileID, options: struct {
        permissions: ?i32 = null,
        owner: ?[]const u8 = null,
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withFile", .{
            .path = path,
            .source = source,
            .permissions = options.permissions,
            .owner = options.owner,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container plus the contents of the given files copied to the given path.
    pub fn withfiles(self: *@This(), path: []const u8, sources: []const FileID, options: struct {
        permissions: ?i32 = null,
        owner: ?[]const u8 = null,
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withFiles", .{
            .path = path,
            .sources = sources,
            .permissions = options.permissions,
            .owner = options.owner,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container plus the given label.
    pub fn withlabel(self: *@This(), name: []const u8, value: []const u8) !Container {
        const qb = try self.query_builder.selection("withLabel", .{
            .name = name,
            .value = value,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container plus a cache volume mounted at the given path.
    pub fn withmountedcache(self: *@This(), path: []const u8, cache: CacheVolumeID, options: struct {
        source: ?DirectoryID = null,
        sharing: ?CacheSharingMode = null,
        owner: ?[]const u8 = null,
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withMountedCache", .{
            .path = path,
            .cache = cache,
            .source = options.source,
            .sharing = options.sharing,
            .owner = options.owner,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container plus a directory mounted at the given path.
    pub fn withmounteddirectory(self: *@This(), path: []const u8, source: DirectoryID, options: struct {
        owner: ?[]const u8 = null,
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withMountedDirectory", .{
            .path = path,
            .source = source,
            .owner = options.owner,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container plus a file mounted at the given path.
    pub fn withmountedfile(self: *@This(), path: []const u8, source: FileID, options: struct {
        owner: ?[]const u8 = null,
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withMountedFile", .{
            .path = path,
            .source = source,
            .owner = options.owner,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container plus a secret mounted into a file at the given path.
    pub fn withmountedsecret(self: *@This(), path: []const u8, source: SecretID, options: struct {
        owner: ?[]const u8 = null,
        mode: ?i32 = null,
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withMountedSecret", .{
            .path = path,
            .source = source,
            .owner = options.owner,
            .mode = options.mode,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container plus a temporary directory mounted at the given path. Any writes will be ephemeral to a single withExec call; they will not be persisted to subsequent withExecs.
    pub fn withmountedtemp(self: *@This(), path: []const u8, options: struct {
        size: ?i32 = null,
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withMountedTemp", .{
            .path = path,
            .size = options.size,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Return a new container snapshot, with a file added to its filesystem with text content
    pub fn withnewfile(self: *@This(), path: []const u8, contents: []const u8, options: struct {
        permissions: ?i32 = null,
        owner: ?[]const u8 = null,
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withNewFile", .{
            .path = path,
            .contents = contents,
            .permissions = options.permissions,
            .owner = options.owner,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Attach credentials for future publishing to a registry. Use in combination with publish
    pub fn withregistryauth(self: *@This(), address: []const u8, username: []const u8, secret: SecretID) !Container {
        const qb = try self.query_builder.selection("withRegistryAuth", .{
            .address = address,
            .username = username,
            .secret = secret,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Change the container's root filesystem. The previous root filesystem will be lost.
    pub fn withrootfs(self: *@This(), directory: DirectoryID) !Container {
        const qb = try self.query_builder.selection("withRootfs", .{
            .directory = directory,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Set a new environment variable, using a secret value
    pub fn withsecretvariable(self: *@This(), name: []const u8, secret: SecretID) !Container {
        const qb = try self.query_builder.selection("withSecretVariable", .{
            .name = name,
            .secret = secret,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Establish a runtime dependency from a container to a network service.
    ///
    /// The service will be started automatically when needed and detached when it is no longer needed, executing the default command if none is set.
    ///
    /// The service will be reachable from the container via the provided hostname alias.
    ///
    /// The service dependency will also convey to any files or directories produced by the container.
    pub fn withservicebinding(self: *@This(), alias: []const u8, service: ServiceID) !Container {
        const qb = try self.query_builder.selection("withServiceBinding", .{
            .alias = alias,
            .service = service,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Return a snapshot with a symlink
    pub fn withsymlink(self: *@This(), target: []const u8, linkname: []const u8, options: struct {
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withSymlink", .{
            .target = target,
            .linkname = linkname,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container plus a socket forwarded to the given Unix socket path.
    pub fn withunixsocket(self: *@This(), path: []const u8, source: SocketID, options: struct {
        owner: ?[]const u8 = null,
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withUnixSocket", .{
            .path = path,
            .source = source,
            .owner = options.owner,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container with a different command user.
    pub fn withuser(self: *@This(), name: []const u8) !Container {
        const qb = try self.query_builder.selection("withUser", .{
            .name = name,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Change the container's working directory. Like WORKDIR in Dockerfile.
    pub fn withworkdir(self: *@This(), path: []const u8, options: struct {
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withWorkdir", .{
            .path = path,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container minus the given OCI annotation.
    pub fn withoutannotation(self: *@This(), name: []const u8) !Container {
        const qb = try self.query_builder.selection("withoutAnnotation", .{
            .name = name,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Remove the container's default arguments.
    pub fn withoutdefaultargs(self: *@This()) !Container {
        const qb = try self.query_builder.selection("withoutDefaultArgs", .{});
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Return a new container snapshot, with a directory removed from its filesystem
    pub fn withoutdirectory(self: *@This(), path: []const u8, options: struct {
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withoutDirectory", .{
            .path = path,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container without a configured docker healtcheck command.
    pub fn withoutdockerhealthcheck(self: *@This()) !Container {
        const qb = try self.query_builder.selection("withoutDockerHealthcheck", .{});
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Reset the container's OCI entrypoint.
    pub fn withoutentrypoint(self: *@This(), options: struct {
        keepdefaultargs: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withoutEntrypoint", .{
            .keepdefaultargs = options.keepdefaultargs,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container minus the given environment variable.
    pub fn withoutenvvariable(self: *@This(), name: []const u8) !Container {
        const qb = try self.query_builder.selection("withoutEnvVariable", .{
            .name = name,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Unexpose a previously exposed port.
    pub fn withoutexposedport(self: *@This(), port: i32, options: struct {
        protocol: ?NetworkProtocol = null,
    }) !Container {
        const qb = try self.query_builder.selection("withoutExposedPort", .{
            .port = port,
            .protocol = options.protocol,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container with the file at the given path removed.
    pub fn withoutfile(self: *@This(), path: []const u8, options: struct {
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withoutFile", .{
            .path = path,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Return a new container spanshot with specified files removed
    pub fn withoutfiles(self: *@This(), paths: []const []const u8, options: struct {
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withoutFiles", .{
            .paths = paths,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container minus the given environment label.
    pub fn withoutlabel(self: *@This(), name: []const u8) !Container {
        const qb = try self.query_builder.selection("withoutLabel", .{
            .name = name,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container after unmounting everything at the given path.
    pub fn withoutmount(self: *@This(), path: []const u8, options: struct {
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withoutMount", .{
            .path = path,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container without the registry authentication of a given address.
    pub fn withoutregistryauth(self: *@This(), address: []const u8) !Container {
        const qb = try self.query_builder.selection("withoutRegistryAuth", .{
            .address = address,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container minus the given environment variable containing the secret.
    pub fn withoutsecretvariable(self: *@This(), name: []const u8) !Container {
        const qb = try self.query_builder.selection("withoutSecretVariable", .{
            .name = name,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container with a previously added Unix socket removed.
    pub fn withoutunixsocket(self: *@This(), path: []const u8, options: struct {
        expand: ?bool = null,
    }) !Container {
        const qb = try self.query_builder.selection("withoutUnixSocket", .{
            .path = path,
            .expand = options.expand,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this container with an unset command user.
    ///
    /// Should default to root.
    pub fn withoutuser(self: *@This()) !Container {
        const qb = try self.query_builder.selection("withoutUser", .{});
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Unset the container's working directory.
    ///
    /// Should default to "/".
    pub fn withoutworkdir(self: *@This()) !Container {
        const qb = try self.query_builder.selection("withoutWorkdir", .{});
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves the working directory for all commands.
    pub fn workdir(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("workdir", .{});
        return try self.client.execute(qb, []const u8);
    }
};

/// Reflective module API provided to functions at runtime.
pub const CurrentModule = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The dependencies of the module.
    pub fn dependencies(self: *@This()) ![]const Module {
        const qb = try self.query_builder.selection("dependencies", .{});
        const ids = try self.client.execute(qb, []const ModuleID);
        var results = try self.client.allocator.alloc(Module, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadModuleFromID(id);
        }
        return results;
    }

    /// The generated files and directories made on top of the module source's context directory.
    pub fn generatedcontextdirectory(self: *@This()) !Directory {
        const qb = try self.query_builder.selection("generatedContextDirectory", .{});
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Return all generators defined by the module
    pub fn generators(self: *@This(), options: struct {
        include: ?[]const []const u8 = null,
    }) !GeneratorGroup {
        const qb = try self.query_builder.selection("generators", .{
            .include = options.include,
        });
        return GeneratorGroup{ .query_builder = qb, .client = self.client };
    }

    /// A unique identifier for this CurrentModule.
    pub fn id(self: *@This()) !CurrentModuleID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, CurrentModuleID);
    }

    /// The name of the module being executed in
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The directory containing the module's source code loaded into the engine (plus any generated code that may have been created).
    pub fn source(self: *@This()) !Directory {
        const qb = try self.query_builder.selection("source", .{});
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Load a directory from the module's scratch working directory, including any changes that may have been made to it during module function execution.
    pub fn workdir(self: *@This(), path: []const u8, options: struct {
        exclude: ?[]const []const u8 = null,
        include: ?[]const []const u8 = null,
        gitignore: ?bool = null,
    }) !Directory {
        const qb = try self.query_builder.selection("workdir", .{
            .path = path,
            .exclude = options.exclude,
            .include = options.include,
            .gitignore = options.gitignore,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Load a file from the module's scratch working directory, including any changes that may have been made to it during module function execution.Load a file from the module's scratch working directory, including any changes that may have been made to it during module function execution.
    pub fn workdirfile(self: *@This(), path: []const u8) !File {
        const qb = try self.query_builder.selection("workdirFile", .{
            .path = path,
        });
        return File{ .query_builder = qb, .client = self.client };
    }
};

///
pub const DiffStat = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Number of added lines for this path.
    pub fn addedlines(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("addedLines", .{});
        return try self.client.execute(qb, i32);
    }

    /// A unique identifier for this DiffStat.
    pub fn id(self: *@This()) !DiffStatID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, DiffStatID);
    }

    /// Type of change.
    pub fn kind(self: *@This()) !DiffStatKind {
        const qb = try self.query_builder.selection("kind", .{});
        const s = try self.client.execute(qb, []const u8);
        return try DiffStatKind.fromString(s);
    }

    /// Previous path of the file, set only for renames.
    pub fn oldpath(self: *@This()) !?[]const u8 {
        const qb = try self.query_builder.selection("oldPath", .{});
        return try self.client.execute(qb, ?[]const u8);
    }

    /// Path of the changed file or directory.
    pub fn path(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("path", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Number of removed lines for this path.
    pub fn removedlines(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("removedLines", .{});
        return try self.client.execute(qb, i32);
    }
};

/// A directory.
pub const Directory = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Converts this directory to a local git repository
    pub fn asgit(self: *@This()) !GitRepository {
        const qb = try self.query_builder.selection("asGit", .{});
        return GitRepository{ .query_builder = qb, .client = self.client };
    }

    /// Load the directory as a Dagger module source
    pub fn asmodule(self: *@This(), options: struct {
        sourcerootpath: ?[]const u8 = null,
    }) !Module {
        const qb = try self.query_builder.selection("asModule", .{
            .sourcerootpath = options.sourcerootpath,
        });
        return Module{ .query_builder = qb, .client = self.client };
    }

    /// Load the directory as a Dagger module source
    pub fn asmodulesource(self: *@This(), options: struct {
        sourcerootpath: ?[]const u8 = null,
    }) !ModuleSource {
        const qb = try self.query_builder.selection("asModuleSource", .{
            .sourcerootpath = options.sourcerootpath,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Return the difference between this directory and another directory, typically an older snapshot.
    ///
    /// The difference is encoded as a changeset, which also tracks removed files, and can be applied to other directories.
    pub fn changes(self: *@This(), from: DirectoryID) !Changeset {
        const qb = try self.query_builder.selection("changes", .{
            .from = from,
        });
        return Changeset{ .query_builder = qb, .client = self.client };
    }

    /// Change the owner of the directory contents recursively.
    pub fn chown(self: *@This(), path: []const u8, owner: []const u8) !Directory {
        const qb = try self.query_builder.selection("chown", .{
            .path = path,
            .owner = owner,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Return the difference between this directory and an another directory. The difference is encoded as a directory.
    pub fn diff(self: *@This(), other: DirectoryID) !Directory {
        const qb = try self.query_builder.selection("diff", .{
            .other = other,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Return the directory's digest. The format of the digest is not guaranteed to be stable between releases of Dagger. It is guaranteed to be stable between invocations of the same Dagger engine.
    pub fn digest(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("digest", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Retrieves a directory at the given path.
    pub fn directory(self: *@This(), path: []const u8) !Directory {
        const qb = try self.query_builder.selection("directory", .{
            .path = path,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Use Dockerfile compatibility to build a container from this directory. Only use this function for Dockerfile compatibility. Otherwise use the native Container type directly, it is feature-complete and supports all Dockerfile features.
    pub fn dockerbuild(self: *@This(), options: struct {
        dockerfile: ?[]const u8 = null,
        platform: ?Platform = null,
        buildargs: ?[]const BuildArg = null,
        target: ?[]const u8 = null,
        secrets: ?[]const SecretID = null,
        noinit: ?bool = null,
        ssh: ?SocketID = null,
    }) !Container {
        const qb = try self.query_builder.selection("dockerBuild", .{
            .dockerfile = options.dockerfile,
            .platform = options.platform,
            .buildargs = options.buildargs,
            .target = options.target,
            .secrets = options.secrets,
            .noinit = options.noinit,
            .ssh = options.ssh,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Returns a list of files and directories at the given path.
    pub fn entries(self: *@This(), options: struct {
        path: ?[]const u8 = null,
    }) ![]const []const u8 {
        const qb = try self.query_builder.selection("entries", .{
            .path = options.path,
        });
        return try self.client.execute(qb, []const []const u8);
    }

    /// check if a file or directory exists
    pub fn exists(self: *@This(), path: []const u8, options: struct {
        expectedtype: ?ExistsType = null,
        donotfollowsymlinks: ?bool = null,
    }) !bool {
        const qb = try self.query_builder.selection("exists", .{
            .path = path,
            .expectedtype = options.expectedtype,
            .donotfollowsymlinks = options.donotfollowsymlinks,
        });
        return try self.client.execute(qb, bool);
    }

    /// Writes the contents of the directory to a path on the host.
    pub fn @"export"(self: *@This(), path: []const u8, options: struct {
        wipe: ?bool = null,
    }) ![]const u8 {
        const qb = try self.query_builder.selection("export", .{
            .path = path,
            .wipe = options.wipe,
        });
        return try self.client.execute(qb, []const u8);
    }

    /// Retrieve a file at the given path.
    pub fn file(self: *@This(), path: []const u8) !File {
        const qb = try self.query_builder.selection("file", .{
            .path = path,
        });
        return File{ .query_builder = qb, .client = self.client };
    }

    /// Return a snapshot with some paths included or excluded
    pub fn filter(self: *@This(), options: struct {
        exclude: ?[]const []const u8 = null,
        include: ?[]const []const u8 = null,
        gitignore: ?bool = null,
    }) !Directory {
        const qb = try self.query_builder.selection("filter", .{
            .exclude = options.exclude,
            .include = options.include,
            .gitignore = options.gitignore,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Search up the directory tree for a file or directory, and return its path. If no match, return null
    pub fn findup(self: *@This(), name: []const u8, start: []const u8) !?[]const u8 {
        const qb = try self.query_builder.selection("findUp", .{
            .name = name,
            .start = start,
        });
        return try self.client.execute(qb, ?[]const u8);
    }

    /// Returns a list of files and directories that matche the given pattern.
    pub fn glob(self: *@This(), pattern: []const u8) ![]const []const u8 {
        const qb = try self.query_builder.selection("glob", .{
            .pattern = pattern,
        });
        return try self.client.execute(qb, []const []const u8);
    }

    /// A unique identifier for this Directory.
    pub fn id(self: *@This()) !DirectoryID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, DirectoryID);
    }

    /// Returns the name of the directory.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Searches for content matching the given regular expression or literal string.
    ///
    /// Uses Rust regex syntax; escape literal ., [, ], {, }, | with backslashes.
    pub fn search(self: *@This(), pattern: []const u8, options: struct {
        paths: ?[]const []const u8 = null,
        globs: ?[]const []const u8 = null,
        literal: ?bool = null,
        multiline: ?bool = null,
        dotall: ?bool = null,
        insensitive: ?bool = null,
        skipignored: ?bool = null,
        skiphidden: ?bool = null,
        filesonly: ?bool = null,
        limit: ?i32 = null,
    }) ![]const SearchResult {
        const qb = try self.query_builder.selection("search", .{
            .pattern = pattern,
            .paths = options.paths,
            .globs = options.globs,
            .literal = options.literal,
            .multiline = options.multiline,
            .dotall = options.dotall,
            .insensitive = options.insensitive,
            .skipignored = options.skipignored,
            .skiphidden = options.skiphidden,
            .filesonly = options.filesonly,
            .limit = options.limit,
        });
        const ids = try self.client.execute(qb, []const SearchResultID);
        var results = try self.client.allocator.alloc(SearchResult, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadSearchResultFromID(id);
        }
        return results;
    }

    /// Return file status
    pub fn stat(self: *@This(), path: []const u8, options: struct {
        donotfollowsymlinks: ?bool = null,
    }) !?Stat {
        const qb = try self.query_builder.selection("stat", .{
            .path = path,
            .donotfollowsymlinks = options.donotfollowsymlinks,
        });
        return Stat{ .query_builder = qb, .client = self.client };
    }

    /// Force evaluation in the engine.
    pub fn sync(self: *@This()) !DirectoryID {
        const qb = try self.query_builder.selection("sync", .{});
        const id = try self.client.execute(qb, DirectoryID);
        return try self.client.loadDirectoryFromID(id);
    }

    /// Opens an interactive terminal in new container with this directory mounted inside.
    pub fn terminal(self: *@This(), options: struct {
        container: ?ContainerID = null,
        cmd: ?[]const []const u8 = null,
        experimentalprivilegednesting: ?bool = null,
        insecurerootcapabilities: ?bool = null,
    }) !Directory {
        const qb = try self.query_builder.selection("terminal", .{
            .container = options.container,
            .cmd = options.cmd,
            .experimentalprivilegednesting = options.experimentalprivilegednesting,
            .insecurerootcapabilities = options.insecurerootcapabilities,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Return a directory with changes from another directory applied to it.
    pub fn withchanges(self: *@This(), changes: ChangesetID) !Directory {
        const qb = try self.query_builder.selection("withChanges", .{
            .changes = changes,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Return a snapshot with a directory added
    pub fn withdirectory(self: *@This(), path: []const u8, source: DirectoryID, options: struct {
        exclude: ?[]const []const u8 = null,
        include: ?[]const []const u8 = null,
        gitignore: ?bool = null,
        owner: ?[]const u8 = null,
    }) !Directory {
        const qb = try self.query_builder.selection("withDirectory", .{
            .path = path,
            .source = source,
            .exclude = options.exclude,
            .include = options.include,
            .gitignore = options.gitignore,
            .owner = options.owner,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Raise an error.
    pub fn witherror(self: *@This(), err: []const u8) !Directory {
        const qb = try self.query_builder.selection("withError", .{
            .err = err,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this directory plus the contents of the given file copied to the given path.
    pub fn withfile(self: *@This(), path: []const u8, source: FileID, options: struct {
        permissions: ?i32 = null,
        owner: ?[]const u8 = null,
    }) !Directory {
        const qb = try self.query_builder.selection("withFile", .{
            .path = path,
            .source = source,
            .permissions = options.permissions,
            .owner = options.owner,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this directory plus the contents of the given files copied to the given path.
    pub fn withfiles(self: *@This(), path: []const u8, sources: []const FileID, options: struct {
        permissions: ?i32 = null,
    }) !Directory {
        const qb = try self.query_builder.selection("withFiles", .{
            .path = path,
            .sources = sources,
            .permissions = options.permissions,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this directory plus a new directory created at the given path.
    pub fn withnewdirectory(self: *@This(), path: []const u8, options: struct {
        permissions: ?i32 = null,
    }) !Directory {
        const qb = try self.query_builder.selection("withNewDirectory", .{
            .path = path,
            .permissions = options.permissions,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Return a snapshot with a new file added
    pub fn withnewfile(self: *@This(), path: []const u8, contents: []const u8, options: struct {
        permissions: ?i32 = null,
    }) !Directory {
        const qb = try self.query_builder.selection("withNewFile", .{
            .path = path,
            .contents = contents,
            .permissions = options.permissions,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this directory with the given Git-compatible patch applied.
    pub fn withpatch(self: *@This(), patch: []const u8) !Directory {
        const qb = try self.query_builder.selection("withPatch", .{
            .patch = patch,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this directory with the given Git-compatible patch file applied.
    pub fn withpatchfile(self: *@This(), patch: FileID) !Directory {
        const qb = try self.query_builder.selection("withPatchFile", .{
            .patch = patch,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Return a snapshot with a symlink
    pub fn withsymlink(self: *@This(), target: []const u8, linkname: []const u8) !Directory {
        const qb = try self.query_builder.selection("withSymlink", .{
            .target = target,
            .linkname = linkname,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this directory with all file/dir timestamps set to the given time.
    pub fn withtimestamps(self: *@This(), timestamp: i32) !Directory {
        const qb = try self.query_builder.selection("withTimestamps", .{
            .timestamp = timestamp,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Return a snapshot with a subdirectory removed
    pub fn withoutdirectory(self: *@This(), path: []const u8) !Directory {
        const qb = try self.query_builder.selection("withoutDirectory", .{
            .path = path,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Return a snapshot with a file removed
    pub fn withoutfile(self: *@This(), path: []const u8) !Directory {
        const qb = try self.query_builder.selection("withoutFile", .{
            .path = path,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Return a snapshot with files removed
    pub fn withoutfiles(self: *@This(), paths: []const []const u8) !Directory {
        const qb = try self.query_builder.selection("withoutFiles", .{
            .paths = paths,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }
};

/// The Dagger engine configuration and state
pub const Engine = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The list of connected client IDs
    pub fn clients(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("clients", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// A unique identifier for this Engine.
    pub fn id(self: *@This()) !EngineID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, EngineID);
    }

    /// The local (on-disk) cache for the Dagger engine
    pub fn localcache(self: *@This()) !EngineCache {
        const qb = try self.query_builder.selection("localCache", .{});
        return EngineCache{ .query_builder = qb, .client = self.client };
    }

    /// The name of the engine instance.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }
};

/// A cache storage for the Dagger engine
pub const EngineCache = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The current set of entries in the cache
    pub fn entryset(self: *@This(), options: struct {
        key: ?[]const u8 = null,
    }) !EngineCacheEntrySet {
        const qb = try self.query_builder.selection("entrySet", .{
            .key = options.key,
        });
        return EngineCacheEntrySet{ .query_builder = qb, .client = self.client };
    }

    /// A unique identifier for this EngineCache.
    pub fn id(self: *@This()) !EngineCacheID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, EngineCacheID);
    }

    /// The maximum bytes to keep in the cache without pruning.
    pub fn maxusedspace(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("maxUsedSpace", .{});
        return try self.client.execute(qb, i32);
    }

    /// The target amount of free disk space the garbage collector will attempt to leave.
    pub fn minfreespace(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("minFreeSpace", .{});
        return try self.client.execute(qb, i32);
    }

    /// Prune the cache of releaseable entries
    pub fn prune(self: *@This(), options: struct {
        usedefaultpolicy: ?bool = null,
        maxusedspace: ?[]const u8 = null,
        reservedspace: ?[]const u8 = null,
        minfreespace: ?[]const u8 = null,
        targetspace: ?[]const u8 = null,
    }) !?void {
        const qb = try self.query_builder.selection("prune", .{
            .usedefaultpolicy = options.usedefaultpolicy,
            .maxusedspace = options.maxusedspace,
            .reservedspace = options.reservedspace,
            .minfreespace = options.minfreespace,
            .targetspace = options.targetspace,
        });
        _ = try self.client.execute(qb, void);
    }

    /// The minimum amount of disk space this policy is guaranteed to retain.
    pub fn reservedspace(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("reservedSpace", .{});
        return try self.client.execute(qb, i32);
    }

    /// The target number of bytes to keep when pruning.
    pub fn targetspace(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("targetSpace", .{});
        return try self.client.execute(qb, i32);
    }
};

/// An individual cache entry in a cache entry set
pub const EngineCacheEntry = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Whether the cache entry is actively being used.
    pub fn activelyused(self: *@This()) !bool {
        const qb = try self.query_builder.selection("activelyUsed", .{});
        return try self.client.execute(qb, bool);
    }

    /// The time the cache entry was created, in Unix nanoseconds.
    pub fn createdtimeunixnano(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("createdTimeUnixNano", .{});
        return try self.client.execute(qb, i32);
    }

    /// The description of the cache entry.
    pub fn description(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("description", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The disk space used by the cache entry.
    pub fn diskspacebytes(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("diskSpaceBytes", .{});
        return try self.client.execute(qb, i32);
    }

    /// A unique identifier for this EngineCacheEntry.
    pub fn id(self: *@This()) !EngineCacheEntryID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, EngineCacheEntryID);
    }

    /// The most recent time the cache entry was used, in Unix nanoseconds.
    pub fn mostrecentusetimeunixnano(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("mostRecentUseTimeUnixNano", .{});
        return try self.client.execute(qb, i32);
    }

    /// The type of the cache record (e.g. regular, internal, frontend, source.local, source.git.checkout, exec.cachemount).
    pub fn recordtype(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("recordType", .{});
        return try self.client.execute(qb, []const u8);
    }
};

/// A set of cache entries returned by a query to a cache
pub const EngineCacheEntrySet = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The total disk space used by the cache entries in this set.
    pub fn diskspacebytes(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("diskSpaceBytes", .{});
        return try self.client.execute(qb, i32);
    }

    /// The list of individual cache entries in the set
    pub fn entries(self: *@This()) ![]const EngineCacheEntry {
        const qb = try self.query_builder.selection("entries", .{});
        const ids = try self.client.execute(qb, []const EngineCacheEntryID);
        var results = try self.client.allocator.alloc(EngineCacheEntry, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadEngineCacheEntryFromID(id);
        }
        return results;
    }

    /// The number of cache entries in this set.
    pub fn entrycount(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("entryCount", .{});
        return try self.client.execute(qb, i32);
    }

    /// A unique identifier for this EngineCacheEntrySet.
    pub fn id(self: *@This()) !EngineCacheEntrySetID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, EngineCacheEntrySetID);
    }
};

/// A definition of a custom enum defined in a Module.
pub const EnumTypeDef = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// A doc string for the enum, if any.
    pub fn description(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("description", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this EnumTypeDef.
    pub fn id(self: *@This()) !EnumTypeDefID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, EnumTypeDefID);
    }

    /// The members of the enum.
    pub fn members(self: *@This()) ![]const EnumValueTypeDef {
        const qb = try self.query_builder.selection("members", .{});
        const ids = try self.client.execute(qb, []const EnumValueTypeDefID);
        var results = try self.client.allocator.alloc(EnumValueTypeDef, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadEnumValueTypeDefFromID(id);
        }
        return results;
    }

    /// The name of the enum.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The location of this enum declaration.
    pub fn sourcemap(self: *@This()) !?SourceMap {
        const qb = try self.query_builder.selection("sourceMap", .{});
        return SourceMap{ .query_builder = qb, .client = self.client };
    }

    /// If this EnumTypeDef is associated with a Module, the name of the module. Unset otherwise.
    pub fn sourcemodulename(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("sourceModuleName", .{});
        return try self.client.execute(qb, []const u8);
    }

    ///
    /// @deprecated: use members instead
    pub fn values(self: *@This()) ![]const EnumValueTypeDef {
        const qb = try self.query_builder.selection("values", .{});
        const ids = try self.client.execute(qb, []const EnumValueTypeDefID);
        var results = try self.client.allocator.alloc(EnumValueTypeDef, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadEnumValueTypeDefFromID(id);
        }
        return results;
    }
};

/// A definition of a value in a custom enum defined in a Module.
pub const EnumValueTypeDef = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The reason this enum member is deprecated, if any.
    pub fn deprecated(self: *@This()) !?[]const u8 {
        const qb = try self.query_builder.selection("deprecated", .{});
        return try self.client.execute(qb, ?[]const u8);
    }

    /// A doc string for the enum member, if any.
    pub fn description(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("description", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this EnumValueTypeDef.
    pub fn id(self: *@This()) !EnumValueTypeDefID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, EnumValueTypeDefID);
    }

    /// The name of the enum member.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The location of this enum member declaration.
    pub fn sourcemap(self: *@This()) !?SourceMap {
        const qb = try self.query_builder.selection("sourceMap", .{});
        return SourceMap{ .query_builder = qb, .client = self.client };
    }

    /// The value of the enum member
    pub fn value(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("value", .{});
        return try self.client.execute(qb, []const u8);
    }
};

///
pub const Env = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Return the check with the given name from the installed modules. Must match exactly one check.
    pub fn check(self: *@This(), name: []const u8) !Check {
        const qb = try self.query_builder.selection("check", .{
            .name = name,
        });
        return Check{ .query_builder = qb, .client = self.client };
    }

    /// Return all checks defined by the installed modules
    pub fn checks(self: *@This(), options: struct {
        include: ?[]const []const u8 = null,
    }) !CheckGroup {
        const qb = try self.query_builder.selection("checks", .{
            .include = options.include,
        });
        return CheckGroup{ .query_builder = qb, .client = self.client };
    }

    /// A unique identifier for this Env.
    pub fn id(self: *@This()) !EnvID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, EnvID);
    }

    /// Retrieves an input binding by name
    pub fn input(self: *@This(), name: []const u8) !Binding {
        const qb = try self.query_builder.selection("input", .{
            .name = name,
        });
        return Binding{ .query_builder = qb, .client = self.client };
    }

    /// Returns all input bindings provided to the environment
    pub fn inputs(self: *@This()) ![]const Binding {
        const qb = try self.query_builder.selection("inputs", .{});
        const ids = try self.client.execute(qb, []const BindingID);
        var results = try self.client.allocator.alloc(Binding, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadBindingFromID(id);
        }
        return results;
    }

    /// Retrieves an output binding by name
    pub fn output(self: *@This(), name: []const u8) !Binding {
        const qb = try self.query_builder.selection("output", .{
            .name = name,
        });
        return Binding{ .query_builder = qb, .client = self.client };
    }

    /// Returns all declared output bindings for the environment
    pub fn outputs(self: *@This()) ![]const Binding {
        const qb = try self.query_builder.selection("outputs", .{});
        const ids = try self.client.execute(qb, []const BindingID);
        var results = try self.client.allocator.alloc(Binding, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadBindingFromID(id);
        }
        return results;
    }

    /// Return all services defined by the installed modules
    pub fn services(self: *@This(), options: struct {
        include: ?[]const []const u8 = null,
    }) !UpGroup {
        const qb = try self.query_builder.selection("services", .{
            .include = options.include,
        });
        return UpGroup{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type Address in the environment
    pub fn withaddressinput(self: *@This(), name: []const u8, value: AddressID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withAddressInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired Address output to be assigned in the environment
    pub fn withaddressoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withAddressOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type CacheVolume in the environment
    pub fn withcachevolumeinput(self: *@This(), name: []const u8, value: CacheVolumeID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withCacheVolumeInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired CacheVolume output to be assigned in the environment
    pub fn withcachevolumeoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withCacheVolumeOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type Changeset in the environment
    pub fn withchangesetinput(self: *@This(), name: []const u8, value: ChangesetID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withChangesetInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired Changeset output to be assigned in the environment
    pub fn withchangesetoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withChangesetOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type CheckGroup in the environment
    pub fn withcheckgroupinput(self: *@This(), name: []const u8, value: CheckGroupID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withCheckGroupInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired CheckGroup output to be assigned in the environment
    pub fn withcheckgroupoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withCheckGroupOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type Check in the environment
    pub fn withcheckinput(self: *@This(), name: []const u8, value: CheckID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withCheckInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired Check output to be assigned in the environment
    pub fn withcheckoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withCheckOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type Cloud in the environment
    pub fn withcloudinput(self: *@This(), name: []const u8, value: CloudID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withCloudInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired Cloud output to be assigned in the environment
    pub fn withcloudoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withCloudOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type Container in the environment
    pub fn withcontainerinput(self: *@This(), name: []const u8, value: ContainerID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withContainerInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired Container output to be assigned in the environment
    pub fn withcontaineroutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withContainerOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Installs the current module into the environment, exposing its functions to the model
    ///
    /// Contextual path arguments will be populated using the environment's workspace.
    pub fn withcurrentmodule(self: *@This()) !Env {
        const qb = try self.query_builder.selection("withCurrentModule", .{});
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type DiffStat in the environment
    pub fn withdiffstatinput(self: *@This(), name: []const u8, value: DiffStatID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withDiffStatInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired DiffStat output to be assigned in the environment
    pub fn withdiffstatoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withDiffStatOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type Directory in the environment
    pub fn withdirectoryinput(self: *@This(), name: []const u8, value: DirectoryID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withDirectoryInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired Directory output to be assigned in the environment
    pub fn withdirectoryoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withDirectoryOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type EnvFile in the environment
    pub fn withenvfileinput(self: *@This(), name: []const u8, value: EnvFileID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withEnvFileInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired EnvFile output to be assigned in the environment
    pub fn withenvfileoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withEnvFileOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type Env in the environment
    pub fn withenvinput(self: *@This(), name: []const u8, value: EnvID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withEnvInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired Env output to be assigned in the environment
    pub fn withenvoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withEnvOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type File in the environment
    pub fn withfileinput(self: *@This(), name: []const u8, value: FileID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withFileInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired File output to be assigned in the environment
    pub fn withfileoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withFileOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type GeneratorGroup in the environment
    pub fn withgeneratorgroupinput(self: *@This(), name: []const u8, value: GeneratorGroupID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withGeneratorGroupInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired GeneratorGroup output to be assigned in the environment
    pub fn withgeneratorgroupoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withGeneratorGroupOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type Generator in the environment
    pub fn withgeneratorinput(self: *@This(), name: []const u8, value: GeneratorID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withGeneratorInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired Generator output to be assigned in the environment
    pub fn withgeneratoroutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withGeneratorOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type GitRef in the environment
    pub fn withgitrefinput(self: *@This(), name: []const u8, value: GitRefID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withGitRefInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired GitRef output to be assigned in the environment
    pub fn withgitrefoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withGitRefOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type GitRepository in the environment
    pub fn withgitrepositoryinput(self: *@This(), name: []const u8, value: GitRepositoryID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withGitRepositoryInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired GitRepository output to be assigned in the environment
    pub fn withgitrepositoryoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withGitRepositoryOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type JSONValue in the environment
    pub fn withjsonvalueinput(self: *@This(), name: []const u8, value: JSONValueID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withJSONValueInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired JSONValue output to be assigned in the environment
    pub fn withjsonvalueoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withJSONValueOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Sets the main module for this environment (the project being worked on)
    ///
    /// Contextual path arguments will be populated using the environment's workspace.
    pub fn withmainmodule(self: *@This(), module: ModuleID) !Env {
        const qb = try self.query_builder.selection("withMainModule", .{
            .module = module,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Installs a module into the environment, exposing its functions to the model
    ///
    /// Contextual path arguments will be populated using the environment's workspace.
    /// @deprecated: Use withMainModule instead
    pub fn withmodule(self: *@This(), module: ModuleID) !Env {
        const qb = try self.query_builder.selection("withModule", .{
            .module = module,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type ModuleConfigClient in the environment
    pub fn withmoduleconfigclientinput(self: *@This(), name: []const u8, value: ModuleConfigClientID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withModuleConfigClientInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired ModuleConfigClient output to be assigned in the environment
    pub fn withmoduleconfigclientoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withModuleConfigClientOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type Module in the environment
    pub fn withmoduleinput(self: *@This(), name: []const u8, value: ModuleID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withModuleInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired Module output to be assigned in the environment
    pub fn withmoduleoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withModuleOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type ModuleSource in the environment
    pub fn withmodulesourceinput(self: *@This(), name: []const u8, value: ModuleSourceID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withModuleSourceInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired ModuleSource output to be assigned in the environment
    pub fn withmodulesourceoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withModuleSourceOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type SearchResult in the environment
    pub fn withsearchresultinput(self: *@This(), name: []const u8, value: SearchResultID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withSearchResultInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired SearchResult output to be assigned in the environment
    pub fn withsearchresultoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withSearchResultOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type SearchSubmatch in the environment
    pub fn withsearchsubmatchinput(self: *@This(), name: []const u8, value: SearchSubmatchID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withSearchSubmatchInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired SearchSubmatch output to be assigned in the environment
    pub fn withsearchsubmatchoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withSearchSubmatchOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type Secret in the environment
    pub fn withsecretinput(self: *@This(), name: []const u8, value: SecretID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withSecretInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired Secret output to be assigned in the environment
    pub fn withsecretoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withSecretOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type Service in the environment
    pub fn withserviceinput(self: *@This(), name: []const u8, value: ServiceID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withServiceInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired Service output to be assigned in the environment
    pub fn withserviceoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withServiceOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type Socket in the environment
    pub fn withsocketinput(self: *@This(), name: []const u8, value: SocketID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withSocketInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired Socket output to be assigned in the environment
    pub fn withsocketoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withSocketOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type Stat in the environment
    pub fn withstatinput(self: *@This(), name: []const u8, value: StatID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withStatInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired Stat output to be assigned in the environment
    pub fn withstatoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withStatOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Provides a string input binding to the environment
    pub fn withstringinput(self: *@This(), name: []const u8, value: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withStringInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declares a desired string output binding
    pub fn withstringoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withStringOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type UpGroup in the environment
    pub fn withupgroupinput(self: *@This(), name: []const u8, value: UpGroupID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withUpGroupInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired UpGroup output to be assigned in the environment
    pub fn withupgroupoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withUpGroupOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type Up in the environment
    pub fn withupinput(self: *@This(), name: []const u8, value: UpID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withUpInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired Up output to be assigned in the environment
    pub fn withupoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withUpOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Returns a new environment with the provided workspace
    pub fn withworkspace(self: *@This(), workspace: DirectoryID) !Env {
        const qb = try self.query_builder.selection("withWorkspace", .{
            .workspace = workspace,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Create or update a binding of type Workspace in the environment
    pub fn withworkspaceinput(self: *@This(), name: []const u8, value: WorkspaceID, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withWorkspaceInput", .{
            .name = name,
            .value = value,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Declare a desired Workspace output to be assigned in the environment
    pub fn withworkspaceoutput(self: *@This(), name: []const u8, description: []const u8) !Env {
        const qb = try self.query_builder.selection("withWorkspaceOutput", .{
            .name = name,
            .description = description,
        });
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Returns a new environment without any outputs
    pub fn withoutoutputs(self: *@This()) !Env {
        const qb = try self.query_builder.selection("withoutOutputs", .{});
        return Env{ .query_builder = qb, .client = self.client };
    }

    ///
    pub fn workspace(self: *@This()) !Directory {
        const qb = try self.query_builder.selection("workspace", .{});
        return Directory{ .query_builder = qb, .client = self.client };
    }
};

/// A collection of environment variables.
pub const EnvFile = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Return as a file
    pub fn asfile(self: *@This()) !File {
        const qb = try self.query_builder.selection("asFile", .{});
        return File{ .query_builder = qb, .client = self.client };
    }

    /// Check if a variable exists
    pub fn exists(self: *@This(), name: []const u8) !bool {
        const qb = try self.query_builder.selection("exists", .{
            .name = name,
        });
        return try self.client.execute(qb, bool);
    }

    /// Lookup a variable (last occurrence wins) and return its value, or an empty string
    pub fn get(self: *@This(), name: []const u8, options: struct {
        raw: ?bool = null,
    }) ![]const u8 {
        const qb = try self.query_builder.selection("get", .{
            .name = name,
            .raw = options.raw,
        });
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this EnvFile.
    pub fn id(self: *@This()) !EnvFileID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, EnvFileID);
    }

    /// Filters variables by prefix and removes the pref from keys. Variables without the prefix are excluded. For example, with the prefix "MY_APP_" and variables: MY_APP_TOKEN=topsecret MY_APP_NAME=hello FOO=bar the resulting environment will contain: TOKEN=topsecret NAME=hello
    pub fn namespace(self: *@This(), prefix: []const u8) !EnvFile {
        const qb = try self.query_builder.selection("namespace", .{
            .prefix = prefix,
        });
        return EnvFile{ .query_builder = qb, .client = self.client };
    }

    /// Return all variables
    pub fn variables(self: *@This(), options: struct {
        raw: ?bool = null,
    }) ![]const EnvVariable {
        const qb = try self.query_builder.selection("variables", .{
            .raw = options.raw,
        });
        const ids = try self.client.execute(qb, []const EnvVariableID);
        var results = try self.client.allocator.alloc(EnvVariable, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadEnvVariableFromID(id);
        }
        return results;
    }

    /// Add a variable
    pub fn withvariable(self: *@This(), name: []const u8, value: []const u8) !EnvFile {
        const qb = try self.query_builder.selection("withVariable", .{
            .name = name,
            .value = value,
        });
        return EnvFile{ .query_builder = qb, .client = self.client };
    }

    /// Remove all occurrences of the named variable
    pub fn withoutvariable(self: *@This(), name: []const u8) !EnvFile {
        const qb = try self.query_builder.selection("withoutVariable", .{
            .name = name,
        });
        return EnvFile{ .query_builder = qb, .client = self.client };
    }
};

/// An environment variable name and value.
pub const EnvVariable = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// A unique identifier for this EnvVariable.
    pub fn id(self: *@This()) !EnvVariableID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, EnvVariableID);
    }

    /// The environment variable name.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The environment variable value.
    pub fn value(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("value", .{});
        return try self.client.execute(qb, []const u8);
    }
};

///
pub const Error = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// A unique identifier for this Error.
    pub fn id(self: *@This()) !ErrorID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, ErrorID);
    }

    /// A description of the error.
    pub fn message(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("message", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The extensions of the error.
    pub fn values(self: *@This()) ![]const ErrorValue {
        const qb = try self.query_builder.selection("values", .{});
        const ids = try self.client.execute(qb, []const ErrorValueID);
        var results = try self.client.allocator.alloc(ErrorValue, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadErrorValueFromID(id);
        }
        return results;
    }

    /// Add a value to the error.
    pub fn withvalue(self: *@This(), name: []const u8, value: JSON) !Error {
        const qb = try self.query_builder.selection("withValue", .{
            .name = name,
            .value = value,
        });
        return Error{ .query_builder = qb, .client = self.client };
    }
};

///
pub const ErrorValue = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// A unique identifier for this ErrorValue.
    pub fn id(self: *@This()) !ErrorValueID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, ErrorValueID);
    }

    /// The name of the value.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The value.
    pub fn value(self: *@This()) !JSON {
        const qb = try self.query_builder.selection("value", .{});
        return try self.client.execute(qb, JSON);
    }
};

/// A definition of a field on a custom object defined in a Module.
///
/// A field on an object has a static value, as opposed to a function on an object whose value is computed by invoking code (and can accept arguments).
pub const FieldTypeDef = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The reason this enum member is deprecated, if any.
    pub fn deprecated(self: *@This()) !?[]const u8 {
        const qb = try self.query_builder.selection("deprecated", .{});
        return try self.client.execute(qb, ?[]const u8);
    }

    /// A doc string for the field, if any.
    pub fn description(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("description", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this FieldTypeDef.
    pub fn id(self: *@This()) !FieldTypeDefID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, FieldTypeDefID);
    }

    /// The name of the field in lowerCamelCase format.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The location of this field declaration.
    pub fn sourcemap(self: *@This()) !?SourceMap {
        const qb = try self.query_builder.selection("sourceMap", .{});
        return SourceMap{ .query_builder = qb, .client = self.client };
    }

    /// The type of the field.
    pub fn typedef(self: *@This()) !TypeDef {
        const qb = try self.query_builder.selection("typeDef", .{});
        return TypeDef{ .query_builder = qb, .client = self.client };
    }
};

/// A file.
pub const File = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Parse as an env file
    pub fn asenvfile(self: *@This(), options: struct {
        expand: ?bool = null,
    }) !EnvFile {
        const qb = try self.query_builder.selection("asEnvFile", .{
            .expand = options.expand,
        });
        return EnvFile{ .query_builder = qb, .client = self.client };
    }

    /// Parse the file contents as JSON.
    pub fn asjson(self: *@This()) !JSONValue {
        const qb = try self.query_builder.selection("asJSON", .{});
        return JSONValue{ .query_builder = qb, .client = self.client };
    }

    /// Change the owner of the file recursively.
    pub fn chown(self: *@This(), owner: []const u8) !File {
        const qb = try self.query_builder.selection("chown", .{
            .owner = owner,
        });
        return File{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves the contents of the file.
    pub fn contents(self: *@This(), options: struct {
        offsetlines: ?i32 = null,
        limitlines: ?i32 = null,
    }) ![]const u8 {
        const qb = try self.query_builder.selection("contents", .{
            .offsetlines = options.offsetlines,
            .limitlines = options.limitlines,
        });
        return try self.client.execute(qb, []const u8);
    }

    /// Return the file's digest. The format of the digest is not guaranteed to be stable between releases of Dagger. It is guaranteed to be stable between invocations of the same Dagger engine.
    pub fn digest(self: *@This(), options: struct {
        excludemetadata: ?bool = null,
    }) ![]const u8 {
        const qb = try self.query_builder.selection("digest", .{
            .excludemetadata = options.excludemetadata,
        });
        return try self.client.execute(qb, []const u8);
    }

    /// Writes the file to a file path on the host.
    pub fn @"export"(self: *@This(), path: []const u8, options: struct {
        allowparentdirpath: ?bool = null,
    }) ![]const u8 {
        const qb = try self.query_builder.selection("export", .{
            .path = path,
            .allowparentdirpath = options.allowparentdirpath,
        });
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this File.
    pub fn id(self: *@This()) !FileID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, FileID);
    }

    /// Retrieves the name of the file.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Searches for content matching the given regular expression or literal string.
    ///
    /// Uses Rust regex syntax; escape literal ., [, ], {, }, | with backslashes.
    pub fn search(self: *@This(), pattern: []const u8, options: struct {
        literal: ?bool = null,
        multiline: ?bool = null,
        dotall: ?bool = null,
        insensitive: ?bool = null,
        skipignored: ?bool = null,
        skiphidden: ?bool = null,
        filesonly: ?bool = null,
        limit: ?i32 = null,
        paths: ?[]const []const u8 = null,
        globs: ?[]const []const u8 = null,
    }) ![]const SearchResult {
        const qb = try self.query_builder.selection("search", .{
            .pattern = pattern,
            .literal = options.literal,
            .multiline = options.multiline,
            .dotall = options.dotall,
            .insensitive = options.insensitive,
            .skipignored = options.skipignored,
            .skiphidden = options.skiphidden,
            .filesonly = options.filesonly,
            .limit = options.limit,
            .paths = options.paths,
            .globs = options.globs,
        });
        const ids = try self.client.execute(qb, []const SearchResultID);
        var results = try self.client.allocator.alloc(SearchResult, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadSearchResultFromID(id);
        }
        return results;
    }

    /// Retrieves the size of the file, in bytes.
    pub fn size(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("size", .{});
        return try self.client.execute(qb, i32);
    }

    /// Return file status
    pub fn stat(self: *@This()) !?Stat {
        const qb = try self.query_builder.selection("stat", .{});
        return Stat{ .query_builder = qb, .client = self.client };
    }

    /// Force evaluation in the engine.
    pub fn sync(self: *@This()) !FileID {
        const qb = try self.query_builder.selection("sync", .{});
        const id = try self.client.execute(qb, FileID);
        return try self.client.loadFileFromID(id);
    }

    /// Retrieves this file with its name set to the given name.
    pub fn withname(self: *@This(), name: []const u8) !File {
        const qb = try self.query_builder.selection("withName", .{
            .name = name,
        });
        return File{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves the file with content replaced with the given text.
    ///
    /// If 'all' is true, all occurrences of the pattern will be replaced.
    ///
    /// If 'firstAfter' is specified, only the first match starting at the specified line will be replaced.
    ///
    /// If neither are specified, and there are multiple matches for the pattern, this will error.
    ///
    /// If there are no matches for the pattern, this will error.
    pub fn withreplaced(self: *@This(), search: []const u8, replacement: []const u8, options: struct {
        all: ?bool = null,
        firstfrom: ?i32 = null,
    }) !File {
        const qb = try self.query_builder.selection("withReplaced", .{
            .search = search,
            .replacement = replacement,
            .all = options.all,
            .firstfrom = options.firstfrom,
        });
        return File{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves this file with its created/modified timestamps set to the given time.
    pub fn withtimestamps(self: *@This(), timestamp: i32) !File {
        const qb = try self.query_builder.selection("withTimestamps", .{
            .timestamp = timestamp,
        });
        return File{ .query_builder = qb, .client = self.client };
    }
};

/// Function represents a resolver provided by a Module.
///
/// A function always evaluates against a parent object and is given a set of named arguments.
pub const Function = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Arguments accepted by the function, if any.
    pub fn args(self: *@This()) ![]const FunctionArg {
        const qb = try self.query_builder.selection("args", .{});
        const ids = try self.client.execute(qb, []const FunctionArgID);
        var results = try self.client.allocator.alloc(FunctionArg, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadFunctionArgFromID(id);
        }
        return results;
    }

    /// The reason this function is deprecated, if any.
    pub fn deprecated(self: *@This()) !?[]const u8 {
        const qb = try self.query_builder.selection("deprecated", .{});
        return try self.client.execute(qb, ?[]const u8);
    }

    /// A doc string for the function, if any.
    pub fn description(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("description", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this Function.
    pub fn id(self: *@This()) !FunctionID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, FunctionID);
    }

    /// The name of the function.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The type returned by the function.
    pub fn returntype(self: *@This()) !TypeDef {
        const qb = try self.query_builder.selection("returnType", .{});
        return TypeDef{ .query_builder = qb, .client = self.client };
    }

    /// The location of this function declaration.
    pub fn sourcemap(self: *@This()) !?SourceMap {
        const qb = try self.query_builder.selection("sourceMap", .{});
        return SourceMap{ .query_builder = qb, .client = self.client };
    }

    /// If this function is provided by a module, the name of the module. Unset otherwise.
    pub fn sourcemodulename(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("sourceModuleName", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Returns the function with the provided argument
    pub fn witharg(self: *@This(), name: []const u8, typedef: TypeDefID, options: struct {
        description: ?[]const u8 = null,
        defaultvalue: ?JSON = null,
        defaultpath: ?[]const u8 = null,
        ignore: ?[]const []const u8 = null,
        sourcemap: ?SourceMapID = null,
        deprecated: ?[]const u8 = null,
        defaultaddress: ?[]const u8 = null,
    }) !Function {
        const qb = try self.query_builder.selection("withArg", .{
            .name = name,
            .typedef = typedef,
            .description = options.description,
            .defaultvalue = options.defaultvalue,
            .defaultpath = options.defaultpath,
            .ignore = options.ignore,
            .sourcemap = options.sourcemap,
            .deprecated = options.deprecated,
            .defaultaddress = options.defaultaddress,
        });
        return Function{ .query_builder = qb, .client = self.client };
    }

    /// Returns the function updated to use the provided cache policy.
    pub fn withcachepolicy(self: *@This(), policy: FunctionCachePolicy, options: struct {
        timetolive: ?[]const u8 = null,
    }) !Function {
        const qb = try self.query_builder.selection("withCachePolicy", .{
            .policy = policy,
            .timetolive = options.timetolive,
        });
        return Function{ .query_builder = qb, .client = self.client };
    }

    /// Returns the function with a flag indicating it's a check.
    pub fn withcheck(self: *@This()) !Function {
        const qb = try self.query_builder.selection("withCheck", .{});
        return Function{ .query_builder = qb, .client = self.client };
    }

    /// Returns the function with the provided deprecation reason.
    pub fn withdeprecated(self: *@This(), options: struct {
        reason: ?[]const u8 = null,
    }) !Function {
        const qb = try self.query_builder.selection("withDeprecated", .{
            .reason = options.reason,
        });
        return Function{ .query_builder = qb, .client = self.client };
    }

    /// Returns the function with the given doc string.
    pub fn withdescription(self: *@This(), description: []const u8) !Function {
        const qb = try self.query_builder.selection("withDescription", .{
            .description = description,
        });
        return Function{ .query_builder = qb, .client = self.client };
    }

    /// Returns the function with a flag indicating it's a generator.
    pub fn withgenerator(self: *@This()) !Function {
        const qb = try self.query_builder.selection("withGenerator", .{});
        return Function{ .query_builder = qb, .client = self.client };
    }

    /// Returns the function with the given source map.
    pub fn withsourcemap(self: *@This(), sourcemap: SourceMapID) !Function {
        const qb = try self.query_builder.selection("withSourceMap", .{
            .sourcemap = sourcemap,
        });
        return Function{ .query_builder = qb, .client = self.client };
    }

    /// Returns the function with a flag indicating it returns a service for dagger up.
    pub fn withup(self: *@This()) !Function {
        const qb = try self.query_builder.selection("withUp", .{});
        return Function{ .query_builder = qb, .client = self.client };
    }
};

/// An argument accepted by a function.
///
/// This is a specification for an argument at function definition time, not an argument passed at function call time.
pub const FunctionArg = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Only applies to arguments of type Container. If the argument is not set, load it from the given address (e.g. alpine:latest)
    pub fn defaultaddress(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("defaultAddress", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Only applies to arguments of type File or Directory. If the argument is not set, load it from the given path in the context directory
    pub fn defaultpath(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("defaultPath", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// A default value to use for this argument when not explicitly set by the caller, if any.
    pub fn defaultvalue(self: *@This()) !JSON {
        const qb = try self.query_builder.selection("defaultValue", .{});
        return try self.client.execute(qb, JSON);
    }

    /// The reason this function is deprecated, if any.
    pub fn deprecated(self: *@This()) !?[]const u8 {
        const qb = try self.query_builder.selection("deprecated", .{});
        return try self.client.execute(qb, ?[]const u8);
    }

    /// A doc string for the argument, if any.
    pub fn description(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("description", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this FunctionArg.
    pub fn id(self: *@This()) !FunctionArgID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, FunctionArgID);
    }

    /// Only applies to arguments of type Directory. The ignore patterns are applied to the input directory, and matching entries are filtered out, in a cache-efficient manner.
    pub fn ignore(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("ignore", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// The name of the argument in lowerCamelCase format.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The location of this arg declaration.
    pub fn sourcemap(self: *@This()) !?SourceMap {
        const qb = try self.query_builder.selection("sourceMap", .{});
        return SourceMap{ .query_builder = qb, .client = self.client };
    }

    /// The type of the argument.
    pub fn typedef(self: *@This()) !TypeDef {
        const qb = try self.query_builder.selection("typeDef", .{});
        return TypeDef{ .query_builder = qb, .client = self.client };
    }
};

/// An active function call.
pub const FunctionCall = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// A unique identifier for this FunctionCall.
    pub fn id(self: *@This()) !FunctionCallID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, FunctionCallID);
    }

    /// The argument values the function is being invoked with.
    pub fn inputargs(self: *@This()) ![]const FunctionCallArgValue {
        const qb = try self.query_builder.selection("inputArgs", .{});
        const ids = try self.client.execute(qb, []const FunctionCallArgValueID);
        var results = try self.client.allocator.alloc(FunctionCallArgValue, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadFunctionCallArgValueFromID(id);
        }
        return results;
    }

    /// The name of the function being called.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The value of the parent object of the function being called. If the function is top-level to the module, this is always an empty object.
    pub fn parent(self: *@This()) !JSON {
        const qb = try self.query_builder.selection("parent", .{});
        return try self.client.execute(qb, JSON);
    }

    /// The name of the parent object of the function being called. If the function is top-level to the module, this is the name of the module.
    pub fn parentname(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("parentName", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Return an error from the function.
    pub fn returnerror(self: *@This(), @"error": ErrorID) !?void {
        const qb = try self.query_builder.selection("returnError", .{
            .@"error" = @"error",
        });
        _ = try self.client.execute(qb, void);
    }

    /// Set the return value of the function call to the provided value.
    pub fn returnvalue(self: *@This(), value: JSON) !?void {
        const qb = try self.query_builder.selection("returnValue", .{
            .value = value,
        });
        _ = try self.client.execute(qb, void);
    }
};

/// A value passed as a named argument to a function call.
pub const FunctionCallArgValue = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// A unique identifier for this FunctionCallArgValue.
    pub fn id(self: *@This()) !FunctionCallArgValueID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, FunctionCallArgValueID);
    }

    /// The name of the argument.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The value of the argument represented as a JSON serialized string.
    pub fn value(self: *@This()) !JSON {
        const qb = try self.query_builder.selection("value", .{});
        return try self.client.execute(qb, JSON);
    }
};

/// The result of running an SDK's codegen.
pub const GeneratedCode = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The directory containing the generated code.
    pub fn code(self: *@This()) !Directory {
        const qb = try self.query_builder.selection("code", .{});
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// A unique identifier for this GeneratedCode.
    pub fn id(self: *@This()) !GeneratedCodeID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, GeneratedCodeID);
    }

    /// List of paths to mark generated in version control (i.e. .gitattributes).
    pub fn vcsgeneratedpaths(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("vcsGeneratedPaths", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// List of paths to ignore in version control (i.e. .gitignore).
    pub fn vcsignoredpaths(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("vcsIgnoredPaths", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// Set the list of paths to mark generated in version control.
    pub fn withvcsgeneratedpaths(self: *@This(), paths: []const []const u8) !GeneratedCode {
        const qb = try self.query_builder.selection("withVCSGeneratedPaths", .{
            .paths = paths,
        });
        return GeneratedCode{ .query_builder = qb, .client = self.client };
    }

    /// Set the list of paths to ignore in version control.
    pub fn withvcsignoredpaths(self: *@This(), paths: []const []const u8) !GeneratedCode {
        const qb = try self.query_builder.selection("withVCSIgnoredPaths", .{
            .paths = paths,
        });
        return GeneratedCode{ .query_builder = qb, .client = self.client };
    }
};

///
pub const Generator = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The generated changeset from the last run
    pub fn changes(self: *@This()) !Changeset {
        const qb = try self.query_builder.selection("changes", .{});
        return Changeset{ .query_builder = qb, .client = self.client };
    }

    /// Whether the generator complete
    pub fn completed(self: *@This()) !bool {
        const qb = try self.query_builder.selection("completed", .{});
        return try self.client.execute(qb, bool);
    }

    /// Return the description of the generator
    pub fn description(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("description", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this Generator.
    pub fn id(self: *@This()) !GeneratorID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, GeneratorID);
    }

    /// Whether changeset from the last generator run is empty or not
    pub fn isempty(self: *@This()) !bool {
        const qb = try self.query_builder.selection("isEmpty", .{});
        return try self.client.execute(qb, bool);
    }

    /// Return the fully qualified name of the generator
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The original module in which the generator has been defined
    pub fn originalmodule(self: *@This()) !Module {
        const qb = try self.query_builder.selection("originalModule", .{});
        return Module{ .query_builder = qb, .client = self.client };
    }

    /// The path of the generator within its module
    pub fn path(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("path", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// Execute the generator
    pub fn run(self: *@This()) !Generator {
        const qb = try self.query_builder.selection("run", .{});
        return Generator{ .query_builder = qb, .client = self.client };
    }
};

///
pub const GeneratorGroup = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The combined changes from the last run of the generators
    ///
    /// If any conflict occurs, for instance if the same file is modified by multiple generators, or if a file is both modified and deleted, an error is raised and the merge of the changesets will failed.
    ///
    /// Set 'continueOnConflicts' flag to force to merge the changes in a 'last write wins' strategy.
    pub fn changes(self: *@This(), options: struct {
        onconflict: ?ChangesetsMergeConflict = null,
    }) !Changeset {
        const qb = try self.query_builder.selection("changes", .{
            .onconflict = options.onconflict,
        });
        return Changeset{ .query_builder = qb, .client = self.client };
    }

    /// A unique identifier for this GeneratorGroup.
    pub fn id(self: *@This()) !GeneratorGroupID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, GeneratorGroupID);
    }

    /// Whether the generated changeset from the last run is empty or not
    pub fn isempty(self: *@This()) !bool {
        const qb = try self.query_builder.selection("isEmpty", .{});
        return try self.client.execute(qb, bool);
    }

    /// Return a list of individual generators and their details
    pub fn list(self: *@This()) ![]const Generator {
        const qb = try self.query_builder.selection("list", .{});
        const ids = try self.client.execute(qb, []const GeneratorID);
        var results = try self.client.allocator.alloc(Generator, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadGeneratorFromID(id);
        }
        return results;
    }

    /// Execute all selected generators
    pub fn run(self: *@This()) !GeneratorGroup {
        const qb = try self.query_builder.selection("run", .{});
        return GeneratorGroup{ .query_builder = qb, .client = self.client };
    }
};

/// A git ref (tag, branch, or commit).
pub const GitRef = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The resolved commit id at this ref.
    pub fn commit(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("commit", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Find the best common ancestor between this ref and another ref.
    pub fn commonancestor(self: *@This(), other: GitRefID) !GitRef {
        const qb = try self.query_builder.selection("commonAncestor", .{
            .other = other,
        });
        return GitRef{ .query_builder = qb, .client = self.client };
    }

    /// A unique identifier for this GitRef.
    pub fn id(self: *@This()) !GitRefID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, GitRefID);
    }

    /// The resolved ref name at this ref.
    pub fn ref(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("ref", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The filesystem tree at this ref.
    pub fn tree(self: *@This(), options: struct {
        discardgitdir: ?bool = null,
        depth: ?i32 = null,
        includetags: ?bool = null,
    }) !Directory {
        const qb = try self.query_builder.selection("tree", .{
            .discardgitdir = options.discardgitdir,
            .depth = options.depth,
            .includetags = options.includetags,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }
};

/// A git repository.
pub const GitRepository = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Returns details of a branch.
    pub fn branch(self: *@This(), name: []const u8) !GitRef {
        const qb = try self.query_builder.selection("branch", .{
            .name = name,
        });
        return GitRef{ .query_builder = qb, .client = self.client };
    }

    /// branches that match any of the given glob patterns.
    pub fn branches(self: *@This(), options: struct {
        patterns: ?[]const []const u8 = null,
    }) ![]const []const u8 {
        const qb = try self.query_builder.selection("branches", .{
            .patterns = options.patterns,
        });
        return try self.client.execute(qb, []const []const u8);
    }

    /// Returns details of a commit.
    pub fn commit(self: *@This(), id: []const u8) !GitRef {
        const qb = try self.query_builder.selection("commit", .{
            .id = id,
        });
        return GitRef{ .query_builder = qb, .client = self.client };
    }

    /// Returns details for HEAD.
    pub fn head(self: *@This()) !GitRef {
        const qb = try self.query_builder.selection("head", .{});
        return GitRef{ .query_builder = qb, .client = self.client };
    }

    /// A unique identifier for this GitRepository.
    pub fn id(self: *@This()) !GitRepositoryID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, GitRepositoryID);
    }

    /// Returns details for the latest semver tag.
    pub fn latestversion(self: *@This()) !GitRef {
        const qb = try self.query_builder.selection("latestVersion", .{});
        return GitRef{ .query_builder = qb, .client = self.client };
    }

    /// Returns details of a ref.
    pub fn ref(self: *@This(), name: []const u8) !GitRef {
        const qb = try self.query_builder.selection("ref", .{
            .name = name,
        });
        return GitRef{ .query_builder = qb, .client = self.client };
    }

    /// Returns details of a tag.
    pub fn tag(self: *@This(), name: []const u8) !GitRef {
        const qb = try self.query_builder.selection("tag", .{
            .name = name,
        });
        return GitRef{ .query_builder = qb, .client = self.client };
    }

    /// tags that match any of the given glob patterns.
    pub fn tags(self: *@This(), options: struct {
        patterns: ?[]const []const u8 = null,
    }) ![]const []const u8 {
        const qb = try self.query_builder.selection("tags", .{
            .patterns = options.patterns,
        });
        return try self.client.execute(qb, []const []const u8);
    }

    /// Returns the changeset of uncommitted changes in the git repository.
    pub fn uncommitted(self: *@This()) !Changeset {
        const qb = try self.query_builder.selection("uncommitted", .{});
        return Changeset{ .query_builder = qb, .client = self.client };
    }

    /// The URL of the git repository.
    pub fn url(self: *@This()) !?[]const u8 {
        const qb = try self.query_builder.selection("url", .{});
        return try self.client.execute(qb, ?[]const u8);
    }
};

/// Image healthcheck configuration.
pub const HealthcheckConfig = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Healthcheck command arguments.
    pub fn args(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("args", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// A unique identifier for this HealthcheckConfig.
    pub fn id(self: *@This()) !HealthcheckConfigID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, HealthcheckConfigID);
    }

    /// Interval between running healthcheck. Example:30s
    pub fn interval(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("interval", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The maximum number of consecutive failures before the container is marked as unhealthy. Example:3
    pub fn retries(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("retries", .{});
        return try self.client.execute(qb, i32);
    }

    /// Healthcheck command is a shell command.
    pub fn shell(self: *@This()) !bool {
        const qb = try self.query_builder.selection("shell", .{});
        return try self.client.execute(qb, bool);
    }

    /// StartInterval configures the duration between checks during the startup phase. Example:5s
    pub fn startinterval(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("startInterval", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// StartPeriod allows for failures during this initial startup period which do not count towards maximum number of retries. Example:0s
    pub fn startperiod(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("startPeriod", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Healthcheck timeout. Example:3s
    pub fn timeout(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("timeout", .{});
        return try self.client.execute(qb, []const u8);
    }
};

/// Information about the host environment.
pub const Host = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Accesses a container image on the host.
    pub fn containerimage(self: *@This(), name: []const u8) !Container {
        const qb = try self.query_builder.selection("containerImage", .{
            .name = name,
        });
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// Accesses a directory on the host.
    pub fn directory(self: *@This(), path: []const u8, options: struct {
        exclude: ?[]const []const u8 = null,
        include: ?[]const []const u8 = null,
        nocache: ?bool = null,
        gitignore: ?bool = null,
    }) !Directory {
        const qb = try self.query_builder.selection("directory", .{
            .path = path,
            .exclude = options.exclude,
            .include = options.include,
            .nocache = options.nocache,
            .gitignore = options.gitignore,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Accesses a file on the host.
    pub fn file(self: *@This(), path: []const u8, options: struct {
        nocache: ?bool = null,
    }) !File {
        const qb = try self.query_builder.selection("file", .{
            .path = path,
            .nocache = options.nocache,
        });
        return File{ .query_builder = qb, .client = self.client };
    }

    /// Search for a file or directory by walking up the tree from system workdir. Return its relative path. If no match, return null
    pub fn findup(self: *@This(), name: []const u8, options: struct {
        nocache: ?bool = null,
    }) !?[]const u8 {
        const qb = try self.query_builder.selection("findUp", .{
            .name = name,
            .nocache = options.nocache,
        });
        return try self.client.execute(qb, ?[]const u8);
    }

    /// A unique identifier for this Host.
    pub fn id(self: *@This()) !HostID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, HostID);
    }

    /// Creates a service that forwards traffic to a specified address via the host.
    pub fn service(self: *@This(), ports: []const PortForward, options: struct {
        host: ?[]const u8 = null,
    }) !Service {
        const qb = try self.query_builder.selection("service", .{
            .ports = ports,
            .host = options.host,
        });
        return Service{ .query_builder = qb, .client = self.client };
    }

    /// Creates a tunnel that forwards traffic from the host to a service.
    pub fn tunnel(self: *@This(), service: ServiceID, options: struct {
        native: ?bool = null,
        ports: ?[]const PortForward = null,
    }) !Service {
        const qb = try self.query_builder.selection("tunnel", .{
            .service = service,
            .native = options.native,
            .ports = options.ports,
        });
        return Service{ .query_builder = qb, .client = self.client };
    }

    /// Accesses a Unix socket on the host.
    pub fn unixsocket(self: *@This(), path: []const u8) !Socket {
        const qb = try self.query_builder.selection("unixSocket", .{
            .path = path,
        });
        return Socket{ .query_builder = qb, .client = self.client };
    }
};

/// A graphql input type, which is essentially just a group of named args.
/// This is currently only used to represent pre-existing usage of graphql input types
/// in the core API. It is not used by user modules and shouldn't ever be as user
/// module accept input objects via their id rather than graphql input types.
pub const InputTypeDef = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Static fields defined on this input object, if any.
    pub fn fields(self: *@This()) ![]const FieldTypeDef {
        const qb = try self.query_builder.selection("fields", .{});
        const ids = try self.client.execute(qb, []const FieldTypeDefID);
        var results = try self.client.allocator.alloc(FieldTypeDef, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadFieldTypeDefFromID(id);
        }
        return results;
    }

    /// A unique identifier for this InputTypeDef.
    pub fn id(self: *@This()) !InputTypeDefID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, InputTypeDefID);
    }

    /// The name of the input object.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }
};

/// A definition of a custom interface defined in a Module.
pub const InterfaceTypeDef = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The doc string for the interface, if any.
    pub fn description(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("description", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Functions defined on this interface, if any.
    pub fn functions(self: *@This()) ![]const Function {
        const qb = try self.query_builder.selection("functions", .{});
        const ids = try self.client.execute(qb, []const FunctionID);
        var results = try self.client.allocator.alloc(Function, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadFunctionFromID(id);
        }
        return results;
    }

    /// A unique identifier for this InterfaceTypeDef.
    pub fn id(self: *@This()) !InterfaceTypeDefID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, InterfaceTypeDefID);
    }

    /// The name of the interface.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The location of this interface declaration.
    pub fn sourcemap(self: *@This()) !?SourceMap {
        const qb = try self.query_builder.selection("sourceMap", .{});
        return SourceMap{ .query_builder = qb, .client = self.client };
    }

    /// If this InterfaceTypeDef is associated with a Module, the name of the module. Unset otherwise.
    pub fn sourcemodulename(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("sourceModuleName", .{});
        return try self.client.execute(qb, []const u8);
    }
};

///
pub const JSONValue = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Decode an array from json
    pub fn asarray(self: *@This()) ![]const JSONValue {
        const qb = try self.query_builder.selection("asArray", .{});
        const ids = try self.client.execute(qb, []const JSONValueID);
        var results = try self.client.allocator.alloc(JSONValue, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadJSONValueFromID(id);
        }
        return results;
    }

    /// Decode a boolean from json
    pub fn asboolean(self: *@This()) !bool {
        const qb = try self.query_builder.selection("asBoolean", .{});
        return try self.client.execute(qb, bool);
    }

    /// Decode an integer from json
    pub fn asinteger(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("asInteger", .{});
        return try self.client.execute(qb, i32);
    }

    /// Decode a string from json
    pub fn asstring(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("asString", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Return the value encoded as json
    pub fn contents(self: *@This(), options: struct {
        pretty: ?bool = null,
        indent: ?[]const u8 = null,
    }) !JSON {
        const qb = try self.query_builder.selection("contents", .{
            .pretty = options.pretty,
            .indent = options.indent,
        });
        return try self.client.execute(qb, JSON);
    }

    /// Lookup the field at the given path, and return its value.
    pub fn field(self: *@This(), path: []const []const u8) !JSONValue {
        const qb = try self.query_builder.selection("field", .{
            .path = path,
        });
        return JSONValue{ .query_builder = qb, .client = self.client };
    }

    /// List fields of the encoded object
    pub fn fields(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("fields", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// A unique identifier for this JSONValue.
    pub fn id(self: *@This()) !JSONValueID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, JSONValueID);
    }

    /// Encode a boolean to json
    pub fn newboolean(self: *@This(), value: bool) !JSONValue {
        const qb = try self.query_builder.selection("newBoolean", .{
            .value = value,
        });
        return JSONValue{ .query_builder = qb, .client = self.client };
    }

    /// Encode an integer to json
    pub fn newinteger(self: *@This(), value: i32) !JSONValue {
        const qb = try self.query_builder.selection("newInteger", .{
            .value = value,
        });
        return JSONValue{ .query_builder = qb, .client = self.client };
    }

    /// Encode a string to json
    pub fn newstring(self: *@This(), value: []const u8) !JSONValue {
        const qb = try self.query_builder.selection("newString", .{
            .value = value,
        });
        return JSONValue{ .query_builder = qb, .client = self.client };
    }

    /// Return a new json value, decoded from the given content
    pub fn withcontents(self: *@This(), contents: JSON) !JSONValue {
        const qb = try self.query_builder.selection("withContents", .{
            .contents = contents,
        });
        return JSONValue{ .query_builder = qb, .client = self.client };
    }

    /// Set a new field at the given path
    pub fn withfield(self: *@This(), path: []const []const u8, value: JSONValueID) !JSONValue {
        const qb = try self.query_builder.selection("withField", .{
            .path = path,
            .value = value,
        });
        return JSONValue{ .query_builder = qb, .client = self.client };
    }
};

///
pub const LLM = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// create a branch in the LLM's history
    pub fn attempt(self: *@This(), number: i32) !LLM {
        const qb = try self.query_builder.selection("attempt", .{
            .number = number,
        });
        return LLM{ .query_builder = qb, .client = self.client };
    }

    /// returns the type of the current state
    pub fn bindresult(self: *@This(), name: []const u8) !?Binding {
        const qb = try self.query_builder.selection("bindResult", .{
            .name = name,
        });
        return Binding{ .query_builder = qb, .client = self.client };
    }

    /// return the LLM's current environment
    pub fn env(self: *@This()) !Env {
        const qb = try self.query_builder.selection("env", .{});
        return Env{ .query_builder = qb, .client = self.client };
    }

    /// Indicates whether there are any queued prompts or tool results to send to the model
    pub fn hasprompt(self: *@This()) !bool {
        const qb = try self.query_builder.selection("hasPrompt", .{});
        return try self.client.execute(qb, bool);
    }

    /// return the llm message history
    pub fn history(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("history", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// return the raw llm message history as json
    pub fn historyjson(self: *@This()) !JSON {
        const qb = try self.query_builder.selection("historyJSON", .{});
        return try self.client.execute(qb, JSON);
    }

    /// A unique identifier for this LLM.
    pub fn id(self: *@This()) !LLMID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, LLMID);
    }

    /// return the last llm reply from the history
    pub fn lastreply(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("lastReply", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Submit the queued prompt, evaluate any tool calls, queue their results, and keep going until the model ends its turn
    pub fn loop(self: *@This()) !LLM {
        const qb = try self.query_builder.selection("loop", .{});
        return LLM{ .query_builder = qb, .client = self.client };
    }

    /// return the model used by the llm
    pub fn model(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("model", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// return the provider used by the llm
    pub fn provider(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("provider", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Submit the queued prompt or tool call results, evaluate any tool calls, and queue their results
    pub fn step(self: *@This()) !LLMID {
        const qb = try self.query_builder.selection("step", .{});
        const id = try self.client.execute(qb, LLMID);
        return try self.client.loadLLMFromID(id);
    }

    /// synchronize LLM state
    pub fn sync(self: *@This()) !LLMID {
        const qb = try self.query_builder.selection("sync", .{});
        const id = try self.client.execute(qb, LLMID);
        return try self.client.loadLLMFromID(id);
    }

    /// returns the token usage of the current state
    pub fn tokenusage(self: *@This()) !LLMTokenUsage {
        const qb = try self.query_builder.selection("tokenUsage", .{});
        return LLMTokenUsage{ .query_builder = qb, .client = self.client };
    }

    /// print documentation for available tools
    pub fn tools(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("tools", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Return a new LLM with the specified function no longer exposed as a tool
    pub fn withblockedfunction(self: *@This(), typename: []const u8, function: []const u8) !LLM {
        const qb = try self.query_builder.selection("withBlockedFunction", .{
            .typename = typename,
            .function = function,
        });
        return LLM{ .query_builder = qb, .client = self.client };
    }

    /// allow the LLM to interact with an environment via MCP
    pub fn withenv(self: *@This(), env: EnvID) !LLM {
        const qb = try self.query_builder.selection("withEnv", .{
            .env = env,
        });
        return LLM{ .query_builder = qb, .client = self.client };
    }

    /// Add an external MCP server to the LLM
    pub fn withmcpserver(self: *@This(), name: []const u8, service: ServiceID) !LLM {
        const qb = try self.query_builder.selection("withMCPServer", .{
            .name = name,
            .service = service,
        });
        return LLM{ .query_builder = qb, .client = self.client };
    }

    /// swap out the llm model
    pub fn withmodel(self: *@This(), model: []const u8) !LLM {
        const qb = try self.query_builder.selection("withModel", .{
            .model = model,
        });
        return LLM{ .query_builder = qb, .client = self.client };
    }

    /// append a prompt to the llm context
    pub fn withprompt(self: *@This(), prompt: []const u8) !LLM {
        const qb = try self.query_builder.selection("withPrompt", .{
            .prompt = prompt,
        });
        return LLM{ .query_builder = qb, .client = self.client };
    }

    /// append the contents of a file to the llm context
    pub fn withpromptfile(self: *@This(), file: FileID) !LLM {
        const qb = try self.query_builder.selection("withPromptFile", .{
            .file = file,
        });
        return LLM{ .query_builder = qb, .client = self.client };
    }

    /// Use a static set of tools for method calls, e.g. for MCP clients that do not support dynamic tool registration
    pub fn withstatictools(self: *@This()) !LLM {
        const qb = try self.query_builder.selection("withStaticTools", .{});
        return LLM{ .query_builder = qb, .client = self.client };
    }

    /// Add a system prompt to the LLM's environment
    pub fn withsystemprompt(self: *@This(), prompt: []const u8) !LLM {
        const qb = try self.query_builder.selection("withSystemPrompt", .{
            .prompt = prompt,
        });
        return LLM{ .query_builder = qb, .client = self.client };
    }

    /// Disable the default system prompt
    pub fn withoutdefaultsystemprompt(self: *@This()) !LLM {
        const qb = try self.query_builder.selection("withoutDefaultSystemPrompt", .{});
        return LLM{ .query_builder = qb, .client = self.client };
    }

    /// Clear the message history, leaving only the system prompts
    pub fn withoutmessagehistory(self: *@This()) !LLM {
        const qb = try self.query_builder.selection("withoutMessageHistory", .{});
        return LLM{ .query_builder = qb, .client = self.client };
    }

    /// Clear the system prompts, leaving only the default system prompt
    pub fn withoutsystemprompts(self: *@This()) !LLM {
        const qb = try self.query_builder.selection("withoutSystemPrompts", .{});
        return LLM{ .query_builder = qb, .client = self.client };
    }
};

///
pub const LLMTokenUsage = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    ///
    pub fn cachedtokenreads(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("cachedTokenReads", .{});
        return try self.client.execute(qb, i32);
    }

    ///
    pub fn cachedtokenwrites(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("cachedTokenWrites", .{});
        return try self.client.execute(qb, i32);
    }

    /// A unique identifier for this LLMTokenUsage.
    pub fn id(self: *@This()) !LLMTokenUsageID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, LLMTokenUsageID);
    }

    ///
    pub fn inputtokens(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("inputTokens", .{});
        return try self.client.execute(qb, i32);
    }

    ///
    pub fn outputtokens(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("outputTokens", .{});
        return try self.client.execute(qb, i32);
    }

    ///
    pub fn totaltokens(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("totalTokens", .{});
        return try self.client.execute(qb, i32);
    }
};

/// A simple key value object that represents a label.
pub const Label = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// A unique identifier for this Label.
    pub fn id(self: *@This()) !LabelID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, LabelID);
    }

    /// The label name.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The label value.
    pub fn value(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("value", .{});
        return try self.client.execute(qb, []const u8);
    }
};

/// A definition of a list type in a Module.
pub const ListTypeDef = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The type of the elements in the list.
    pub fn elementtypedef(self: *@This()) !TypeDef {
        const qb = try self.query_builder.selection("elementTypeDef", .{});
        return TypeDef{ .query_builder = qb, .client = self.client };
    }

    /// A unique identifier for this ListTypeDef.
    pub fn id(self: *@This()) !ListTypeDefID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, ListTypeDefID);
    }
};

/// A Dagger module.
pub const Module = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Return the check defined by the module with the given name. Must match to exactly one check.
    pub fn check(self: *@This(), name: []const u8) !Check {
        const qb = try self.query_builder.selection("check", .{
            .name = name,
        });
        return Check{ .query_builder = qb, .client = self.client };
    }

    /// Return all checks defined by the module
    pub fn checks(self: *@This(), options: struct {
        include: ?[]const []const u8 = null,
    }) !CheckGroup {
        const qb = try self.query_builder.selection("checks", .{
            .include = options.include,
        });
        return CheckGroup{ .query_builder = qb, .client = self.client };
    }

    /// The dependencies of the module.
    pub fn dependencies(self: *@This()) ![]const Module {
        const qb = try self.query_builder.selection("dependencies", .{});
        const ids = try self.client.execute(qb, []const ModuleID);
        var results = try self.client.allocator.alloc(Module, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadModuleFromID(id);
        }
        return results;
    }

    /// The doc string of the module, if any
    pub fn description(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("description", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Enumerations served by this module.
    pub fn enums(self: *@This()) ![]const TypeDef {
        const qb = try self.query_builder.selection("enums", .{});
        const ids = try self.client.execute(qb, []const TypeDefID);
        var results = try self.client.allocator.alloc(TypeDef, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadTypeDefFromID(id);
        }
        return results;
    }

    /// The generated files and directories made on top of the module source's context directory.
    pub fn generatedcontextdirectory(self: *@This()) !Directory {
        const qb = try self.query_builder.selection("generatedContextDirectory", .{});
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Return the generator defined by the module with the given name. Must match to exactly one generator.
    pub fn generator(self: *@This(), name: []const u8) !Generator {
        const qb = try self.query_builder.selection("generator", .{
            .name = name,
        });
        return Generator{ .query_builder = qb, .client = self.client };
    }

    /// Return all generators defined by the module
    pub fn generators(self: *@This(), options: struct {
        include: ?[]const []const u8 = null,
    }) !GeneratorGroup {
        const qb = try self.query_builder.selection("generators", .{
            .include = options.include,
        });
        return GeneratorGroup{ .query_builder = qb, .client = self.client };
    }

    /// A unique identifier for this Module.
    pub fn id(self: *@This()) !ModuleID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, ModuleID);
    }

    /// Interfaces served by this module.
    pub fn interfaces(self: *@This()) ![]const TypeDef {
        const qb = try self.query_builder.selection("interfaces", .{});
        const ids = try self.client.execute(qb, []const TypeDefID);
        var results = try self.client.allocator.alloc(TypeDef, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadTypeDefFromID(id);
        }
        return results;
    }

    /// The introspection schema JSON file for this module.
    ///
    /// This file represents the schema visible to the module's source code, including all core types and those from the dependencies.
    ///
    /// Note: this is in the context of a module, so some core types may be hidden.
    pub fn introspectionschemajson(self: *@This()) !File {
        const qb = try self.query_builder.selection("introspectionSchemaJSON", .{});
        return File{ .query_builder = qb, .client = self.client };
    }

    /// The name of the module
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Objects served by this module.
    pub fn objects(self: *@This()) ![]const TypeDef {
        const qb = try self.query_builder.selection("objects", .{});
        const ids = try self.client.execute(qb, []const TypeDefID);
        var results = try self.client.allocator.alloc(TypeDef, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadTypeDefFromID(id);
        }
        return results;
    }

    /// The container that runs the module's entrypoint. It will fail to execute if the module doesn't compile.
    pub fn runtime(self: *@This()) !?Container {
        const qb = try self.query_builder.selection("runtime", .{});
        return Container{ .query_builder = qb, .client = self.client };
    }

    /// The SDK config used by this module.
    pub fn sdk(self: *@This()) !?SDKConfig {
        const qb = try self.query_builder.selection("sdk", .{});
        return SDKConfig{ .query_builder = qb, .client = self.client };
    }

    /// Serve a module's API in the current session.
    ///
    /// Note: this can only be called once per session. In the future, it could return a stream or service to remove the side effect.
    pub fn serve(self: *@This(), options: struct {
        includedependencies: ?bool = null,
        entrypoint: ?bool = null,
    }) !?void {
        const qb = try self.query_builder.selection("serve", .{
            .includedependencies = options.includedependencies,
            .entrypoint = options.entrypoint,
        });
        _ = try self.client.execute(qb, void);
    }

    /// Return all services defined by the module
    pub fn services(self: *@This(), options: struct {
        include: ?[]const []const u8 = null,
    }) !UpGroup {
        const qb = try self.query_builder.selection("services", .{
            .include = options.include,
        });
        return UpGroup{ .query_builder = qb, .client = self.client };
    }

    /// The source for the module.
    pub fn source(self: *@This()) !?ModuleSource {
        const qb = try self.query_builder.selection("source", .{});
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Forces evaluation of the module, including any loading into the engine and associated validation.
    pub fn sync(self: *@This()) !ModuleID {
        const qb = try self.query_builder.selection("sync", .{});
        const id = try self.client.execute(qb, ModuleID);
        return try self.client.loadModuleFromID(id);
    }

    /// User-defined default values, loaded from local .env files.
    pub fn userdefaults(self: *@This()) !EnvFile {
        const qb = try self.query_builder.selection("userDefaults", .{});
        return EnvFile{ .query_builder = qb, .client = self.client };
    }

    /// Retrieves the module with the given description
    pub fn withdescription(self: *@This(), description: []const u8) !Module {
        const qb = try self.query_builder.selection("withDescription", .{
            .description = description,
        });
        return Module{ .query_builder = qb, .client = self.client };
    }

    /// This module plus the given Enum type and associated values
    pub fn withenum(self: *@This(), @"enum": TypeDefID) !Module {
        const qb = try self.query_builder.selection("withEnum", .{
            .@"enum" = @"enum",
        });
        return Module{ .query_builder = qb, .client = self.client };
    }

    /// This module plus the given Interface type and associated functions
    pub fn withinterface(self: *@This(), iface: TypeDefID) !Module {
        const qb = try self.query_builder.selection("withInterface", .{
            .iface = iface,
        });
        return Module{ .query_builder = qb, .client = self.client };
    }

    /// This module plus the given Object type and associated functions.
    pub fn withobject(self: *@This(), object: TypeDefID) !Module {
        const qb = try self.query_builder.selection("withObject", .{
            .object = object,
        });
        return Module{ .query_builder = qb, .client = self.client };
    }
};

/// The client generated for the module.
pub const ModuleConfigClient = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The directory the client is generated in.
    pub fn directory(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("directory", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The generator to use
    pub fn generator(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("generator", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this ModuleConfigClient.
    pub fn id(self: *@This()) !ModuleConfigClientID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, ModuleConfigClientID);
    }
};

/// The source needed to load and run a module, along with any metadata about the source such as versions/urls/etc.
pub const ModuleSource = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Load the source as a module. If this is a local source, the parent directory must have been provided during module source creation
    pub fn asmodule(self: *@This()) !Module {
        const qb = try self.query_builder.selection("asModule", .{});
        return Module{ .query_builder = qb, .client = self.client };
    }

    /// A human readable ref string representation of this module source.
    pub fn asstring(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("asString", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The blueprint referenced by the module source.
    pub fn blueprint(self: *@This()) !ModuleSource {
        const qb = try self.query_builder.selection("blueprint", .{});
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// The ref to clone the root of the git repo from. Only valid for git sources.
    pub fn cloneref(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("cloneRef", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The resolved commit of the git repo this source points to.
    pub fn commit(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("commit", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The clients generated for the module.
    pub fn configclients(self: *@This()) ![]const ModuleConfigClient {
        const qb = try self.query_builder.selection("configClients", .{});
        const ids = try self.client.execute(qb, []const ModuleConfigClientID);
        var results = try self.client.allocator.alloc(ModuleConfigClient, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadModuleConfigClientFromID(id);
        }
        return results;
    }

    /// Whether an existing dagger.json for the module was found.
    pub fn configexists(self: *@This()) !bool {
        const qb = try self.query_builder.selection("configExists", .{});
        return try self.client.execute(qb, bool);
    }

    /// The full directory loaded for the module source, including the source code as a subdirectory.
    pub fn contextdirectory(self: *@This()) !Directory {
        const qb = try self.query_builder.selection("contextDirectory", .{});
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// The dependencies of the module source.
    pub fn dependencies(self: *@This()) ![]const ModuleSource {
        const qb = try self.query_builder.selection("dependencies", .{});
        const ids = try self.client.execute(qb, []const ModuleSourceID);
        var results = try self.client.allocator.alloc(ModuleSource, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadModuleSourceFromID(id);
        }
        return results;
    }

    /// A content-hash of the module source. Module sources with the same digest will output the same generated context and convert into the same module instance.
    pub fn digest(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("digest", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The directory containing the module configuration and source code (source code may be in a subdir).
    pub fn directory(self: *@This(), path: []const u8) !Directory {
        const qb = try self.query_builder.selection("directory", .{
            .path = path,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// The engine version of the module.
    pub fn engineversion(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("engineVersion", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The generated files and directories made on top of the module source's context directory, returned as a Changeset.
    pub fn generatedcontextchangeset(self: *@This()) !Changeset {
        const qb = try self.query_builder.selection("generatedContextChangeset", .{});
        return Changeset{ .query_builder = qb, .client = self.client };
    }

    /// The generated files and directories made on top of the module source's context directory.
    pub fn generatedcontextdirectory(self: *@This()) !Directory {
        const qb = try self.query_builder.selection("generatedContextDirectory", .{});
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// The URL to access the web view of the repository (e.g., GitHub, GitLab, Bitbucket).
    pub fn htmlrepourl(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("htmlRepoURL", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The URL to the source's git repo in a web browser. Only valid for git sources.
    pub fn htmlurl(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("htmlURL", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this ModuleSource.
    pub fn id(self: *@This()) !ModuleSourceID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, ModuleSourceID);
    }

    /// The introspection schema JSON file for this module source.
    ///
    /// This file represents the schema visible to the module's source code, including all core types and those from the dependencies.
    ///
    /// Note: this is in the context of a module, so some core types may be hidden.
    pub fn introspectionschemajson(self: *@This()) !File {
        const qb = try self.query_builder.selection("introspectionSchemaJSON", .{});
        return File{ .query_builder = qb, .client = self.client };
    }

    /// The kind of module source (currently local, git or dir).
    pub fn kind(self: *@This()) !ModuleSourceKind {
        const qb = try self.query_builder.selection("kind", .{});
        const s = try self.client.execute(qb, []const u8);
        return try ModuleSourceKind.fromString(s);
    }

    /// The full absolute path to the context directory on the caller's host filesystem that this module source is loaded from. Only valid for local module sources.
    pub fn localcontextdirectorypath(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("localContextDirectoryPath", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The name of the module, including any setting via the withName API.
    pub fn modulename(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("moduleName", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The original name of the module as read from the module's dagger.json (or set for the first time with the withName API).
    pub fn moduleoriginalname(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("moduleOriginalName", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The original subpath used when instantiating this module source, relative to the context directory.
    pub fn originalsubpath(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("originalSubpath", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The pinned version of this module source.
    pub fn pin(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("pin", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The import path corresponding to the root of the git repo this source points to. Only valid for git sources.
    pub fn reporootpath(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("repoRootPath", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The SDK configuration of the module.
    pub fn sdk(self: *@This()) !?SDKConfig {
        const qb = try self.query_builder.selection("sdk", .{});
        return SDKConfig{ .query_builder = qb, .client = self.client };
    }

    /// The path, relative to the context directory, that contains the module's dagger.json.
    pub fn sourcerootsubpath(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("sourceRootSubpath", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The path to the directory containing the module's source code, relative to the context directory.
    pub fn sourcesubpath(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("sourceSubpath", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Forces evaluation of the module source, including any loading into the engine and associated validation.
    pub fn sync(self: *@This()) !ModuleSourceID {
        const qb = try self.query_builder.selection("sync", .{});
        const id = try self.client.execute(qb, ModuleSourceID);
        return try self.client.loadModuleSourceFromID(id);
    }

    /// The toolchains referenced by the module source.
    pub fn toolchains(self: *@This()) ![]const ModuleSource {
        const qb = try self.query_builder.selection("toolchains", .{});
        const ids = try self.client.execute(qb, []const ModuleSourceID);
        var results = try self.client.allocator.alloc(ModuleSource, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadModuleSourceFromID(id);
        }
        return results;
    }

    /// User-defined defaults read from local .env files
    pub fn userdefaults(self: *@This()) !EnvFile {
        const qb = try self.query_builder.selection("userDefaults", .{});
        return EnvFile{ .query_builder = qb, .client = self.client };
    }

    /// The specified version of the git repo this source points to.
    pub fn version(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("version", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Set a blueprint for the module source.
    pub fn withblueprint(self: *@This(), blueprint: ModuleSourceID) !ModuleSource {
        const qb = try self.query_builder.selection("withBlueprint", .{
            .blueprint = blueprint,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Update the module source with a new client to generate.
    pub fn withclient(self: *@This(), generator: []const u8, outputdir: []const u8) !ModuleSource {
        const qb = try self.query_builder.selection("withClient", .{
            .generator = generator,
            .outputdir = outputdir,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Append the provided dependencies to the module source's dependency list.
    pub fn withdependencies(self: *@This(), dependencies: []const ModuleSourceID) !ModuleSource {
        const qb = try self.query_builder.selection("withDependencies", .{
            .dependencies = dependencies,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Upgrade the engine version of the module to the given value.
    pub fn withengineversion(self: *@This(), version: []const u8) !ModuleSource {
        const qb = try self.query_builder.selection("withEngineVersion", .{
            .version = version,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Enable the experimental features for the module source.
    pub fn withexperimentalfeatures(self: *@This(), features: []const ModuleSourceExperimentalFeature) !ModuleSource {
        const qb = try self.query_builder.selection("withExperimentalFeatures", .{
            .features = features,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Update the module source with additional include patterns for files+directories from its context that are required for building it
    pub fn withincludes(self: *@This(), patterns: []const []const u8) !ModuleSource {
        const qb = try self.query_builder.selection("withIncludes", .{
            .patterns = patterns,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Update the module source with a new name.
    pub fn withname(self: *@This(), name: []const u8) !ModuleSource {
        const qb = try self.query_builder.selection("withName", .{
            .name = name,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Update the module source with a new SDK.
    pub fn withsdk(self: *@This(), source: []const u8) !ModuleSource {
        const qb = try self.query_builder.selection("withSDK", .{
            .source = source,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Update the module source with a new source subpath.
    pub fn withsourcesubpath(self: *@This(), path: []const u8) !ModuleSource {
        const qb = try self.query_builder.selection("withSourceSubpath", .{
            .path = path,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Add toolchains to the module source.
    pub fn withtoolchains(self: *@This(), toolchains: []const ModuleSourceID) !ModuleSource {
        const qb = try self.query_builder.selection("withToolchains", .{
            .toolchains = toolchains,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Update the blueprint module to the latest version.
    pub fn withupdateblueprint(self: *@This()) !ModuleSource {
        const qb = try self.query_builder.selection("withUpdateBlueprint", .{});
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Update one or more module dependencies.
    pub fn withupdatedependencies(self: *@This(), dependencies: []const []const u8) !ModuleSource {
        const qb = try self.query_builder.selection("withUpdateDependencies", .{
            .dependencies = dependencies,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Update one or more toolchains.
    pub fn withupdatetoolchains(self: *@This(), toolchains: []const []const u8) !ModuleSource {
        const qb = try self.query_builder.selection("withUpdateToolchains", .{
            .toolchains = toolchains,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Update one or more clients.
    pub fn withupdatedclients(self: *@This(), clients: []const []const u8) !ModuleSource {
        const qb = try self.query_builder.selection("withUpdatedClients", .{
            .clients = clients,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Remove the current blueprint from the module source.
    pub fn withoutblueprint(self: *@This()) !ModuleSource {
        const qb = try self.query_builder.selection("withoutBlueprint", .{});
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Remove a client from the module source.
    pub fn withoutclient(self: *@This(), path: []const u8) !ModuleSource {
        const qb = try self.query_builder.selection("withoutClient", .{
            .path = path,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Remove the provided dependencies from the module source's dependency list.
    pub fn withoutdependencies(self: *@This(), dependencies: []const []const u8) !ModuleSource {
        const qb = try self.query_builder.selection("withoutDependencies", .{
            .dependencies = dependencies,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Disable experimental features for the module source.
    pub fn withoutexperimentalfeatures(self: *@This(), features: []const ModuleSourceExperimentalFeature) !ModuleSource {
        const qb = try self.query_builder.selection("withoutExperimentalFeatures", .{
            .features = features,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }

    /// Remove the provided toolchains from the module source.
    pub fn withouttoolchains(self: *@This(), toolchains: []const []const u8) !ModuleSource {
        const qb = try self.query_builder.selection("withoutToolchains", .{
            .toolchains = toolchains,
        });
        return ModuleSource{ .query_builder = qb, .client = self.client };
    }
};

/// A definition of a custom object defined in a Module.
pub const ObjectTypeDef = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The function used to construct new instances of this object, if any
    pub fn constructor(self: *@This()) !?Function {
        const qb = try self.query_builder.selection("constructor", .{});
        return Function{ .query_builder = qb, .client = self.client };
    }

    /// The reason this enum member is deprecated, if any.
    pub fn deprecated(self: *@This()) !?[]const u8 {
        const qb = try self.query_builder.selection("deprecated", .{});
        return try self.client.execute(qb, ?[]const u8);
    }

    /// The doc string for the object, if any.
    pub fn description(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("description", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Static fields defined on this object, if any.
    pub fn fields(self: *@This()) ![]const FieldTypeDef {
        const qb = try self.query_builder.selection("fields", .{});
        const ids = try self.client.execute(qb, []const FieldTypeDefID);
        var results = try self.client.allocator.alloc(FieldTypeDef, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadFieldTypeDefFromID(id);
        }
        return results;
    }

    /// Functions defined on this object, if any.
    pub fn functions(self: *@This()) ![]const Function {
        const qb = try self.query_builder.selection("functions", .{});
        const ids = try self.client.execute(qb, []const FunctionID);
        var results = try self.client.allocator.alloc(Function, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadFunctionFromID(id);
        }
        return results;
    }

    /// A unique identifier for this ObjectTypeDef.
    pub fn id(self: *@This()) !ObjectTypeDefID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, ObjectTypeDefID);
    }

    /// The name of the object.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The location of this object declaration.
    pub fn sourcemap(self: *@This()) !?SourceMap {
        const qb = try self.query_builder.selection("sourceMap", .{});
        return SourceMap{ .query_builder = qb, .client = self.client };
    }

    /// If this ObjectTypeDef is associated with a Module, the name of the module. Unset otherwise.
    pub fn sourcemodulename(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("sourceModuleName", .{});
        return try self.client.execute(qb, []const u8);
    }
};

/// A port exposed by a container.
pub const Port = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The port description.
    pub fn description(self: *@This()) !?[]const u8 {
        const qb = try self.query_builder.selection("description", .{});
        return try self.client.execute(qb, ?[]const u8);
    }

    /// Skip the health check when run as a service.
    pub fn experimentalskiphealthcheck(self: *@This()) !bool {
        const qb = try self.query_builder.selection("experimentalSkipHealthcheck", .{});
        return try self.client.execute(qb, bool);
    }

    /// A unique identifier for this Port.
    pub fn id(self: *@This()) !PortID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, PortID);
    }

    /// The port number.
    pub fn port(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("port", .{});
        return try self.client.execute(qb, i32);
    }

    /// The transport layer protocol.
    pub fn protocol(self: *@This()) !NetworkProtocol {
        const qb = try self.query_builder.selection("protocol", .{});
        const s = try self.client.execute(qb, []const u8);
        return try NetworkProtocol.fromString(s);
    }
};

/// The SDK config of the module.
pub const SDKConfig = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Whether to start the SDK runtime in debug mode with an interactive terminal.
    pub fn debug(self: *@This()) !bool {
        const qb = try self.query_builder.selection("debug", .{});
        return try self.client.execute(qb, bool);
    }

    /// A unique identifier for this SDKConfig.
    pub fn id(self: *@This()) !SDKConfigID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, SDKConfigID);
    }

    /// Source of the SDK. Either a name of a builtin SDK or a module source ref string pointing to the SDK's implementation.
    pub fn source(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("source", .{});
        return try self.client.execute(qb, []const u8);
    }
};

/// A definition of a custom scalar defined in a Module.
pub const ScalarTypeDef = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// A doc string for the scalar, if any.
    pub fn description(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("description", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this ScalarTypeDef.
    pub fn id(self: *@This()) !ScalarTypeDefID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, ScalarTypeDefID);
    }

    /// The name of the scalar.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// If this ScalarTypeDef is associated with a Module, the name of the module. Unset otherwise.
    pub fn sourcemodulename(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("sourceModuleName", .{});
        return try self.client.execute(qb, []const u8);
    }
};

///
pub const SearchResult = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The byte offset of this line within the file.
    pub fn absoluteoffset(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("absoluteOffset", .{});
        return try self.client.execute(qb, i32);
    }

    /// The path to the file that matched.
    pub fn filepath(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("filePath", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this SearchResult.
    pub fn id(self: *@This()) !SearchResultID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, SearchResultID);
    }

    /// The first line that matched.
    pub fn linenumber(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("lineNumber", .{});
        return try self.client.execute(qb, i32);
    }

    /// The line content that matched.
    pub fn matchedlines(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("matchedLines", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Sub-match positions and content within the matched lines.
    pub fn submatches(self: *@This()) ![]const SearchSubmatch {
        const qb = try self.query_builder.selection("submatches", .{});
        const ids = try self.client.execute(qb, []const SearchSubmatchID);
        var results = try self.client.allocator.alloc(SearchSubmatch, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadSearchSubmatchFromID(id);
        }
        return results;
    }
};

///
pub const SearchSubmatch = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The match's end offset within the matched lines.
    pub fn end(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("end", .{});
        return try self.client.execute(qb, i32);
    }

    /// A unique identifier for this SearchSubmatch.
    pub fn id(self: *@This()) !SearchSubmatchID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, SearchSubmatchID);
    }

    /// The match's start offset within the matched lines.
    pub fn start(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("start", .{});
        return try self.client.execute(qb, i32);
    }

    /// The matched text.
    pub fn text(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("text", .{});
        return try self.client.execute(qb, []const u8);
    }
};

/// A reference to a secret value, which can be handled more safely than the value itself.
pub const Secret = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// A unique identifier for this Secret.
    pub fn id(self: *@This()) !SecretID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, SecretID);
    }

    /// The name of this secret.
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The value of this secret.
    pub fn plaintext(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("plaintext", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The URI of this secret.
    pub fn uri(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("uri", .{});
        return try self.client.execute(qb, []const u8);
    }
};

/// A content-addressed service providing TCP connectivity.
pub const Service = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Retrieves an endpoint that clients can use to reach this container.
    ///
    /// If no port is specified, the first exposed port is used. If none exist an error is returned.
    ///
    /// If a scheme is specified, a URL is returned. Otherwise, a host:port pair is returned.
    pub fn endpoint(self: *@This(), options: struct {
        port: ?i32 = null,
        scheme: ?[]const u8 = null,
    }) ![]const u8 {
        const qb = try self.query_builder.selection("endpoint", .{
            .port = options.port,
            .scheme = options.scheme,
        });
        return try self.client.execute(qb, []const u8);
    }

    /// Retrieves a hostname which can be used by clients to reach this container.
    pub fn hostname(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("hostname", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this Service.
    pub fn id(self: *@This()) !ServiceID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, ServiceID);
    }

    /// Retrieves the list of ports provided by the service.
    pub fn ports(self: *@This()) ![]const Port {
        const qb = try self.query_builder.selection("ports", .{});
        const ids = try self.client.execute(qb, []const PortID);
        var results = try self.client.allocator.alloc(Port, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadPortFromID(id);
        }
        return results;
    }

    /// Start the service and wait for its health checks to succeed.
    ///
    /// Services bound to a Container do not need to be manually started.
    pub fn start(self: *@This()) !ServiceID {
        const qb = try self.query_builder.selection("start", .{});
        const id = try self.client.execute(qb, ServiceID);
        return try self.client.loadServiceFromID(id);
    }

    /// Stop the service.
    pub fn stop(self: *@This(), options: struct {
        kill: ?bool = null,
    }) !ServiceID {
        const qb = try self.query_builder.selection("stop", .{
            .kill = options.kill,
        });
        const id = try self.client.execute(qb, ServiceID);
        return try self.client.loadServiceFromID(id);
    }

    /// Forces evaluation of the pipeline in the engine.
    pub fn sync(self: *@This()) !ServiceID {
        const qb = try self.query_builder.selection("sync", .{});
        const id = try self.client.execute(qb, ServiceID);
        return try self.client.loadServiceFromID(id);
    }

    ///
    pub fn terminal(self: *@This(), options: struct {
        cmd: ?[]const []const u8 = null,
    }) !Service {
        const qb = try self.query_builder.selection("terminal", .{
            .cmd = options.cmd,
        });
        return Service{ .query_builder = qb, .client = self.client };
    }

    /// Creates a tunnel that forwards traffic from the caller's network to this service.
    pub fn up(self: *@This(), options: struct {
        ports: ?[]const PortForward = null,
        random: ?bool = null,
    }) !?void {
        const qb = try self.query_builder.selection("up", .{
            .ports = options.ports,
            .random = options.random,
        });
        _ = try self.client.execute(qb, void);
    }

    /// Configures a hostname which can be used by clients within the session to reach this container.
    pub fn withhostname(self: *@This(), hostname: []const u8) !Service {
        const qb = try self.query_builder.selection("withHostname", .{
            .hostname = hostname,
        });
        return Service{ .query_builder = qb, .client = self.client };
    }
};

/// A Unix or TCP/IP socket that can be mounted into a container.
pub const Socket = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// A unique identifier for this Socket.
    pub fn id(self: *@This()) !SocketID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, SocketID);
    }
};

/// Source location information.
pub const SourceMap = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The column number within the line.
    pub fn column(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("column", .{});
        return try self.client.execute(qb, i32);
    }

    /// The filename from the module source.
    pub fn filename(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("filename", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this SourceMap.
    pub fn id(self: *@This()) !SourceMapID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, SourceMapID);
    }

    /// The line number within the filename.
    pub fn line(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("line", .{});
        return try self.client.execute(qb, i32);
    }

    /// The module dependency this was declared in.
    pub fn module(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("module", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The URL to the file, if any. This can be used to link to the source map in the browser.
    pub fn url(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("url", .{});
        return try self.client.execute(qb, []const u8);
    }
};

/// A file or directory status object.
pub const Stat = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// file type
    pub fn filetype(self: *@This()) !?FileType {
        const qb = try self.query_builder.selection("fileType", .{});
        const s = try self.client.execute(qb, []const u8);
        return try FileType.fromString(s);
    }

    /// A unique identifier for this Stat.
    pub fn id(self: *@This()) !StatID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, StatID);
    }

    /// file name
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// permission bits
    pub fn permissions(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("permissions", .{});
        return try self.client.execute(qb, i32);
    }

    /// file size
    pub fn size(self: *@This()) !i32 {
        const qb = try self.query_builder.selection("size", .{});
        return try self.client.execute(qb, i32);
    }
};

/// An interactive terminal that clients can connect to.
pub const Terminal = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// A unique identifier for this Terminal.
    pub fn id(self: *@This()) !TerminalID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, TerminalID);
    }

    /// Forces evaluation of the pipeline in the engine.
    ///
    /// It doesn't run the default command if no exec has been set.
    pub fn sync(self: *@This()) !TerminalID {
        const qb = try self.query_builder.selection("sync", .{});
        const id = try self.client.execute(qb, TerminalID);
        return try self.client.loadTerminalFromID(id);
    }
};

/// A definition of a parameter or return type in a Module.
pub const TypeDef = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// If kind is ENUM, the enum-specific type definition. If kind is not ENUM, this will be null.
    pub fn asenum(self: *@This()) !?EnumTypeDef {
        const qb = try self.query_builder.selection("asEnum", .{});
        return EnumTypeDef{ .query_builder = qb, .client = self.client };
    }

    /// If kind is INPUT, the input-specific type definition. If kind is not INPUT, this will be null.
    pub fn asinput(self: *@This()) !?InputTypeDef {
        const qb = try self.query_builder.selection("asInput", .{});
        return InputTypeDef{ .query_builder = qb, .client = self.client };
    }

    /// If kind is INTERFACE, the interface-specific type definition. If kind is not INTERFACE, this will be null.
    pub fn asinterface(self: *@This()) !?InterfaceTypeDef {
        const qb = try self.query_builder.selection("asInterface", .{});
        return InterfaceTypeDef{ .query_builder = qb, .client = self.client };
    }

    /// If kind is LIST, the list-specific type definition. If kind is not LIST, this will be null.
    pub fn aslist(self: *@This()) !?ListTypeDef {
        const qb = try self.query_builder.selection("asList", .{});
        return ListTypeDef{ .query_builder = qb, .client = self.client };
    }

    /// If kind is OBJECT, the object-specific type definition. If kind is not OBJECT, this will be null.
    pub fn asobject(self: *@This()) !?ObjectTypeDef {
        const qb = try self.query_builder.selection("asObject", .{});
        return ObjectTypeDef{ .query_builder = qb, .client = self.client };
    }

    /// If kind is SCALAR, the scalar-specific type definition. If kind is not SCALAR, this will be null.
    pub fn asscalar(self: *@This()) !?ScalarTypeDef {
        const qb = try self.query_builder.selection("asScalar", .{});
        return ScalarTypeDef{ .query_builder = qb, .client = self.client };
    }

    /// A unique identifier for this TypeDef.
    pub fn id(self: *@This()) !TypeDefID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, TypeDefID);
    }

    /// The kind of type this is (e.g. primitive, list, object).
    pub fn kind(self: *@This()) !TypeDefKind {
        const qb = try self.query_builder.selection("kind", .{});
        const s = try self.client.execute(qb, []const u8);
        return try TypeDefKind.fromString(s);
    }

    /// Whether this type can be set to null. Defaults to false.
    pub fn optional(self: *@This()) !bool {
        const qb = try self.query_builder.selection("optional", .{});
        return try self.client.execute(qb, bool);
    }

    /// Adds a function for constructing a new instance of an Object TypeDef, failing if the type is not an object.
    pub fn withconstructor(self: *@This(), function: FunctionID) !TypeDef {
        const qb = try self.query_builder.selection("withConstructor", .{
            .function = function,
        });
        return TypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Returns a TypeDef of kind Enum with the provided name.
    ///
    /// Note that an enum's values may be omitted if the intent is only to refer to an enum. This is how functions are able to return their own, or any other circular reference.
    pub fn withenum(self: *@This(), name: []const u8, options: struct {
        description: ?[]const u8 = null,
        sourcemap: ?SourceMapID = null,
    }) !TypeDef {
        const qb = try self.query_builder.selection("withEnum", .{
            .name = name,
            .description = options.description,
            .sourcemap = options.sourcemap,
        });
        return TypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Adds a static value for an Enum TypeDef, failing if the type is not an enum.
    pub fn withenummember(self: *@This(), name: []const u8, options: struct {
        value: ?[]const u8 = null,
        description: ?[]const u8 = null,
        sourcemap: ?SourceMapID = null,
        deprecated: ?[]const u8 = null,
    }) !TypeDef {
        const qb = try self.query_builder.selection("withEnumMember", .{
            .name = name,
            .value = options.value,
            .description = options.description,
            .sourcemap = options.sourcemap,
            .deprecated = options.deprecated,
        });
        return TypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Adds a static value for an Enum TypeDef, failing if the type is not an enum.
    /// @deprecated: Use `withEnumMember` instead
    pub fn withenumvalue(self: *@This(), value: []const u8, options: struct {
        description: ?[]const u8 = null,
        sourcemap: ?SourceMapID = null,
        deprecated: ?[]const u8 = null,
    }) !TypeDef {
        const qb = try self.query_builder.selection("withEnumValue", .{
            .value = value,
            .description = options.description,
            .sourcemap = options.sourcemap,
            .deprecated = options.deprecated,
        });
        return TypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Adds a static field for an Object TypeDef, failing if the type is not an object.
    pub fn withfield(self: *@This(), name: []const u8, typedef: TypeDefID, options: struct {
        description: ?[]const u8 = null,
        sourcemap: ?SourceMapID = null,
        deprecated: ?[]const u8 = null,
    }) !TypeDef {
        const qb = try self.query_builder.selection("withField", .{
            .name = name,
            .typedef = typedef,
            .description = options.description,
            .sourcemap = options.sourcemap,
            .deprecated = options.deprecated,
        });
        return TypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Adds a function for an Object or Interface TypeDef, failing if the type is not one of those kinds.
    pub fn withfunction(self: *@This(), function: FunctionID) !TypeDef {
        const qb = try self.query_builder.selection("withFunction", .{
            .function = function,
        });
        return TypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Returns a TypeDef of kind Interface with the provided name.
    pub fn withinterface(self: *@This(), name: []const u8, options: struct {
        description: ?[]const u8 = null,
        sourcemap: ?SourceMapID = null,
    }) !TypeDef {
        const qb = try self.query_builder.selection("withInterface", .{
            .name = name,
            .description = options.description,
            .sourcemap = options.sourcemap,
        });
        return TypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Sets the kind of the type.
    pub fn withkind(self: *@This(), kind: TypeDefKind) !TypeDef {
        const qb = try self.query_builder.selection("withKind", .{
            .kind = kind,
        });
        return TypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Returns a TypeDef of kind List with the provided type for its elements.
    pub fn withlistof(self: *@This(), elementtype: TypeDefID) !TypeDef {
        const qb = try self.query_builder.selection("withListOf", .{
            .elementtype = elementtype,
        });
        return TypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Returns a TypeDef of kind Object with the provided name.
    ///
    /// Note that an object's fields and functions may be omitted if the intent is only to refer to an object. This is how functions are able to return their own object, or any other circular reference.
    pub fn withobject(self: *@This(), name: []const u8, options: struct {
        description: ?[]const u8 = null,
        sourcemap: ?SourceMapID = null,
        deprecated: ?[]const u8 = null,
    }) !TypeDef {
        const qb = try self.query_builder.selection("withObject", .{
            .name = name,
            .description = options.description,
            .sourcemap = options.sourcemap,
            .deprecated = options.deprecated,
        });
        return TypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Sets whether this type can be set to null.
    pub fn withoptional(self: *@This(), optional: bool) !TypeDef {
        const qb = try self.query_builder.selection("withOptional", .{
            .optional = optional,
        });
        return TypeDef{ .query_builder = qb, .client = self.client };
    }

    /// Returns a TypeDef of kind Scalar with the provided name.
    pub fn withscalar(self: *@This(), name: []const u8, options: struct {
        description: ?[]const u8 = null,
    }) !TypeDef {
        const qb = try self.query_builder.selection("withScalar", .{
            .name = name,
            .description = options.description,
        });
        return TypeDef{ .query_builder = qb, .client = self.client };
    }
};

///
pub const Up = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// The description of the service
    pub fn description(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("description", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// A unique identifier for this Up.
    pub fn id(self: *@This()) !UpID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, UpID);
    }

    /// Return the fully qualified name of the service
    pub fn name(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("name", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// The original module in which the service has been defined
    pub fn originalmodule(self: *@This()) !Module {
        const qb = try self.query_builder.selection("originalModule", .{});
        return Module{ .query_builder = qb, .client = self.client };
    }

    /// The path of the service within its module
    pub fn path(self: *@This()) ![]const []const u8 {
        const qb = try self.query_builder.selection("path", .{});
        return try self.client.execute(qb, []const []const u8);
    }

    /// Execute the service function
    pub fn run(self: *@This()) !Up {
        const qb = try self.query_builder.selection("run", .{});
        return Up{ .query_builder = qb, .client = self.client };
    }
};

///
pub const UpGroup = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// A unique identifier for this UpGroup.
    pub fn id(self: *@This()) !UpGroupID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, UpGroupID);
    }

    /// Return a list of individual services and their details
    pub fn list(self: *@This()) ![]const Up {
        const qb = try self.query_builder.selection("list", .{});
        const ids = try self.client.execute(qb, []const UpID);
        var results = try self.client.allocator.alloc(Up, ids.len);
        for (ids, 0..) |id, i| {
            results[i] = try self.client.loadUpFromID(id);
        }
        return results;
    }

    /// Execute all selected service functions
    pub fn run(self: *@This()) !UpGroup {
        const qb = try self.query_builder.selection("run", .{});
        return UpGroup{ .query_builder = qb, .client = self.client };
    }
};

/// A Dagger workspace detected from the current working directory.
pub const Workspace = struct {
    query_builder: *QueryBuilder,
    client: *Client,

    /// Canonical Dagger address of the workspace directory.
    pub fn address(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("address", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Return all checks from modules loaded in the workspace.
    pub fn checks(self: *@This(), options: struct {
        include: ?[]const []const u8 = null,
    }) !CheckGroup {
        const qb = try self.query_builder.selection("checks", .{
            .include = options.include,
        });
        return CheckGroup{ .query_builder = qb, .client = self.client };
    }

    /// The client ID that owns this workspace's host filesystem.
    pub fn clientid(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("clientId", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Path to config.toml relative to the workspace boundary (empty if not initialized).
    pub fn configpath(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("configPath", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Returns a Directory from the workspace.
    ///
    /// Relative paths resolve from the workspace directory. Absolute paths resolve from the workspace boundary.
    pub fn directory(self: *@This(), path: []const u8, options: struct {
        exclude: ?[]const []const u8 = null,
        include: ?[]const []const u8 = null,
        gitignore: ?bool = null,
    }) !Directory {
        const qb = try self.query_builder.selection("directory", .{
            .path = path,
            .exclude = options.exclude,
            .include = options.include,
            .gitignore = options.gitignore,
        });
        return Directory{ .query_builder = qb, .client = self.client };
    }

    /// Returns a File from the workspace.
    ///
    /// Relative paths resolve from the workspace directory. Absolute paths resolve from the workspace boundary.
    pub fn file(self: *@This(), path: []const u8) !File {
        const qb = try self.query_builder.selection("file", .{
            .path = path,
        });
        return File{ .query_builder = qb, .client = self.client };
    }

    /// Search for a file or directory by walking up from the start path within the workspace.
    ///
    /// Returns the absolute workspace path if found, or null if not found.
    ///
    /// Relative start paths resolve from the workspace directory.
    ///
    /// The search stops at the workspace boundary and will not traverse above it.
    pub fn findup(self: *@This(), name: []const u8, options: struct {
        from: ?[]const u8 = null,
    }) !?[]const u8 {
        const qb = try self.query_builder.selection("findUp", .{
            .name = name,
            .from = options.from,
        });
        return try self.client.execute(qb, ?[]const u8);
    }

    /// Return all generators from modules loaded in the workspace.
    pub fn generators(self: *@This(), options: struct {
        include: ?[]const []const u8 = null,
    }) !GeneratorGroup {
        const qb = try self.query_builder.selection("generators", .{
            .include = options.include,
        });
        return GeneratorGroup{ .query_builder = qb, .client = self.client };
    }

    /// Whether a config.toml file exists in the workspace.
    pub fn hasconfig(self: *@This()) !bool {
        const qb = try self.query_builder.selection("hasConfig", .{});
        return try self.client.execute(qb, bool);
    }

    /// A unique identifier for this Workspace.
    pub fn id(self: *@This()) !WorkspaceID {
        const qb = try self.query_builder.selection("id", .{});
        return try self.client.execute(qb, WorkspaceID);
    }

    /// Whether .dagger/config.toml exists.
    pub fn initialized(self: *@This()) !bool {
        const qb = try self.query_builder.selection("initialized", .{});
        return try self.client.execute(qb, bool);
    }

    /// Workspace directory path relative to the workspace boundary.
    pub fn path(self: *@This()) ![]const u8 {
        const qb = try self.query_builder.selection("path", .{});
        return try self.client.execute(qb, []const u8);
    }

    /// Return all services from modules loaded in the workspace.
    pub fn services(self: *@This(), options: struct {
        include: ?[]const []const u8 = null,
    }) !UpGroup {
        const qb = try self.query_builder.selection("services", .{
            .include = options.include,
        });
        return UpGroup{ .query_builder = qb, .client = self.client };
    }
};
